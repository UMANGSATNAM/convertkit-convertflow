<div class="modern-grid" style="padding: 100px 40px; background-color: #0f1115; text-align: center;">
  <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 30px;">
    {% for product in collections['all'].products limit: 4 %}
      <div class="product-card" style="text-align: left; background: #181a20; padding: 20px; border-radius: var(--radius, 16px); border: 1px solid rgba(255,255,255,0.05); transition: transform 0.3s ease, box-shadow 0.3s ease;" onmouseover="this.style.transform='translateY(-5px)'; this.style.boxShadow='0 20px 40px rgba(0,0,0,0.4)'" onmouseout="this.style.transform='translateY(0)'; this.style.boxShadow='none'">
        <a href="{{ product.url }}" style="text-decoration: none;">
          <div style="width: 100%; padding-top: 100%; position: relative; background-color: #20222a; border-radius: calc(var(--radius, 16px) - 8px); overflow: hidden;">
            <img src="{{ product.featured_image | img_url: '600x' }}" alt="{{ product.title }}" style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; object-fit: cover;">
          </div>
          <div style="display: flex; justify-content: space-between; align-items: center; margin-top: 20px;">
            <h3 style="font-family: var(--fontHeading, 'Space Grotesk', sans-serif); font-size: 18px; font-weight: 600; color: #ffffff; margin: 0;">{{ product.title }}</h3>
            <span style="font-family: var(--fontBody, 'Inter', sans-serif); font-size: 16px; font-weight: 500; color: #3b82f6;">{{ product.price | money }}</span>
          </div>
        </a>
      </div>
    {% else %}
      <!-- Mock Data -->
      {% for i in (1..4) %}
        <div class="product-card" style="text-align: left; background: #181a20; padding: 20px; border-radius: var(--radius, 16px); border: 1px solid rgba(255,255,255,0.05);">
          <div style="width: 100%; padding-top: 100%; position: relative; background-color: #20222a; border-radius: calc(var(--radius, 16px) - 8px);"></div>
          <div style="display: flex; justify-content: space-between; align-items: center; margin-top: 20px;">
            <h3 style="font-family: var(--fontHeading, 'Space Grotesk', sans-serif); font-size: 18px; font-weight: 600; color: #ffffff; margin: 0;">NextGen Device {{ i }}</h3>
            <span style="font-family: var(--fontBody, 'Inter', sans-serif); font-size: 16px; font-weight: 500; color: #3b82f6;">$899</span>
          </div>
        </div>
      {% endfor %}
    {% endfor %}
  </div>
</div>

{% schema %}
{
  "name": "Modern Product Grid",
  "settings": []
}
{% endschema %}
