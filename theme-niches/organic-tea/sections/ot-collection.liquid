<div style="padding:60px 20px 0;background:var(--ot-paper)"><div style="max-width:1320px;margin:0 auto;text-align:center;padding-bottom:56px;border-bottom:1px solid var(--ot-border)"><span class="ot-eyebrow" style="margin-bottom:8px">{{ collection.title | upcase }}</span><h1 style="font-family:'Cormorant Garamond',serif;font-size:clamp(32px,5vw,52px);font-weight:400;color:var(--ot-dark);margin-bottom:12px">{{ collection.title }}</h1>{%- if collection.description != blank -%}<p style="font-family:'Lora',serif;font-style:italic;font-size:15px;color:var(--ot-muted);max-width:560px;margin:0 auto">{{ collection.description }}</p>{%- endif -%}</div></div>
<div style="padding:0 20px 100px;background:var(--ot-paper)"><div style="max-width:1320px;margin:0 auto"><div style="display:flex;justify-content:space-between;align-items:center;padding:24px 0;border-bottom:1px solid var(--ot-border);margin-bottom:40px"><div style="font-family:'Lora',serif;font-style:italic;font-size:14px;color:var(--ot-muted)">{{ collection.products_count }} garden teas</div><select style="padding:10px 20px;border:1px solid var(--ot-border);border-radius:8px;background:#fff;color:var(--ot-dark);font-family:'Inter',sans-serif;font-size:12px;font-weight:600;cursor:pointer"><option>Featured</option><option>Price: Low to High</option><option>Price: High to Low</option><option>Newest</option></select></div><div class="ot-prod-grid">{%- for product in collection.products -%}<a href="{{ product.url }}" class="ot-prod-card"><div class="ot-prod-card__img">{%- if product.featured_image != blank -%}<img src="{{ product.featured_image | image_url: width: 600 }}" alt="{{ product.title | escape }}" width="600" loading="lazy">{%- else -%}<div style="width:100%;height:100%;background:var(--ot-paper)"></div>{%- endif -%}</div><div class="ot-prod-card__name">{{ product.title }}</div><div class="ot-prod-card__price">{{ product.price | money }}</div></a>{%- endfor -%}</div></div></div>
{% schema %}
{"name":"Tea Collection","settings":[],"presets":[{"name":"Tea Collection"}]}
{% endschema %}
