<div style="padding:60px 20px 0;background:var(--cu-sand)"><div style="max-width:1320px;margin:0 auto;text-align:center;padding-bottom:56px;border-bottom:1px solid var(--cu-border)"><span class="cu-eyebrow" style="margin-bottom:8px">{{ collection.title | upcase }}</span><h1 style="font-family:'Cormorant Garamond',serif;font-size:clamp(32px,5vw,52px);font-weight:500;color:var(--cu-dark);margin-bottom:12px;font-style:italic">{{ collection.title }}</h1>{%- if collection.description != blank -%}<p style="font-family:'Outfit',sans-serif;font-size:15px;color:var(--cu-muted);max-width:560px;margin:0 auto;line-height:1.7">{{ collection.description }}</p>{%- endif -%}</div></div>
<div style="padding:0 20px 100px;background:var(--cu-sand)"><div style="max-width:1320px;margin:0 auto"><div style="display:flex;justify-content:space-between;align-items:center;padding:24px 0;border-bottom:1px solid var(--cu-border);margin-bottom:40px"><div style="font-family:'Outfit',sans-serif;font-size:13px;font-weight:700;color:var(--cu-dark);text-transform:uppercase">{{ collection.products_count }} vessels</div><select style="padding:10px 20px;border:1px solid var(--cu-border);border-radius:8px;background:#fff;color:var(--cu-dark);font-family:'Outfit',sans-serif;font-size:12px;font-weight:700;cursor:pointer"><option>Featured</option><option>Price: Low to High</option><option>Price: High to Low</option><option>Newest</option></select></div><div class="cu-prod-grid">{%- for product in collection.products -%}<a href="{{ product.url }}" class="cu-prod-card"><div class="cu-prod-card__img">{%- if product.featured_image != blank -%}<img src="{{ product.featured_image | image_url: width: 600 }}" alt="{{ product.title | escape }}" width="600" loading="lazy">{%- else -%}<div style="width:100%;height:100%;background:var(--cu-sand)"></div>{%- endif -%}</div><div class="cu-prod-card__name">{{ product.title }}</div><div class="cu-prod-card__price">{{ product.price | money }}</div></a>{%- endfor -%}</div></div></div>
{% schema %}
{"name":"Clay Collection","settings":[],"presets":[{"name":"Clay Collection"}]}
{% endschema %}
