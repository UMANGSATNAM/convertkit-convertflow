<div style="padding:80px 20px;background:var(--mg-warm)"><div style="max-width:1320px;margin:0 auto">
  <div class="mg-section-header"><p class="mg-eyebrow">{{ section.settings.eyebrow }}</p><h2>{{ section.settings.heading }}</h2></div>
  {%- assign col = collections[section.settings.collection] -%}
  <div class="mg-prod-grid">{%- for product in col.products limit: section.settings.count -%}<a href="{{ product.url }}" class="mg-prod-card"><div class="mg-prod-card__img">{%- if product.featured_image != blank -%}<img src="{{ product.featured_image | image_url: width: 600 }}" alt="{{ product.title | escape }}" width="600" loading="lazy">{%- else -%}<div style="width:100%;height:100%;background:var(--mg-sand)"></div>{%- endif -%}</div><div class="mg-prod-card__name">{{ product.title }}</div><div class="mg-prod-card__price">{{ product.price | money }}</div></a>{%- endfor -%}</div>
  <div style="text-align:center;margin-top:48px"><a href="{{ section.settings.cta_url | default: '/collections/all' }}" class="mg-btn mg-btn--outline">{{ section.settings.cta_label }}</a></div>
</div></div>
{% schema %}
{"name":"MG Featured","settings":[{"type":"text","id":"eyebrow","label":"Eyebrow","default":"BESTSELLERS"},{"type":"text","id":"heading","label":"Heading","default":"Most Wanted"},{"type":"collection","id":"collection","label":"Collection"},{"type":"range","id":"count","label":"Products","min":4,"max":12,"step":4,"default":8},{"type":"text","id":"cta_label","label":"CTA","default":"VIEW ALL"},{"type":"url","id":"cta_url","label":"CTA URL"}],"presets":[{"name":"MG Featured"}]}
{% endschema %}
