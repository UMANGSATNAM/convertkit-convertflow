<div style="padding:80px 20px;background:var(--cr-cream)"><div style="max-width:1320px;margin:0 auto">
  <div class="cr-section-header"><p class="cr-eyebrow">{{ section.settings.eyebrow }}</p><h2>{{ section.settings.heading }}</h2></div>
  {%- assign col = collections[section.settings.collection] -%}
  <div class="cr-prod-grid">{%- for product in col.products limit: section.settings.count -%}<a href="{{ product.url }}" class="cr-prod-card"><div class="cr-prod-card__img">{%- if product.featured_image != blank -%}<img src="{{ product.featured_image | image_url: width: 600 }}" alt="{{ product.title | escape }}" width="600" loading="lazy">{%- else -%}<div style="width:100%;height:100%;background:var(--cr-warm)"></div>{%- endif -%}</div><div class="cr-prod-card__name">{{ product.title }}</div><div class="cr-prod-card__price">{{ product.price | money }}</div><div class="cr-prod-card__meta">{{ product.type }}</div></a>{%- endfor -%}</div>
  <div style="text-align:center;margin-top:48px"><a href="{{ section.settings.cta_url | default: '/collections/all' }}" class="cr-btn cr-btn--outline">{{ section.settings.cta_label }}</a></div>
</div></div>
{% schema %}
{"name":"CR Featured","settings":[{"type":"text","id":"eyebrow","label":"Eyebrow","default":"FRESH ROASTS"},{"type":"text","id":"heading","label":"Heading","default":"This Week's Roasts"},{"type":"collection","id":"collection","label":"Collection"},{"type":"range","id":"count","label":"Products","min":4,"max":12,"step":4,"default":8},{"type":"text","id":"cta_label","label":"CTA","default":"ALL COFFEES"},{"type":"url","id":"cta_url","label":"CTA URL"}],"presets":[{"name":"CR Featured"}]}
{% endschema %}
