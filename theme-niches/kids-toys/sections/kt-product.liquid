<div style="padding:48px 20px 80px;background:var(--kt-pastel)"><div style="max-width:1320px;margin:0 auto">
  <nav style="font-size:13px;color:var(--kt-muted);font-family:'Fredoka',sans-serif;margin-bottom:32px" aria-label="Breadcrumb"><a href="/" style="color:var(--kt-muted)">Home</a> / <a href="{{ product.collections.first.url }}" style="color:var(--kt-muted)">{{ product.collections.first.title }}</a> / <span style="color:var(--kt-dark)">{{ product.title }}</span></nav>
  <div style="display:grid;grid-template-columns:1fr 1fr;gap:64px;align-items:start">
    <div data-gallery>
      <div style="border-radius:16px;overflow:hidden;background:#fff;border:2px solid var(--kt-border);aspect-ratio:1;margin-bottom:12px;cursor:zoom-in"><img id="MainProductImg" data-main-img src="{{ product.featured_image | image_url: width: 900 }}" alt="{{ product.title | escape }}" width="900" style="width:100%;height:100%;object-fit:cover"></div>
      <div style="display:flex;gap:8px;flex-wrap:wrap">{%- for img in product.images limit: 6 -%}<div data-thumb data-src="{{ img | image_url: width: 900 }}" style="width:72px;height:72px;border-radius:8px;overflow:hidden;cursor:pointer;border:2px solid {% if forloop.first %}var(--kt-blue){% else %}transparent{% endif %};transition:border-color .2s"><img src="{{ img | image_url: width: 144 }}" alt="" width="72" height="72" style="width:100%;height:100%;object-fit:cover"></div>{%- endfor -%}</div>
    </div>
    <div>
      <span class="kt-eyebrow" style="margin-bottom:8px">{{ product.vendor }}</span>
      <h1 style="font-family:'Fredoka',sans-serif;font-size:32px;font-weight:600;color:var(--kt-dark);margin-bottom:12px">{{ product.title }}</h1>
      <div style="display:flex;align-items:center;gap:8px;margin-bottom:16px"><span style="color:var(--kt-yellow);font-size:15px;letter-spacing:2px">★★★★★</span><span style="font-size:13px;color:var(--kt-muted);font-family:'Fredoka',sans-serif">({{ product.metafields.reviews.rating_count | default: '190' }} parents)</span></div>
      <div style="display:flex;align-items:center;gap:12px;margin-bottom:20px;padding-bottom:20px;border-bottom:2px solid var(--kt-border)">
        <span style="font-family:'Fredoka',sans-serif;font-size:28px;font-weight:600;color:var(--kt-blue)" data-price>{{ product.price | money }}</span>
        {%- if product.compare_at_price > product.price -%}<span style="font-family:'Fredoka',sans-serif;font-size:18px;color:var(--kt-muted);text-decoration:line-through" data-compare-price>{{ product.compare_at_price | money }}</span>{%- endif -%}
      </div>
      <!-- Toy Details Grid -->
      <div style="display:flex;gap:8px;margin-bottom:20px;flex-wrap:wrap"><span style="padding:6px 14px;background:#fff;border:2px solid var(--kt-border);border-radius:30px;font-size:12px;font-weight:600;color:var(--kt-dark)">100% Non-Toxic</span><span style="padding:6px 14px;background:#fff;border:2px solid var(--kt-border);border-radius:30px;font-size:12px;font-weight:600;color:var(--kt-dark)">Beech Wood</span><span style="padding:6px 14px;background:#fff;border:2px solid var(--kt-border);border-radius:30px;font-size:12px;font-weight:600;color:var(--kt-dark)">CE Certified</span></div>
      <form action="{{ routes.cart_add_url }}" method="post" id="ProductForm">
        <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
        {%- unless product.has_only_default_variant -%}{%- for option in product.options_with_values -%}<div style="margin-bottom:20px"><label style="font-family:'Fredoka',sans-serif;font-size:13px;font-weight:600;display:block;margin-bottom:10px;color:var(--kt-dark)">{{ option.name }}</label><div style="display:flex;gap:8px;flex-wrap:wrap" data-swatches>{%- for value in option.values -%}<button type="button" data-swatch="{{ value }}" style="padding:10px 20px;border:2px solid {% if option.selected_value == value %}var(--kt-blue){% else %}var(--kt-border){% endif %};background:{% if option.selected_value == value %}var(--kt-blue){% else %}#fff{% endif %};color:{% if option.selected_value == value %}#fff{% else %}var(--kt-dark){% endif %};font-family:'Fredoka',sans-serif;font-size:13px;font-weight:600;cursor:pointer;border-radius:30px;transition:all .2s">{{ value }}</button>{%- endfor -%}</div></div>{%- endfor -%}<select name="id" data-variant-select style="display:none">{%- for variant in product.variants -%}<option value="{{ variant.id }}" {% if variant == product.selected_or_first_available_variant %}selected{% endif %} {% unless variant.available %}disabled{% endunless %}>{{ variant.title }}</option>{%- endfor -%}</select>{%- endunless -%}
        <div style="display:flex;gap:12px;margin-bottom:12px">
          <div style="display:flex;align-items:center;border:2px solid var(--kt-border);border-radius:30px;background:#fff;overflow:hidden"><button type="button" data-dir="down" style="width:44px;height:52px;background:none;border:none;font-size:20px;cursor:pointer">−</button><input type="number" name="quantity" value="1" min="1" style="width:48px;height:52px;text-align:center;border:none;font-size:16px;font-weight:600;font-family:'Fredoka',sans-serif"><button type="button" data-dir="up" style="width:44px;height:52px;background:none;border:none;font-size:20px;cursor:pointer">+</button></div>
          <button type="submit" class="kt-btn" data-atc style="flex:1;justify-content:center" {% unless product.available %}disabled{% endunless %}>{%- if product.available -%}ACQUIRE TOY{%- else -%}OUT OF STOCK{%- endif -%}</button>
        </div>
      </form>
      <!-- Trust List -->
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:20px 0">
        <div style="display:flex;align-items:center;gap:8px;padding:10px;background:#fff;border-radius:16px;font-size:12px;color:var(--kt-dark);border:2px solid var(--kt-border)"><span style="color:var(--kt-blue)">✓</span> certified non-toxic</div>
        <div style="display:flex;align-items:center;gap:8px;padding:10px;background:#fff;border-radius:16px;font-size:12px;color:var(--kt-dark);border:2px solid var(--kt-border)"><span style="color:var(--kt-blue)">✓</span> premium wood guarantee</div>
      </div>
      <div style="font-family:'Fredoka',sans-serif;font-size:15px;line-height:1.7;color:var(--kt-muted)">{{ product.description }}</div>
    </div>
  </div>
</div></div>
{% schema %}
{"name":"Toys Product","settings":[],"presets":[{"name":"Toys Product"}]}
{% endschema %}
