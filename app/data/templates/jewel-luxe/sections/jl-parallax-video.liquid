<style>
  .jl-parallax-sec {
    position: relative;
    height: 80vh;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    clip-path: inset(0);
  }
  .jl-parallax-vid {
    position: fixed;
    top: 0; left: 0;
    width: 100%; height: 100%;
    object-fit: cover;
    z-index: -1;
    pointer-events: none;
  }
  .jl-parallax-overlay {
    position: absolute;
    inset: 0;
    background: rgba(0,0,0,0.5);
  }
  .jl-parallax-content {
    position: relative;
    z-index: 2;
    text-align: center;
    color: #fff;
    font-family: var(--jl-font-sans, sans-serif);
  }
  .jl-parallax-title {
    font-size: clamp(40px, 6vw, 80px);
    font-weight: 800;
    margin-bottom: 20px;
    letter-spacing: -2px;
  }
  .jl-parallax-btn {
    display: inline-block;
    padding: 16px 40px;
    background: #d4af37;
    color: #000;
    text-transform: uppercase;
    text-decoration: none;
    font-weight: 700;
    letter-spacing: 1px;
    border-radius: 4px;
    transition: transform 0.3s;
  }
  .jl-parallax-btn:hover {
    transform: scale(1.05);
  }
</style>

<section class="jl-parallax-sec">
  <video class="jl-parallax-vid" autoplay muted loop playsinline>
    <source src="{{ section.settings.video_url | default: 'https://cdn.shopify.com/videos/c/o/v/02d0ec7849e742c3809cbceb7405e5d3.mp4' }}" type="video/mp4">
  </video>
  <div class="jl-parallax-overlay"></div>
  <div class="jl-parallax-content">
    <h2 class="jl-parallax-title">{{ section.settings.title | default: 'Feel the Motion' }}</h2>
    <a href="#" class="jl-parallax-btn">{{ section.settings.btn | default: 'Discover More' }}</a>
  </div>
</section>

{% schema %}
{
  "name": "Parallax Video",
  "tag": "section",
  "settings": [
    { "type": "text", "id": "video_url", "label": "Video URL" },
    { "type": "text", "id": "title", "label": "Heading" }
  ],
  "presets": [{ "name": "Parallax Video" }]
}
{% endschema %}
