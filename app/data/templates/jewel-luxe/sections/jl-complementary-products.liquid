<style>
  .jl-complementary {
    padding: 80px 5%;
    background-color: {{ section.settings.bg_color }};
  }
  .jl-comp__title {
    font-family: 'Georgia', serif;
    font-size: 24px;
    font-weight: normal;
    text-align: center;
    margin-bottom: 40px;
  }
  .jl-comp__grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
    max-width: 1200px;
    margin: 0 auto;
  }
  @media (min-width: 768px) {
    .jl-comp__grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }
  .jl-comp-card {
    display: block;
    text-decoration: none;
    color: #000;
    font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
  }
  .jl-comp-card__img {
    width: 100%;
    aspect-ratio: 4/5;
    object-fit: cover;
    margin-bottom: 10px;
  }
  .jl-comp-card__title {
    font-size: 13px;
    text-transform: uppercase;
    margin-bottom: 5px;
  }
  .jl-comp-card__price {
    font-size: 13px;
    color: #666;
  }
</style>

<section class="jl-complementary">
  <h2 class="jl-comp__title">{{ section.settings.title }}</h2>
  
  <div class="jl-comp__grid">
    {% assign count = 0 %}
    {% for related in collections.all.products limit: section.settings.limit %}
      {% if related.id != product.id %}
        {% assign count = count | plus: 1 %}
        <a href="{{ related.url }}" class="jl-comp-card">
          <img class="jl-comp-card__img" src="{{ related.featured_image | image_url: width: 400 }}" alt="{{ related.title | escape }}">
          <div class="jl-comp-card__title">{{ related.title }}</div>
          <div class="jl-comp-card__price">{{ related.price | money }}</div>
        </a>
      {% endif %}
    {% endfor %}
    
    {% if count == 0 %}
      <p style="text-align:center; grid-column: 1/-1;">No complementary products found.</p>
    {% endif %}
  </div>
</section>

{% schema %}
{
  "name": "JL Complementary",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Complete The Look"
    },
    {
      "type": "range",
      "id": "limit",
      "min": 2,
      "max": 8,
      "step": 1,
      "label": "Limit",
      "default": 4
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#ffffff"
    }
  ],
  "templates": ["product"],
  "presets": [
    {
      "name": "JL Complementary"
    }
  ]
}
{% endschema %}
