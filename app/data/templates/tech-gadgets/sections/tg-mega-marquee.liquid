<style>
  .tg-marquee-wrapper {
    overflow: hidden;
    padding: 80px 0;
    background: var(--tg-color-bg, #0a0a0a);
    color: var(--tg-color-text, #fff);
    border-top: 1px solid rgba(255,255,255,0.05);
    border-bottom: 1px solid rgba(255,255,255,0.05);
  }
  .tg-marquee {
    display: flex;
    white-space: nowrap;
    animation: tg-marquee-scroll 30s linear infinite;
  }
  .tg-marquee:hover {
    animation-play-state: paused;
  }
  .tg-marquee-text {
    font-family: var(--tg-font-sans, sans-serif);
    font-size: clamp(60px, 8vw, 120px);
    font-weight: 900;
    text-transform: uppercase;
    padding: 0 40px;
    letter-spacing: -2px;
    -webkit-text-stroke: 1px rgba(255,255,255,0.2);
    color: transparent;
    transition: all 0.3s ease;
    cursor: default;
  }
  .tg-marquee-text:hover {
    color: #00ffff;
    -webkit-text-stroke: 0;
    text-shadow: 0 0 40px #00ffff80;
  }
  @keyframes tg-marquee-scroll {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }
</style>

<div class="tg-marquee-wrapper">
  <div class="tg-marquee">
    <div class="tg-marquee-text">{{ section.settings.text | default: 'UNCOMPROMISING QUALITY • INNOVATIVE DESIGN • EXCEPTIONAL EXPERIENCE •' }}</div>
    <div class="tg-marquee-text">{{ section.settings.text | default: 'UNCOMPROMISING QUALITY • INNOVATIVE DESIGN • EXCEPTIONAL EXPERIENCE •' }}</div>
  </div>
</div>

{% schema %}
{
  "name": "Mega Marquee",
  "tag": "section",
  "settings": [
    { "type": "text", "id": "text", "label": "Marquee Text" }
  ],
  "presets": [{ "name": "Mega Marquee" }]
}
{% endschema %}
