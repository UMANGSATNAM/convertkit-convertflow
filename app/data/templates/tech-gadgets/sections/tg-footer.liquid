<style>
  .tg-footer {
    background: var(--tg-color-bg, #0a0a0a);
    color: var(--tg-color-text, #fff);
    padding: 80px 40px 40px;
    border-top: 1px solid rgba(255,255,255,0.05);
    font-family: var(--tg-font-sans, sans-serif);
  }
  .tg-footer-grid {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr 1fr;
    gap: 40px;
    max-width: 1440px;
    margin: 0 auto;
  }
  .tg-footer-brand {
    font-size: 24px;
    font-weight: 800;
    letter-spacing: 2px;
    margin-bottom: 20px;
  }
  .tg-footer-title {
    font-size: 14px;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 20px;
    color: #00ffff;
  }
  .tg-footer-link {
    display: block;
    color: inherit;
    text-decoration: none;
    margin-bottom: 12px;
    opacity: 0.7;
    transition: all 0.3s;
    font-size: 14px;
  }
  .tg-footer-link:hover {
    opacity: 1;
    transform: translateX(5px);
    color: #00ffff;
  }
  .tg-newsletter-form {
    display: flex;
    margin-top: 20px;
  }
  .tg-newsletter-input {
    flex: 1;
    padding: 12px 16px;
    background: rgba(255,255,255,0.05);
    border: 1px solid rgba(255,255,255,0.1);
    color: inherit;
    border-radius: 4px 0 0 4px;
  }
  .tg-newsletter-btn {
    padding: 12px 24px;
    background: #00ffff;
    color: #000;
    border: none;
    font-weight: 700;
    cursor: pointer;
    border-radius: 0 4px 4px 0;
    transition: opacity 0.3s;
  }
  .tg-newsletter-btn:hover {
    opacity: 0.9;
  }
  .tg-footer-bottom {
    max-width: 1440px;
    margin: 60px auto 0;
    padding-top: 20px;
    border-top: 1px solid rgba(255,255,255,0.1);
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 12px;
    opacity: 0.5;
  }
  .tg-payment-icons {
    display: flex;
    gap: 10px;
  }
  @media (max-width: 900px) {
    .tg-footer-grid { grid-template-columns: 1fr; }
    .tg-footer-bottom { flex-direction: column; gap: 20px; text-align: center; }
  }
</style>

<footer class="tg-footer">
  <div class="tg-footer-grid">
    <div>
      <div class="tg-footer-brand">{{ section.settings.logo_text | default: 'BRAND' }}</div>
      <p style="opacity: 0.7; font-size: 14px; max-width: 300px;">{{ section.settings.about_text | default: 'Crafting premium experiences through innovative design and exceptional quality.' }}</p>
      
      <form class="tg-newsletter-form">
        <input type="email" class="tg-newsletter-input" placeholder="Join our newsletter">
        <button type="submit" class="tg-newsletter-btn">→</button>
      </form>
    </div>
    
    <div>
      <h4 class="tg-footer-title">Shop</h4>
      {% for link in linklists.footer.links %}
        <a href="{{ link.url }}" class="tg-footer-link">{{ link.title }}</a>
      {% endfor %}
      <a href="#" class="tg-footer-link">New Arrivals</a>
      <a href="#" class="tg-footer-link">Best Sellers</a>
      <a href="#" class="tg-footer-link">Collections</a>
    </div>

    <div>
      <h4 class="tg-footer-title">Support</h4>
      <a href="#" class="tg-footer-link">Contact Us</a>
      <a href="#" class="tg-footer-link">FAQ</a>
      <a href="#" class="tg-footer-link">Shipping & Returns</a>
      <a href="#" class="tg-footer-link">Track Order</a>
    </div>

    <div>
      <h4 class="tg-footer-title">Social</h4>
      <a href="#" class="tg-footer-link">Instagram</a>
      <a href="#" class="tg-footer-link">TikTok</a>
      <a href="#" class="tg-footer-link">Twitter</a>
    </div>
  </div>
  
  <div class="tg-footer-bottom">
    <div>&copy; {{ 'now' | date: '%Y' }} {{ section.settings.logo_text | default: 'BRAND' }}. All rights reserved.</div>
    <div class="tg-payment-icons">
      <!-- Visa SVG -->
      <svg width="32" height="20" viewBox="0 0 32 20" fill="currentColor"><path d="M10.5 4.5l-2.5 11h-3l1.5-11h4zm8.5 0c-1 0-2 .5-2 1.5s1.5 1.5 1.5 2.5-1 1.5-2 1.5-1.5-.5-1.5-.5l-.5 2.5c.5.5 1.5.5 2.5.5 2 0 3.5-1 3.5-3 0-1.5-1.5-2-1.5-2.5s1-1 2-1h1.5l-1-2.5c-.5-.5-1.5-.5-2.5-.5zm6.5 0l-2 7.5-1-7.5h-3l2.5 11h3l3.5-11h-3z"/></svg>
      <!-- Mastercard SVG -->
      <svg width="32" height="20" viewBox="0 0 32 20" fill="currentColor"><circle cx="10" cy="10" r="6"/><circle cx="20" cy="10" r="6"/></svg>
    </div>
  </div>
</footer>

{% schema %}
{
  "name": "Premium Footer",
  "tag": "section",
  "settings": [
    { "type": "text", "id": "logo_text", "label": "Logo Text", "default": "BRAND" },
    { "type": "textarea", "id": "about_text", "label": "About Text" }
  ]
}
{% endschema %}
