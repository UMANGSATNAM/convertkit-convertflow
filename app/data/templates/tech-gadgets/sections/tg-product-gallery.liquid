<style>
  .tg-pgallery {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  @media (min-width: 768px) {
    .tg-pgallery {
      gap: 20px;
    }
  }
  .tg-pgallery__item {
    width: 100%;
    background-color: #f4f4f4;
  }
  .tg-pgallery__img {
    width: 100%;
    height: auto;
    display: block;
  }
</style>

<div class="tg-pgallery">
  {% for media in product.media %}
    <div class="tg-pgallery__item">
      {% case media.media_type %}
        {% when 'image' %}
          <img class="tg-pgallery__img" src="{{ media | image_url: width: 1200 }}" alt="{{ media.alt | escape }}">
        {% when 'video' %}
          {{ media | video_tag: controls: true, class: 'tg-pgallery__img' }}
      {% endcase %}
    </div>
  {% else %}
    <div class="tg-pgallery__item" style="padding: 100px; text-align: center;">No Images</div>
  {% endfor %}
</div>

{% schema %}
{
  "name": "TG Product Gallery",
  "tag": "section",
  "settings": [],
  "templates": ["product"],
  "presets": [
    {
      "name": "TG Product Gallery"
    }
  ]
}
{% endschema %}
