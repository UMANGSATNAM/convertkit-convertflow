<style>
  .tg-brand-story {
    display: flex;
    flex-direction: column;
    background-color: {{ section.settings.bg_color }};
  }
  @media (min-width: 768px) {
    .tg-brand-story {
      flex-direction: row;
      align-items: center;
    }
    .tg-brand-story--reverse {
      flex-direction: row-reverse;
    }
  }
  .tg-bs__image-col {
    width: 100%;
    position: relative;
  }
  @media (min-width: 768px) {
    .tg-bs__image-col {
      width: 50%;
    }
  }
  .tg-bs__image-wrap {
    position: relative;
    padding-bottom: 120%;
  }
  @media (min-width: 768px) {
    .tg-bs__image-wrap {
      padding-bottom: 100%;
    }
  }
  .tg-bs__image {
    position: absolute;
    top: 0; left: 0; width: 100%; height: 100%;
    object-fit: cover;
  }
  .tg-bs__content-col {
    width: 100%;
    padding: 60px 5%;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  @media (min-width: 768px) {
    .tg-bs__content-col {
      width: 50%;
      padding: 80px 8%;
    }
  }
  .tg-bs__content {
    max-width: 500px;
    text-align: {{ section.settings.text_alignment }};
  }
  .tg-bs__title {
    font-family: 'Georgia', serif;
    font-size: clamp(28px, 4vw, 42px);
    font-weight: normal;
    color: {{ section.settings.text_color }};
    margin-bottom: 25px;
    line-height: 1.2;
  }
  .tg-bs__text {
    font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
    font-size: 15px;
    line-height: 1.6;
    color: {{ section.settings.text_color }};
    margin-bottom: 35px;
  }
  .tg-bs__btn {
    display: inline-block;
    border: 1px solid {{ section.settings.text_color }};
    color: {{ section.settings.text_color }};
    padding: 12px 30px;
    text-decoration: none;
    font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
    font-size: 13px;
    text-transform: uppercase;
    letter-spacing: 1px;
    transition: all 0.3s ease;
  }
  .tg-bs__btn:hover {
    background-color: {{ section.settings.text_color }};
    color: {{ section.settings.bg_color }};
  }
</style>

<section class="tg-brand-story {% if section.settings.image_position == 'right' %}tg-brand-story--reverse{% endif %}">
  <div class="tg-bs__image-col">
    <div class="tg-bs__image-wrap">
      {% if section.settings.image %}
        <img class="tg-bs__image" src="{{ section.settings.image | image_url: width: 1200 }}" alt="{{ section.settings.title | escape }}">
      {% else %}
        <div class="tg-bs__image" style="background-color: #e0e0e0;"></div>
      {% endif %}
    </div>
  </div>
  
  <div class="tg-bs__content-col">
    <div class="tg-bs__content">
      <h2 class="tg-bs__title">{{ section.settings.title }}</h2>
      <div class="tg-bs__text">{{ section.settings.text }}</div>
      {% if section.settings.btn_label != blank %}
        <a href="{{ section.settings.btn_link }}" class="tg-bs__btn">{{ section.settings.btn_label }}</a>
      {% endif %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "TG Brand Story",
  "tag": "section",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "select",
      "id": "image_position",
      "label": "Image Position",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "right", "label": "Right" }
      ],
      "default": "left"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Our Story"
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "Text",
      "default": "<p>Share details about your brand's origins, materials, or vision for the future.</p>"
    },
    {
      "type": "select",
      "id": "text_alignment",
      "label": "Text Alignment",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" }
      ],
      "default": "left"
    },
    {
      "type": "text",
      "id": "btn_label",
      "label": "Button Label",
      "default": "Read More"
    },
    {
      "type": "url",
      "id": "btn_link",
      "label": "Button Link"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#f9f9f9"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#000000"
    }
  ],
  "presets": [
    {
      "name": "TG Brand Story"
    }
  ]
}
{% endschema %}
