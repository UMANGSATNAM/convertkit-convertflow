<style>
  .pj-hotspots-section {
    padding: 80px 20px;
    display: flex;
    justify-content: center;
  }
  .pj-hotspots-container {
    position: relative;
    max-width: 1000px;
    width: 100%;
    border-radius: 20px;
    overflow: hidden;
  }
  .pj-hotspots-img {
    width: 100%;
    height: auto;
    display: block;
  }
  .pj-hotspot {
    position: absolute;
    width: 24px;
    height: 24px;
    background: #ffa500;
    border-radius: 50%;
    transform: translate(-50%, -50%);
    cursor: pointer;
    box-shadow: 0 0 0 0 #ffa50080;
    animation: pj-pulse 2s infinite;
  }
  .pj-hotspot:hover {
    animation: none;
    transform: translate(-50%, -50%) scale(1.2);
  }
  .pj-hotspot-tooltip {
    position: absolute;
    bottom: 130%;
    left: 50%;
    transform: translateX(-50%);
    background: rgba(0,0,0,0.9);
    color: #fff;
    padding: 12px 20px;
    border-radius: 8px;
    font-family: var(--pj-font-sans, sans-serif);
    font-size: 14px;
    white-space: nowrap;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s;
    pointer-events: none;
    border: 1px solid rgba(255,255,255,0.1);
    backdrop-filter: blur(10px);
  }
  .pj-hotspot:hover .pj-hotspot-tooltip {
    opacity: 1;
    visibility: visible;
    bottom: 150%;
  }
  @keyframes pj-pulse {
    0% { box-shadow: 0 0 0 0 #ffa50080; }
    70% { box-shadow: 0 0 0 15px transparent; }
    100% { box-shadow: 0 0 0 0 transparent; }
  }
</style>

<section class="pj-hotspots-section">
  <div class="pj-hotspots-container">
    <img src="{{ section.settings.image | img_url: 'master' | default: 'https://images.unsplash.com/photo-1542393545-10f5cde2c810?w=1200&q=80' }}" class="pj-hotspots-img" alt="Product Hotspots">
    
    <div class="pj-hotspot" style="top: 40%; left: 30%;">
      <div class="pj-hotspot-tooltip">{{ section.settings.hotspot_1 | default: 'Premium Aluminum Chassis' }}</div>
    </div>
    <div class="pj-hotspot" style="top: 60%; left: 70%;">
      <div class="pj-hotspot-tooltip">{{ section.settings.hotspot_2 | default: 'Advanced Cooling System' }}</div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Interactive Hotspots",
  "tag": "section",
  "settings": [
    { "type": "image_picker", "id": "image", "label": "Base Image" },
    { "type": "text", "id": "hotspot_1", "label": "Hotspot 1 Text" },
    { "type": "text", "id": "hotspot_2", "label": "Hotspot 2 Text" }
  ],
  "presets": [{ "name": "Interactive Hotspots" }]
}
{% endschema %}
