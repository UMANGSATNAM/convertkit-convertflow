<style>
  .pj-marquee-wrapper {
    overflow: hidden;
    padding: 80px 0;
    background: var(--pj-color-bg, #0a0a0a);
    color: var(--pj-color-text, #fff);
    border-top: 1px solid rgba(255,255,255,0.05);
    border-bottom: 1px solid rgba(255,255,255,0.05);
  }
  .pj-marquee {
    display: flex;
    white-space: nowrap;
    animation: pj-marquee-scroll 30s linear infinite;
  }
  .pj-marquee:hover {
    animation-play-state: paused;
  }
  .pj-marquee-text {
    font-family: var(--pj-font-sans, sans-serif);
    font-size: clamp(60px, 8vw, 120px);
    font-weight: 900;
    text-transform: uppercase;
    padding: 0 40px;
    letter-spacing: -2px;
    -webkit-text-stroke: 1px rgba(255,255,255,0.2);
    color: transparent;
    transition: all 0.3s ease;
    cursor: default;
  }
  .pj-marquee-text:hover {
    color: #ffa500;
    -webkit-text-stroke: 0;
    text-shadow: 0 0 40px #ffa50080;
  }
  @keyframes pj-marquee-scroll {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }
</style>

<div class="pj-marquee-wrapper">
  <div class="pj-marquee">
    <div class="pj-marquee-text">{{ section.settings.text | default: 'UNCOMPROMISING QUALITY • INNOVATIVE DESIGN • EXCEPTIONAL EXPERIENCE •' }}</div>
    <div class="pj-marquee-text">{{ section.settings.text | default: 'UNCOMPROMISING QUALITY • INNOVATIVE DESIGN • EXCEPTIONAL EXPERIENCE •' }}</div>
  </div>
</div>

{% schema %}
{
  "name": "Mega Marquee",
  "tag": "section",
  "settings": [
    { "type": "text", "id": "text", "label": "Marquee Text" }
  ],
  "presets": [{ "name": "Mega Marquee" }]
}
{% endschema %}
