<style>
  .pj-brand-story {
    display: flex;
    flex-direction: column;
    background-color: {{ section.settings.bg_color }};
  }
  @media (min-width: 768px) {
    .pj-brand-story {
      flex-direction: row;
      align-items: center;
    }
    .pj-brand-story--reverse {
      flex-direction: row-reverse;
    }
  }
  .pj-bs__image-col {
    width: 100%;
    position: relative;
  }
  @media (min-width: 768px) {
    .pj-bs__image-col {
      width: 50%;
    }
  }
  .pj-bs__image-wrap {
    position: relative;
    padding-bottom: 120%;
  }
  @media (min-width: 768px) {
    .pj-bs__image-wrap {
      padding-bottom: 100%;
    }
  }
  .pj-bs__image {
    position: absolute;
    top: 0; left: 0; width: 100%; height: 100%;
    object-fit: cover;
  }
  .pj-bs__content-col {
    width: 100%;
    padding: 60px 5%;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  @media (min-width: 768px) {
    .pj-bs__content-col {
      width: 50%;
      padding: 80px 8%;
    }
  }
  .pj-bs__content {
    max-width: 500px;
    text-align: {{ section.settings.text_alignment }};
  }
  .pj-bs__title {
    font-family: 'Georgia', serif;
    font-size: clamp(28px, 4vw, 42px);
    font-weight: normal;
    color: {{ section.settings.text_color }};
    margin-bottom: 25px;
    line-height: 1.2;
  }
  .pj-bs__text {
    font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
    font-size: 15px;
    line-height: 1.6;
    color: {{ section.settings.text_color }};
    margin-bottom: 35px;
  }
  .pj-bs__btn {
    display: inline-block;
    border: 1px solid {{ section.settings.text_color }};
    color: {{ section.settings.text_color }};
    padding: 12px 30px;
    text-decoration: none;
    font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
    font-size: 13px;
    text-transform: uppercase;
    letter-spacing: 1px;
    transition: all 0.3s ease;
  }
  .pj-bs__btn:hover {
    background-color: {{ section.settings.text_color }};
    color: {{ section.settings.bg_color }};
  }
</style>

<section class="pj-brand-story {% if section.settings.image_position == 'right' %}pj-brand-story--reverse{% endif %}">
  <div class="pj-bs__image-col">
    <div class="pj-bs__image-wrap">
      {% if section.settings.image %}
        <img class="pj-bs__image" src="{{ section.settings.image | image_url: width: 1200 }}" alt="{{ section.settings.title | escape }}">
      {% else %}
        <div class="pj-bs__image" style="background-color: #e0e0e0;"></div>
      {% endif %}
    </div>
  </div>
  
  <div class="pj-bs__content-col">
    <div class="pj-bs__content">
      <h2 class="pj-bs__title">{{ section.settings.title }}</h2>
      <div class="pj-bs__text">{{ section.settings.text }}</div>
      {% if section.settings.btn_label != blank %}
        <a href="{{ section.settings.btn_link }}" class="pj-bs__btn">{{ section.settings.btn_label }}</a>
      {% endif %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "PJ Brand Story",
  "tag": "section",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "select",
      "id": "image_position",
      "label": "Image Position",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "right", "label": "Right" }
      ],
      "default": "left"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Our Story"
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "Text",
      "default": "<p>Share details about your brand's origins, materials, or vision for the future.</p>"
    },
    {
      "type": "select",
      "id": "text_alignment",
      "label": "Text Alignment",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" }
      ],
      "default": "left"
    },
    {
      "type": "text",
      "id": "btn_label",
      "label": "Button Label",
      "default": "Read More"
    },
    {
      "type": "url",
      "id": "btn_link",
      "label": "Button Link"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#f9f9f9"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#000000"
    }
  ],
  "presets": [
    {
      "name": "PJ Brand Story"
    }
  ]
}
{% endschema %}
