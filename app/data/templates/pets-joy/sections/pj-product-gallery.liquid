<style>
  .pj-pgallery {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  @media (min-width: 768px) {
    .pj-pgallery {
      gap: 20px;
    }
  }
  .pj-pgallery__item {
    width: 100%;
    background-color: #f4f4f4;
  }
  .pj-pgallery__img {
    width: 100%;
    height: auto;
    display: block;
  }
</style>

<div class="pj-pgallery">
  {% for media in product.media %}
    <div class="pj-pgallery__item">
      {% case media.media_type %}
        {% when 'image' %}
          <img class="pj-pgallery__img" src="{{ media | image_url: width: 1200 }}" alt="{{ media.alt | escape }}">
        {% when 'video' %}
          {{ media | video_tag: controls: true, class: 'pj-pgallery__img' }}
      {% endcase %}
    </div>
  {% else %}
    <div class="pj-pgallery__item" style="padding: 100px; text-align: center;">No Images</div>
  {% endfor %}
</div>

{% schema %}
{
  "name": "PJ Product Gallery",
  "tag": "section",
  "settings": [],
  "templates": ["product"],
  "presets": [
    {
      "name": "PJ Product Gallery"
    }
  ]
}
{% endschema %}
