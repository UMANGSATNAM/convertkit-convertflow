<style>
  :root {
    --hd-font-sans: 'Inter', sans-serif;
    --hd-transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }
  
  /* Announcement Bar */
  .hd-announcement {
    background: #d2b48c;
    color: #fff;
    padding: 10px 0;
    overflow: hidden;
    position: relative;
    z-index: 1000;
    font-size: 11px;
    letter-spacing: 2px;
    text-transform: uppercase;
    font-weight: 600;
  }
  .hd-announcement__track {
    display: flex;
    white-space: nowrap;
    animation: hd-marquee 20s linear infinite;
  }
  .hd-announcement__item {
    padding: 0 40px;
  }
  @keyframes hd-marquee {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  /* Header */
  .hd-header-wrapper {
    position: sticky;
    top: 0;
    z-index: 900;
  }
  .hd-header {
    background: rgba(255, 255, 255, 0.85);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border-bottom: 1px solid rgba(0,0,0,0.05);
    transition: var(--hd-transition);
  }
  .hd-header.dark-mode {
    background: rgba(10, 10, 10, 0.85);
    border-bottom: 1px solid rgba(255,255,255,0.05);
  }
  .hd-header.hd-scrolled {
    padding: 5px 0;
    box-shadow: 0 10px 30px rgba(0,0,0,0.1);
  }
  .hd-header__container {
    max-width: 1440px;
    margin: 0 auto;
    padding: 20px 40px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    transition: var(--hd-transition);
  }
  .hd-header.hd-scrolled .hd-header__container {
    padding: 10px 40px;
  }
  
  /* Navigation & Mega Menu */
  .hd-nav {
    display: flex;
    gap: 35px;
  }
  .hd-nav-item {
    position: relative;
  }
  .hd-nav-link {
    text-decoration: none;
    color: inherit;
    font-size: 13px;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    font-weight: 500;
    padding-bottom: 5px;
    position: relative;
  }
  .hd-nav-link::after {
    content: '';
    position: absolute;
    bottom: 0; left: 0;
    width: 0; height: 2px;
    background: #d2b48c;
    transition: var(--hd-transition);
  }
  .hd-nav-link:hover::after {
    width: 100%;
  }
  
  .hd-mega-menu {
    position: absolute;
    top: 100%;
    left: -50px;
    width: 600px;
    background: #fff;
    border-radius: 12px;
    box-shadow: 0 20px 40px rgba(0,0,0,0.1);
    padding: 30px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    opacity: 0;
    visibility: hidden;
    transform: translateY(10px);
    transition: var(--hd-transition);
    pointer-events: none;
  }
  .hd-header.dark-mode .hd-mega-menu {
    background: #111;
    box-shadow: 0 20px 40px rgba(0,0,0,0.5);
    border: 1px solid #222;
  }
  .hd-nav-item:hover .hd-mega-menu {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
    pointer-events: auto;
  }

  /* Logo */
  .hd-logo {
    font-size: 24px;
    font-weight: 800;
    text-decoration: none;
    color: inherit;
    letter-spacing: 2px;
  }
  
  /* Icons */
  .hd-icons {
    display: flex;
    gap: 20px;
    align-items: center;
  }
  .hd-icon-btn {
    background: none;
    border: none;
    cursor: pointer;
    color: inherit;
    transition: transform 0.3s;
    position: relative;
  }
  .hd-icon-btn:hover {
    transform: scale(1.1);
    color: #d2b48c;
  }
  .hd-cart-count {
    position: absolute;
    top: -8px; right: -10px;
    background: #d2b48c;
    color: #fff;
    font-size: 10px;
    width: 18px; height: 18px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
  }

  @media (max-width: 768px) {
    .hd-nav { display: none; }
    .hd-header__container { padding: 15px 20px; }
  }
</style>

<div class="hd-header-wrapper">
  <div class="hd-announcement">
    <div class="hd-announcement__track">
      <div class="hd-announcement__item">✨ Premium Design. Exceptional Quality. Free Shipping Worldwide. ✨</div>
      <div class="hd-announcement__item">✨ Premium Design. Exceptional Quality. Free Shipping Worldwide. ✨</div>
      <div class="hd-announcement__item">✨ Premium Design. Exceptional Quality. Free Shipping Worldwide. ✨</div>
    </div>
  </div>

  <header class="hd-header {% if section.settings.dark_mode %}dark-mode{% endif %}" id="hd-header">
    <div class="hd-header__container">
      
      <div class="hd-mobile-menu-btn" style="display:none;">
         <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="3" y1="12" x2="21" y2="12"></line><line x1="3" y1="6" x2="21" y2="6"></line><line x1="3" y1="18" x2="21" y2="18"></line></svg>
      </div>

      <nav class="hd-nav">
        {% for link in linklists.main-menu.links %}
          <div class="hd-nav-item">
            <a href="{{ link.url }}" class="hd-nav-link">{{ link.title }}</a>
            {% if link.links.size > 0 %}
              <div class="hd-mega-menu">
                {% for child_link in link.links %}
                  <div>
                    <a href="{{ child_link.url }}" style="font-weight: 600; text-decoration: none; color: inherit; display: block; margin-bottom: 10px;">{{ child_link.title }}</a>
                  </div>
                {% endfor %}
              </div>
            {% endif %}
          </div>
        {% endfor %}
      </nav>
      
      <a href="/" class="hd-logo">{{ section.settings.logo_text | default: "BRAND" }}</a>
      
      <div class="hd-icons">
        <button class="hd-icon-btn" aria-label="Search">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line></svg>
        </button>
        <button class="hd-icon-btn" aria-label="Account">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path><circle cx="12" cy="7" r="4"></circle></svg>
        </button>
        <button class="hd-icon-btn" onclick="document.getElementById('hd-cart-drawer').classList.add('active'); document.getElementById('hd-cart-overlay').classList.add('active');">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path><line x1="3" y1="6" x2="21" y2="6"></line><path d="M16 10a4 4 0 0 1-8 0"></path></svg>
          <span class="hd-cart-count">{{ cart.item_count | default: 0 }}</span>
        </button>
      </div>
    </div>
  </header>
</div>

<script>
  window.addEventListener('scroll', () => {
    const header = document.getElementById('hd-header');
    if (window.scrollY > 50) {
      header.classList.add('hd-scrolled');
    } else {
      header.classList.remove('hd-scrolled');
    }
  });
</script>

{% schema %}
{
  "name": "Premium Header",
  "tag": "section",
  "settings": [
    { "type": "text", "id": "logo_text", "label": "Logo Text", "default": "BRAND" },
    { "type": "checkbox", "id": "dark_mode", "label": "Enable Dark Mode", "default": false }
  ]
}
{% endschema %}
