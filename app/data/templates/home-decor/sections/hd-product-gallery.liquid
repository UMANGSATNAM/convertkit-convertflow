<style>
  .hd-pgallery {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  @media (min-width: 768px) {
    .hd-pgallery {
      gap: 20px;
    }
  }
  .hd-pgallery__item {
    width: 100%;
    background-color: #f4f4f4;
  }
  .hd-pgallery__img {
    width: 100%;
    height: auto;
    display: block;
  }
</style>

<div class="hd-pgallery">
  {% for media in product.media %}
    <div class="hd-pgallery__item">
      {% case media.media_type %}
        {% when 'image' %}
          <img class="hd-pgallery__img" src="{{ media | image_url: width: 1200 }}" alt="{{ media.alt | escape }}">
        {% when 'video' %}
          {{ media | video_tag: controls: true, class: 'hd-pgallery__img' }}
      {% endcase %}
    </div>
  {% else %}
    <div class="hd-pgallery__item" style="padding: 100px; text-align: center;">No Images</div>
  {% endfor %}
</div>

{% schema %}
{
  "name": "HD Product Gallery",
  "tag": "section",
  "settings": [],
  "templates": ["product"],
  "presets": [
    {
      "name": "HD Product Gallery"
    }
  ]
}
{% endschema %}
