<style>
  .hd-marquee-wrapper {
    overflow: hidden;
    padding: 80px 0;
    background: var(--hd-color-bg, #0a0a0a);
    color: var(--hd-color-text, #fff);
    border-top: 1px solid rgba(255,255,255,0.05);
    border-bottom: 1px solid rgba(255,255,255,0.05);
  }
  .hd-marquee {
    display: flex;
    white-space: nowrap;
    animation: hd-marquee-scroll 30s linear infinite;
  }
  .hd-marquee:hover {
    animation-play-state: paused;
  }
  .hd-marquee-text {
    font-family: var(--hd-font-sans, sans-serif);
    font-size: clamp(60px, 8vw, 120px);
    font-weight: 900;
    text-transform: uppercase;
    padding: 0 40px;
    letter-spacing: -2px;
    -webkit-text-stroke: 1px rgba(255,255,255,0.2);
    color: transparent;
    transition: all 0.3s ease;
    cursor: default;
  }
  .hd-marquee-text:hover {
    color: #d2b48c;
    -webkit-text-stroke: 0;
    text-shadow: 0 0 40px #d2b48c80;
  }
  @keyframes hd-marquee-scroll {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }
</style>

<div class="hd-marquee-wrapper">
  <div class="hd-marquee">
    <div class="hd-marquee-text">{{ section.settings.text | default: 'UNCOMPROMISING QUALITY • INNOVATIVE DESIGN • EXCEPTIONAL EXPERIENCE •' }}</div>
    <div class="hd-marquee-text">{{ section.settings.text | default: 'UNCOMPROMISING QUALITY • INNOVATIVE DESIGN • EXCEPTIONAL EXPERIENCE •' }}</div>
  </div>
</div>

{% schema %}
{
  "name": "Mega Marquee",
  "tag": "section",
  "settings": [
    { "type": "text", "id": "text", "label": "Marquee Text" }
  ],
  "presets": [{ "name": "Mega Marquee" }]
}
{% endschema %}
