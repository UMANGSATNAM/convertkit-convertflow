<style>
  .fb-footer {
    background: var(--fb-color-bg, #0a0a0a);
    color: var(--fb-color-text, #fff);
    padding: 80px 40px 40px;
    border-top: 1px solid rgba(255,255,255,0.05);
    font-family: var(--fb-font-sans, sans-serif);
  }
  .fb-footer-grid {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr 1fr;
    gap: 40px;
    max-width: 1440px;
    margin: 0 auto;
  }
  .fb-footer-brand {
    font-size: 24px;
    font-weight: 800;
    letter-spacing: 2px;
    margin-bottom: 20px;
  }
  .fb-footer-title {
    font-size: 14px;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 20px;
    color: #4caf50;
  }
  .fb-footer-link {
    display: block;
    color: inherit;
    text-decoration: none;
    margin-bottom: 12px;
    opacity: 0.7;
    transition: all 0.3s;
    font-size: 14px;
  }
  .fb-footer-link:hover {
    opacity: 1;
    transform: translateX(5px);
    color: #4caf50;
  }
  .fb-newsletter-form {
    display: flex;
    margin-top: 20px;
  }
  .fb-newsletter-input {
    flex: 1;
    padding: 12px 16px;
    background: rgba(255,255,255,0.05);
    border: 1px solid rgba(255,255,255,0.1);
    color: inherit;
    border-radius: 4px 0 0 4px;
  }
  .fb-newsletter-btn {
    padding: 12px 24px;
    background: #4caf50;
    color: #000;
    border: none;
    font-weight: 700;
    cursor: pointer;
    border-radius: 0 4px 4px 0;
    transition: opacity 0.3s;
  }
  .fb-newsletter-btn:hover {
    opacity: 0.9;
  }
  .fb-footer-bottom {
    max-width: 1440px;
    margin: 60px auto 0;
    padding-top: 20px;
    border-top: 1px solid rgba(255,255,255,0.1);
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 12px;
    opacity: 0.5;
  }
  .fb-payment-icons {
    display: flex;
    gap: 10px;
  }
  @media (max-width: 900px) {
    .fb-footer-grid { grid-template-columns: 1fr; }
    .fb-footer-bottom { flex-direction: column; gap: 20px; text-align: center; }
  }
</style>

<footer class="fb-footer">
  <div class="fb-footer-grid">
    <div>
      <div class="fb-footer-brand">{{ section.settings.logo_text | default: 'BRAND' }}</div>
      <p style="opacity: 0.7; font-size: 14px; max-width: 300px;">{{ section.settings.about_text | default: 'Crafting premium experiences through innovative design and exceptional quality.' }}</p>
      
      <form class="fb-newsletter-form">
        <input type="email" class="fb-newsletter-input" placeholder="Join our newsletter">
        <button type="submit" class="fb-newsletter-btn">→</button>
      </form>
    </div>
    
    <div>
      <h4 class="fb-footer-title">Shop</h4>
      {% for link in linklists.footer.links %}
        <a href="{{ link.url }}" class="fb-footer-link">{{ link.title }}</a>
      {% endfor %}
      <a href="#" class="fb-footer-link">New Arrivals</a>
      <a href="#" class="fb-footer-link">Best Sellers</a>
      <a href="#" class="fb-footer-link">Collections</a>
    </div>

    <div>
      <h4 class="fb-footer-title">Support</h4>
      <a href="#" class="fb-footer-link">Contact Us</a>
      <a href="#" class="fb-footer-link">FAQ</a>
      <a href="#" class="fb-footer-link">Shipping & Returns</a>
      <a href="#" class="fb-footer-link">Track Order</a>
    </div>

    <div>
      <h4 class="fb-footer-title">Social</h4>
      <a href="#" class="fb-footer-link">Instagram</a>
      <a href="#" class="fb-footer-link">TikTok</a>
      <a href="#" class="fb-footer-link">Twitter</a>
    </div>
  </div>
  
  <div class="fb-footer-bottom">
    <div>&copy; {{ 'now' | date: '%Y' }} {{ section.settings.logo_text | default: 'BRAND' }}. All rights reserved.</div>
    <div class="fb-payment-icons">
      <!-- Visa SVG -->
      <svg width="32" height="20" viewBox="0 0 32 20" fill="currentColor"><path d="M10.5 4.5l-2.5 11h-3l1.5-11h4zm8.5 0c-1 0-2 .5-2 1.5s1.5 1.5 1.5 2.5-1 1.5-2 1.5-1.5-.5-1.5-.5l-.5 2.5c.5.5 1.5.5 2.5.5 2 0 3.5-1 3.5-3 0-1.5-1.5-2-1.5-2.5s1-1 2-1h1.5l-1-2.5c-.5-.5-1.5-.5-2.5-.5zm6.5 0l-2 7.5-1-7.5h-3l2.5 11h3l3.5-11h-3z"/></svg>
      <!-- Mastercard SVG -->
      <svg width="32" height="20" viewBox="0 0 32 20" fill="currentColor"><circle cx="10" cy="10" r="6"/><circle cx="20" cy="10" r="6"/></svg>
    </div>
  </div>
</footer>

{% schema %}
{
  "name": "Premium Footer",
  "tag": "section",
  "settings": [
    { "type": "text", "id": "logo_text", "label": "Logo Text", "default": "BRAND" },
    { "type": "textarea", "id": "about_text", "label": "About Text" }
  ]
}
{% endschema %}
