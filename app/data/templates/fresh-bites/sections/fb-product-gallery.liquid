<style>
  .fb-pgallery {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  @media (min-width: 768px) {
    .fb-pgallery {
      gap: 20px;
    }
  }
  .fb-pgallery__item {
    width: 100%;
    background-color: #f4f4f4;
  }
  .fb-pgallery__img {
    width: 100%;
    height: auto;
    display: block;
  }
</style>

<div class="fb-pgallery">
  {% for media in product.media %}
    <div class="fb-pgallery__item">
      {% case media.media_type %}
        {% when 'image' %}
          <img class="fb-pgallery__img" src="{{ media | image_url: width: 1200 }}" alt="{{ media.alt | escape }}">
        {% when 'video' %}
          {{ media | video_tag: controls: true, class: 'fb-pgallery__img' }}
      {% endcase %}
    </div>
  {% else %}
    <div class="fb-pgallery__item" style="padding: 100px; text-align: center;">No Images</div>
  {% endfor %}
</div>

{% schema %}
{
  "name": "FB Product Gallery",
  "tag": "section",
  "settings": [],
  "templates": ["product"],
  "presets": [
    {
      "name": "FB Product Gallery"
    }
  ]
}
{% endschema %}
