{%- style -%}
  #shopify-section-{{ section.id }} {
    background-color: {{ section.settings.bg_color | default: '#18181B' }};
    padding-top: {{ section.settings.padding_top | default: 90 }}px;
    padding-bottom: {{ section.settings.padding_bottom | default: 90 }}px;
  }
  .hp61-fc__container { max-width: {{ section.settings.container_width | default: 1280 }}px; margin: 0 auto; padding: 0 24px; }
  .hp61-fc__header { text-align: center; margin-bottom: 50px; }
  .hp61-fc__eyebrow { font-family: 'Space Grotesk', sans-serif; font-size: 11px; font-weight: 700; letter-spacing: 3px; text-transform: uppercase; color: {{ section.settings.accent_color | default: '#EF4444' }}; margin: 0 0 12px; }
  .hp61-fc__heading { font-family: 'Syne', sans-serif; font-size: clamp(30px, 4vw, 46px); font-weight: 800; color: #FFFFFF; margin: 0; }
  .hp61-fc__grid { display: grid; grid-template-columns: repeat({{ section.settings.columns | default: 4 }}, 1fr); gap: 20px; }
  .hp61-fc__card {
    background: {{ section.settings.card_bg | default: '#09090B' }};
    border: 1px solid rgba(239, 68, 68, 0.2);
    border-radius: 6px; overflow: hidden;
    transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
  }
  .hp61-fc__card:hover { transform: translateY(-6px); border-color: #EF4444; box-shadow: 0 0 25px rgba(239, 68, 68, 0.25); }
  .hp61-fc__img-wrap { aspect-ratio: 3/4; overflow: hidden; background: #27272A; }
  .hp61-fc__img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.5s ease; filter: brightness(0.9) contrast(1.15); }
  .hp61-fc__card:hover .hp61-fc__img { transform: scale(1.06); }
  .hp61-fc__info { padding: 18px 16px; }
  .hp61-fc__product-title { font-family: 'Syne', sans-serif; font-size: 15px; font-weight: 700; color: #FFFFFF; margin: 0 0 6px; text-decoration: none; display: block; }
  .hp61-fc__price { font-family: 'Space Grotesk', sans-serif; font-size: 15px; font-weight: 700; color: {{ section.settings.accent_color | default: '#EF4444' }}; margin: 0; }
  .hp61-fc__view-all { display: block; text-align: center; margin-top: 40px; }
  .hp61-fc__view-btn {
    display: inline-flex; align-items: center; gap: 8px; padding: 14px 34px;
    background: {{ section.settings.btn_bg | default: '#EF4444' }}; color: #FFFFFF;
    font-family: 'Space Grotesk', sans-serif; font-size: 13px; font-weight: 700;
    letter-spacing: 1.5px; text-transform: uppercase;
    text-decoration: none; border-radius: 4px; transition: all 0.3s ease;
    box-shadow: 0 4px 20px rgba(239, 68, 68, 0.4);
  }
  .hp61-fc__view-btn:hover { box-shadow: 0 8px 30px rgba(239, 68, 68, 0.65); transform: translateY(-2px); }
  @media screen and (max-width: 992px) { .hp61-fc__grid { grid-template-columns: repeat(2, 1fr); } }
  @media screen and (max-width: 480px) { .hp61-fc__grid { grid-template-columns: repeat(2, 1fr); gap: 12px; } }
{%- endstyle -%}
<div class="hp61-fc__container">
  <div class="hp61-fc__header">
    {%- if section.settings.eyebrow != blank -%}<p class="hp61-fc__eyebrow">{{ section.settings.eyebrow }}</p>{%- endif -%}
    <h2 class="hp61-fc__heading">{{ section.settings.heading | default: 'Volcanic Mineral Formulations' }}</h2>
  </div>
  {%- assign collection = collections[section.settings.collection] -%}
  <div class="hp61-fc__grid">
    {%- for product in collection.products limit: section.settings.products_to_show -%}
      <div class="hp61-fc__card">
        <a href="{{ product.url }}" class="hp61-fc__img-wrap">
          {%- if product.featured_image != blank -%}<img src="{{ product.featured_image | image_url: width: 600 }}" class="hp61-fc__img" alt="{{ product.title }}" loading="lazy">{%- endif -%}
        </a>
        <div class="hp61-fc__info">
          <a href="{{ product.url }}" class="hp61-fc__product-title">{{ product.title }}</a>
          <p class="hp61-fc__price">{{ product.price | money }}</p>
        </div>
      </div>
    {%- endfor -%}
  </div>
  {%- if section.settings.show_view_all -%}
    <div class="hp61-fc__view-all"><a href="{{ collection.url | default: '/collections/all' }}" class="hp61-fc__view-btn">{{ section.settings.view_all_text | default: 'Explore Volcanic Line' }} &rarr;</a></div>
  {%- endif -%}
</div>
{% schema %}
{
  "name": "HP61 Collection",
  "class": "section-hp61-fc",
  "settings": [
    { "type": "color", "id": "bg_color", "label": "Background", "default": "#18181B" },
    { "type": "color", "id": "card_bg", "label": "Card BG", "default": "#09090B" },
    { "type": "color", "id": "accent_color", "label": "Accent", "default": "#EF4444" },
    { "type": "color", "id": "btn_bg", "label": "Button BG", "default": "#EF4444" },
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "🌋 THERMAL FORGE" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Volcanic Mineral Formulations" },
    { "type": "collection", "id": "collection", "label": "Collection" },
    { "type": "range", "id": "products_to_show", "min": 2, "max": 12, "step": 1, "label": "Products", "default": 4 },
    { "type": "range", "id": "columns", "min": 2, "max": 4, "step": 1, "label": "Columns", "default": 4 },
    { "type": "checkbox", "id": "show_view_all", "label": "Show View All", "default": true },
    { "type": "text", "id": "view_all_text", "label": "View All Text", "default": "Explore Volcanic Line" },
    { "type": "range", "id": "padding_top", "min": 20, "max": 140, "step": 10, "unit": "px", "label": "Padding Top", "default": 90 },
    { "type": "range", "id": "padding_bottom", "min": 20, "max": 140, "step": 10, "unit": "px", "label": "Padding Bottom", "default": 90 },
    { "type": "range", "id": "container_width", "min": 1000, "max": 1440, "step": 20, "unit": "px", "label": "Container Width", "default": 1280 }
  ],
  "presets": [{ "name": "HP61 Collection" }]
}
{% endschema %}
