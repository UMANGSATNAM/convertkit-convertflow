{%- comment -%}
  PC V35 — NEON TOKYO STREET SNACK PRODUCT GRID
  Design: Midnight Purple (#0F051D), Neon Magenta (#FF007F) & Cyan Green (#00FFCC) accents, Fredoka & Outfit typography
  Card Shape: 16px corners, 2px neon glow borders, Tokyo street arcade pop
{%- endcomment -%}

<style>
  @import url('https://fonts.googleapis.com/css2?family=Fredoka:wght@600;700&family=Outfit:wght@500;600;700;800&display=swap');

  .pcv35 {
    --bg: {{ section.settings.bg_color | default: '#0F051D' }};
    --card-bg: {{ section.settings.card_bg | default: '#1D0B36' }};
    --text: {{ section.settings.text_color | default: '#F3E8FF' }};
    --muted: {{ section.settings.muted_color | default: '#C084FC' }};
    --accent: {{ section.settings.accent_color | default: '#00FFCC' }};
    --accent-hover: {{ section.settings.accent_hover | default: '#00D6AB' }};
    --border: {{ section.settings.border_color | default: '#FF007F' }};
    --badge-sale-bg: #FF007F;
    --badge-sale-text: #FFFFFF;
    --f-heading: 'Fredoka', cursive;
    --f-body: 'Outfit', sans-serif;
    --r-card: 16px;
    --ease: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    background-color: var(--bg);
    color: var(--text);
    font-family: var(--f-body);
    padding: {{ section.settings.padding_top | default: 56 }}px 0 {{ section.settings.padding_bottom | default: 56 }}px 0;
    line-height: 1.4;
    -webkit-font-smoothing: antialiased;
  }

  .pcv35 *, .pcv35 *::before, .pcv35 *::after { box-sizing: border-box; margin: 0; padding: 0; }
  .pcv35 a { color: inherit; text-decoration: none; }
  .pcv35 button { font-family: inherit; cursor: pointer; border: none; background: none; }
  .pcv35 img { max-width: 100%; display: block; }

  .pcv35__wrap { max-width: 1280px; margin: 0 auto; padding: 0 20px; }

  /* HEADER */
  .pcv35__head { display: flex; flex-direction: column; gap: 8px; margin-bottom: 36px; text-align: {{ section.settings.header_align | default: 'center' }}; }
  @media (min-width: 768px) {
    .pcv35__head { display: flex; flex-direction: row; justify-content: space-between; align-items: flex-end; }
  }
  .pcv35__title { font-family: var(--f-heading); font-size: 32px; font-weight: 700; color: #FFFFFF; text-shadow: 0 0 10px #FF007F; letter-spacing: -0.01em; }
  @media (min-width: 768px) { .pcv35__title { font-size: 42px; } }
  .pcv35__sub { font-size: 16px; color: var(--muted); font-weight: 600; }
  .pcv35__viewall { display: inline-flex; align-items: center; gap: 8px; font-weight: 700; font-size: 13px; font-family: var(--f-heading); text-transform: uppercase; color: #0F051D; background: var(--accent); padding: 10px 22px; border-radius: 20px; box-shadow: 0 0 15px rgba(0, 255, 204, 0.5); transition: var(--ease); }
  .pcv35__viewall:hover { background: #FF007F; color: #FFFFFF; box-shadow: 0 0 20px rgba(255, 0, 127, 0.7); transform: scale(1.05); }

  /* GRID */
  .pcv35__grid {
    display: grid;
    grid-template-columns: repeat({{ section.settings.cols_mobile | default: 2 }}, 1fr);
    gap: 18px;
  }
  @media (min-width: 640px) {
    .pcv35__grid { grid-template-columns: repeat({{ section.settings.cols_tablet | default: 3 }}, 1fr); gap: 24px; }
  }
  @media (min-width: 1024px) {
    .pcv35__grid { grid-template-columns: repeat({{ section.settings.cols_desktop | default: 4 }}, 1fr); gap: 28px; }
  }

  /* CARD */
  .pcv35__card {
    background: var(--card-bg);
    border: 2px solid var(--border);
    border-radius: var(--r-card);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    position: relative;
    box-shadow: 0 0 15px rgba(255, 0, 127, 0.2);
    transition: var(--ease);
  }
  .pcv35__card:hover {
    transform: translateY(-5px) rotate(1deg);
    border-color: #00FFCC;
    box-shadow: 0 0 25px rgba(0, 255, 204, 0.4);
  }

  /* IMAGE CONTAINER */
  .pcv35__img-box {
    position: relative;
    width: 100%;
    aspect-ratio: {{ section.settings.img_ratio | default: '1/1' }};
    background: #090312;
    overflow: hidden;
  }
  .pcv35__img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
  }
  .pcv35__img--sec {
    position: absolute;
    top: 0; left: 0;
    opacity: 0;
  }
  .pcv35__card:hover .pcv35__img--pri {
    {% if section.settings.enable_sec_img %}
      opacity: 0;
    {% else %}
      transform: scale(1.06);
    {% endif %}
  }
  .pcv35__card:hover .pcv35__img--sec { opacity: 1; transform: scale(1.06); }

  /* BADGES */
  .pcv35__badges { position: absolute; top: 12px; left: 12px; display: flex; flex-direction: column; gap: 6px; z-index: 2; pointer-events: none; }
  .pcv35__badge { font-size: 10px; font-weight: 800; text-transform: uppercase; padding: 4px 10px; border-radius: 12px; letter-spacing: 0.05em; font-family: var(--f-heading); }
  .pcv35__badge--sale { background: var(--badge-sale-bg); color: var(--badge-sale-text); }
  .pcv35__badge--tag { background: #00FFCC; color: #0F051D; }
  .pcv35__badge--sold { background: #581C87; color: #D8B4FE; }

  /* WISHLIST BUTTON */
  .pcv35__wish {
    position: absolute;
    top: 12px; right: 12px;
    width: 36px; height: 36px;
    border-radius: 50%;
    background: rgba(15, 5, 29, 0.85);
    border: 2px solid var(--border);
    backdrop-filter: blur(6px);
    display: flex; align-items: center; justify-content: center;
    color: #FF007F;
    z-index: 3;
    transition: var(--ease);
  }
  .pcv35__wish:hover { background: #FF007F; color: #FFFFFF; border-color: #FF007F; transform: scale(1.15); box-shadow: 0 0 15px #FF007F; }
  .pcv35__wish.active { color: #FFFFFF; background: #FF007F; border-color: #FF007F; box-shadow: 0 0 15px #FF007F; }

  /* QUICK ADD BUTTON */
  .pcv35__quick-box { padding: 12px; background: var(--card-bg); }
  .pcv35__quick-btn {
    width: 100%;
    padding: 10px 16px;
    background: var(--accent);
    color: #0F051D;
    font-size: 13px; font-weight: 700; font-family: var(--f-heading);
    border-radius: 20px;
    border: none;
    box-shadow: 0 0 15px rgba(0, 255, 204, 0.4);
    display: flex; align-items: center; justify-content: center; gap: 8px;
    transition: var(--ease);
  }
  .pcv35__quick-btn:hover { background: var(--accent-hover); transform: scale(1.03); box-shadow: 0 0 20px rgba(0, 255, 204, 0.6); }
  .pcv35__quick-btn:disabled { background: #581C87; color: #D8B4FE; cursor: not-allowed; box-shadow: none; }

  /* CARD CONTENT */
  .pcv35__content { padding: 14px; display: flex; flex-direction: column; gap: 6px; flex-grow: 1; text-align: center; }

  .pcv35__vendor { font-size: 11px; text-transform: uppercase; color: var(--accent); font-weight: 700; letter-spacing: 0.08em; }
  .pcv35__item-title { font-family: var(--f-heading); font-size: 17px; font-weight: 700; color: var(--text); line-height: 1.3; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }

  /* RATING */
  .pcv35__rating { display: flex; align-items: center; justify-content: center; gap: 4px; font-size: 13px; color: #FEF08A; font-weight: 700; }
  .pcv35__stars { letter-spacing: 1px; text-shadow: 0 0 6px #FEF08A; }
  .pcv35__rev-cnt { color: var(--muted); font-size: 11px; font-weight: 600; }

  /* PRICE */
  .pcv35__price-row { display: flex; align-items: baseline; justify-content: center; gap: 8px; margin-top: auto; padding-top: 4px; }
  .pcv35__price { font-size: 18px; font-weight: 700; color: #FFFFFF; font-family: var(--f-heading); text-shadow: 0 0 8px rgba(255, 255, 255, 0.4); }
  .pcv35__cmp-price { font-size: 13px; color: var(--muted); text-decoration: line-through; }

  /* SWATCHES */
  .pcv35__swatches { display: flex; gap: 6px; align-items: center; justify-content: center; margin-top: 4px; }
  .pcv35__swatch { width: 14px; height: 14px; border-radius: 50%; border: 1px solid var(--border); cursor: pointer; transition: var(--ease); }
  .pcv35__swatch:hover, .pcv35__swatch.active { transform: scale(1.25); border-color: var(--accent); box-shadow: 0 0 8px var(--accent); }

  /* URGENCY MICRO */
  .pcv35__urgency { font-size: 11px; color: #FF007F; font-weight: 700; font-family: var(--f-heading); display: flex; align-items: center; justify-content: center; gap: 4px; margin-top: 2px; text-shadow: 0 0 8px #FF007F; }

  /* TOAST */
  .pcv35__toast {
    position: fixed; bottom: 20px; right: 20px;
    background: var(--accent); color: #0F051D;
    padding: 12px 22px; border-radius: 20px;
    font-size: 13px; font-weight: 700; font-family: var(--f-heading);
    z-index: 9999;
    transform: translateY(100px); opacity: 0;
    transition: all 0.25s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    box-shadow: 0 0 25px rgba(0, 255, 204, 0.6);
  }
  .pcv35__toast.show { transform: translateY(0); opacity: 1; }
</style>

<div class="pcv35">
  <div class="pcv35__wrap">
    
    {% if section.settings.title != blank or section.settings.subheading != blank %}
      <div class="pcv35__head">
        <div>
          {% if section.settings.title != blank %}
            <h2 class="pcv35__title">{{ section.settings.title | escape }}</h2>
          {% endif %}
          {% if section.settings.subheading != blank %}
            <p class="pcv35__sub">{{ section.settings.subheading | escape }}</p>
          {% endif %}
        </div>
        {% if section.settings.show_view_all and section.settings.collection != blank %}
          <a href="{{ collections[section.settings.collection].url }}" class="pcv35__viewall">
            {{ section.settings.view_all_label | default: 'Shop Tokyo Drop' }} 🍡 &rarr;
          </a>
        {% endif %}
      </div>
    {% endif %}

    <div class="pcv35__grid">
      {% assign target_coll = collections[section.settings.collection] %}
      {% assign limit_num = section.settings.products_per_page | default: 8 %}

      {% if target_coll != blank and target_coll.products.size > 0 %}
        {% for product in target_coll.products limit: limit_num %}
          {% assign current_variant = product.selected_or_first_available_variant %}
          
          {% render 'card-v35', product: product, show_rating: section.settings.show_rating, rating_value: section.settings.rating_value, rating_count: section.settings.rating_count %}
        {% endfor %}
      {% else %}
        {% for i in (1..limit_num) %}
          {% render 'card-v35', product: nil, show_rating: section.settings.show_rating, rating_value: section.settings.rating_value, rating_count: section.settings.rating_count %}
        {% endfor %}
      {% endif %}
    </div>

  </div>
</div>

<div class="pcv35__toast" id="pcv35-toast">TOKYO SNACK ADDED!</div>

<script>
(function(){
  // Wishlist is handled globally by assets/wishlist.js (shared store: cf_wishlist_items).
  // Buttons are rendered by snippets/wishlist-button.liquid — no per-section storage.


  document.querySelectorAll('[data-pcv35-add]').forEach(function(btn){
    btn.addEventListener('click', function(e){
      e.preventDefault(); e.stopPropagation();
      var id = btn.dataset.pcv35Add, orig = btn.innerHTML;
      btn.disabled = true;
      btn.innerHTML = 'SNACKING...';

      fetch('/cart/add.js', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ id: id, quantity: 1 })
      })
      .then(function(r){ return r.json(); })
      .then(function(){
        btn.innerHTML = '✓ ADDED';
        showT('TOKYO SNACK ADDED!');
        setTimeout(function(){ btn.disabled = false; btn.innerHTML = orig; }, 2000);
      })
      .catch(function(){
        btn.innerHTML = 'ERROR';
        setTimeout(function(){ btn.disabled = false; btn.innerHTML = orig; }, 2000);
      });
    });
  });

  function showT(msg){
    var t = document.getElementById('pcv35-toast');
    if(!t) return;
    t.textContent = msg;
    t.classList.add('show');
    setTimeout(function(){ t.classList.remove('show'); }, 3000);
  }
})();
</script>

{% schema %}
{
  "name": "PC V35 — Tokyo Street Sna",
  "tag": "section",
  "class": "pcv35-section",
  "settings": [
    {
      "type": "header",
      "content": "Collection Settings"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "range",
      "id": "products_per_page",
      "label": "Products to show",
      "min": 2,
      "max": 16,
      "step": 1,
      "default": 8
    },
    {
      "type": "header",
      "content": "Grid Layout"
    },
    {
      "type": "range",
      "id": "cols_desktop",
      "label": "Columns on Desktop",
      "min": 2,
      "max": 5,
      "default": 4
    },
    {
      "type": "range",
      "id": "cols_tablet",
      "label": "Columns on Tablet",
      "min": 2,
      "max": 4,
      "default": 3
    },
    {
      "type": "select",
      "id": "cols_mobile",
      "label": "Columns on Mobile",
      "options": [
        {
          "value": "1",
          "label": "1 Column"
        },
        {
          "value": "2",
          "label": "2 Columns"
        }
      ],
      "default": "2"
    },
    {
      "type": "select",
      "id": "img_ratio",
      "label": "Image Ratio",
      "options": [
        {
          "value": "1/1",
          "label": "Square (1:1)"
        },
        {
          "value": "3/4",
          "label": "Portrait (3:4)"
        },
        {
          "value": "4/5",
          "label": "Tall (4:5)"
        }
      ],
      "default": "1/1"
    },
    {
      "type": "header",
      "content": "Card Features"
    },
    {
      "type": "checkbox",
      "id": "enable_sec_img",
      "label": "Show second image on hover",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_quick_add",
      "label": "Enable Quick Add button",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_rating",
      "label": "Show star ratings",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_vendor",
      "label": "Show vendor",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_swatches",
      "label": "Show color swatches preview",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_urgency",
      "label": "Show inventory urgency tag",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_custom_badge",
      "label": "Show custom tag badge",
      "default": true
    },
    {
      "type": "text",
      "id": "custom_badge_text",
      "label": "Custom Badge Text",
      "default": "TOKYO DROP 🍡"
    },
    {
      "type": "header",
      "content": "Section Header"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "TOKYO STREET ARCADE"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Neon arcade street food and Japanese pop culture treats."
    },
    {
      "type": "select",
      "id": "header_align",
      "label": "Header Alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        }
      ],
      "default": "center"
    },
    {
      "type": "checkbox",
      "id": "show_view_all",
      "label": "Show 'View All' link",
      "default": true
    },
    {
      "type": "text",
      "id": "view_all_label",
      "label": "'View All' text",
      "default": "Shop Tokyo Drop"
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background",
      "default": "#0F051D"
    },
    {
      "type": "color",
      "id": "card_bg",
      "label": "Card Background",
      "default": "#1D0B36"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#F3E8FF"
    },
    {
      "type": "color",
      "id": "muted_color",
      "label": "Muted Text Color",
      "default": "#C084FC"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Accent / CTA Color",
      "default": "#00FFCC"
    },
    {
      "type": "color",
      "id": "border_color",
      "label": "Card Border Color",
      "default": "#FF007F"
    },
    {
      "type": "header",
      "content": "Spacing"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top Padding (px)",
      "min": 0,
      "max": 100,
      "step": 4,
      "default": 56
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom Padding (px)",
      "min": 0,
      "max": 100,
      "step": 4,
      "default": 56
    },
    {
      "type": "text",
      "id": "rating_value",
      "label": "Rating Value (e.g. 4.9)",
      "default": "4.9"
    },
    {
      "type": "text",
      "id": "rating_count",
      "label": "Rating Count (e.g. 128)",
      "default": "128"
    }
  ],
  "presets": [
    {
      "name": "PC V35 — Tokyo Street Sna"
    }
  ]
}
{% endschema %}
