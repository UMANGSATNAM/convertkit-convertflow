{%- comment -%}
  PC V65 — MINIMALIST NORDIC SUSTAINABLE BEDDING PRODUCT GRID
  Design: Linen White (#FAFAFA), Sage Olive (#556B2F) & Soft Charcoal (#2F4F4F) accents, Lora & Inter typography
  Card Shape: 12px corners, 1px linen borders, organic Scandinavian bedding aesthetic
{%- endcomment -%}

<style>
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Lora:ital,wght@0,500;0,600;1,400&display=swap');

  .pcv65 {
    --bg: {{ section.settings.bg_color | default: '#FAFAFA' }};
    --card-bg: {{ section.settings.card_bg | default: '#FFFFFF' }};
    --text: {{ section.settings.text_color | default: '#2F4F4F' }};
    --muted: {{ section.settings.muted_color | default: '#667C7C' }};
    --accent: {{ section.settings.accent_color | default: '#556B2F' }};
    --accent-hover: {{ section.settings.accent_hover | default: '#3E4F22' }};
    --border: {{ section.settings.border_color | default: '#E2E8F0' }};
    --badge-sale-bg: #556B2F;
    --badge-sale-text: #FFFFFF;
    --f-serif: 'Lora', serif;
    --f-sans: 'Inter', sans-serif;
    --r-card: 12px;
    --ease: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    background-color: var(--bg);
    color: var(--text);
    font-family: var(--f-sans);
    padding: {{ section.settings.padding_top | default: 56 }}px 0 {{ section.settings.padding_bottom | default: 56 }}px 0;
    line-height: 1.5;
    -webkit-font-smoothing: antialiased;
  }

  .pcv65 *, .pcv65 *::before, .pcv65 *::after { box-sizing: border-box; margin: 0; padding: 0; }
  .pcv65 a { color: inherit; text-decoration: none; }
  .pcv65 button { font-family: inherit; cursor: pointer; border: none; background: none; }
  .pcv65 img { max-width: 100%; display: block; }

  .pcv65__wrap { max-width: 1280px; margin: 0 auto; padding: 0 20px; }

  /* HEADER */
  .pcv65__head { display: flex; flex-direction: column; gap: 8px; margin-bottom: 36px; text-align: {{ section.settings.header_align | default: 'center' }}; }
  @media (min-width: 768px) {
    .pcv65__head { display: flex; flex-direction: row; justify-content: space-between; align-items: flex-end; }
  }
  .pcv65__title { font-family: var(--f-serif); font-size: 32px; font-weight: 600; color: var(--text); letter-spacing: -0.01em; }
  @media (min-width: 768px) { .pcv65__title { font-size: 40px; } }
  .pcv65__sub { font-size: 15px; color: var(--muted); font-style: italic; }
  .pcv65__viewall { display: inline-flex; align-items: center; gap: 8px; font-weight: 600; font-size: 13px; color: var(--text); background: #FFFFFF; border: 1px solid var(--border); padding: 8px 20px; border-radius: 20px; transition: var(--ease); }
  .pcv65__viewall:hover { background: var(--accent); color: #FFFFFF; border-color: var(--accent); }

  /* GRID */
  .pcv65__grid {
    display: grid;
    grid-template-columns: repeat({{ section.settings.cols_mobile | default: 2 }}, 1fr);
    gap: 18px;
  }
  @media (min-width: 640px) {
    .pcv65__grid { grid-template-columns: repeat({{ section.settings.cols_tablet | default: 3 }}, 1fr); gap: 24px; }
  }
  @media (min-width: 1024px) {
    .pcv65__grid { grid-template-columns: repeat({{ section.settings.cols_desktop | default: 4 }}, 1fr); gap: 28px; }
  }

  /* CARD */
  .pcv65__card {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: var(--r-card);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    position: relative;
    box-shadow: 0 8px 20px -5px rgba(47, 79, 79, 0.05);
    transition: var(--ease);
  }
  .pcv65__card:hover {
    transform: translateY(-4px);
    border-color: var(--accent);
    box-shadow: 0 12px 24px -5px rgba(47, 79, 79, 0.12);
  }

  /* IMAGE CONTAINER */
  .pcv65__img-box {
    position: relative;
    width: 100%;
    aspect-ratio: {{ section.settings.img_ratio | default: '4/5' }};
    background: #F4F4F2;
    overflow: hidden;
  }
  .pcv65__img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
  }
  .pcv65__img--sec {
    position: absolute;
    top: 0; left: 0;
    opacity: 0;
  }
  .pcv65__card:hover .pcv65__img--pri {
    {% if section.settings.enable_sec_img %}
      opacity: 0;
    {% else %}
      transform: scale(1.05);
    {% endif %}
  }
  .pcv65__card:hover .pcv65__img--sec { opacity: 1; transform: scale(1.05); }

  /* BADGES */
  .pcv65__badges { position: absolute; top: 12px; left: 12px; display: flex; flex-direction: column; gap: 6px; z-index: 2; pointer-events: none; }
  .pcv65__badge { font-size: 10px; font-weight: 600; text-transform: uppercase; padding: 4px 10px; border-radius: 12px; letter-spacing: 0.05em; font-family: var(--f-sans); }
  .pcv65__badge--sale { background: var(--badge-sale-bg); color: var(--badge-sale-text); }
  .pcv65__badge--tag { background: #2F4F4F; color: #FFFFFF; }
  .pcv65__badge--sold { background: #94A3B8; color: #FFFFFF; }

  /* WISHLIST BUTTON */
  .pcv65__wish {
    position: absolute;
    top: 12px; right: 12px;
    width: 36px; height: 36px;
    border-radius: 50%;
    background: rgba(250, 250, 250, 0.9);
    border: 1px solid var(--border);
    backdrop-filter: blur(4px);
    display: flex; align-items: center; justify-content: center;
    color: var(--muted);
    z-index: 3;
    transition: var(--ease);
  }
  .pcv65__wish:hover { background: var(--accent); color: #FFFFFF; border-color: var(--accent); transform: scale(1.1); }
  .pcv65__wish.active { color: #FFFFFF; background: var(--accent); border-color: var(--accent); }

  /* QUICK ADD HOVER */
  .pcv65__quick-box {
    position: absolute;
    bottom: 12px; left: 12px; right: 12px;
    z-index: 3;
    transform: translateY(12px);
    opacity: 0;
    transition: var(--ease);
  }
  .pcv65__card:hover .pcv65__quick-box { transform: translateY(0); opacity: 1; }
  @media (max-width: 767px) {
    .pcv65__quick-box { transform: translateY(0); opacity: 1; position: relative; bottom: auto; left: auto; right: auto; padding: 0 14px 14px; }
  }

  .pcv65__quick-btn {
    width: 100%;
    padding: 11px 16px;
    background: var(--accent);
    color: #FFFFFF;
    font-size: 12px; font-weight: 600; font-family: var(--f-sans);
    border-radius: 20px;
    display: flex; align-items: center; justify-content: center; gap: 8px;
    transition: var(--ease);
    box-shadow: 0 4px 12px rgba(85, 107, 47, 0.25);
  }
  .pcv65__quick-btn:hover { background: var(--accent-hover); transform: scale(1.02); }
  .pcv65__quick-btn:disabled { background: #E2E8F0; color: #667C7C; cursor: not-allowed; box-shadow: none; }

  /* CARD CONTENT */
  .pcv65__content { padding: 16px; display: flex; flex-direction: column; gap: 6px; flex-grow: 1; text-align: center; }

  .pcv65__vendor { font-size: 10px; text-transform: uppercase; color: var(--accent); font-weight: 600; font-family: var(--f-sans); letter-spacing: 0.08em; }
  .pcv65__item-title { font-family: var(--f-serif); font-size: 18px; font-weight: 600; color: var(--text); line-height: 1.35; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }

  /* RATING */
  .pcv65__rating { display: flex; align-items: center; justify-content: center; gap: 4px; font-size: 12px; color: var(--accent); font-weight: 700; }
  .pcv65__stars { letter-spacing: 1px; }
  .pcv65__rev-cnt { color: var(--muted); font-size: 11px; font-weight: 400; }

  /* PRICE */
  .pcv65__price-row { display: flex; align-items: baseline; justify-content: center; gap: 8px; margin-top: auto; padding-top: 4px; }
  .pcv65__price { font-size: 16px; font-weight: 600; color: var(--text); font-family: var(--f-sans); }
  .pcv65__cmp-price { font-size: 13px; color: var(--muted); text-decoration: line-through; }

  /* SWATCHES */
  .pcv65__swatches { display: flex; gap: 6px; align-items: center; justify-content: center; margin-top: 4px; }
  .pcv65__swatch { width: 14px; height: 14px; border-radius: 50%; border: 1px solid var(--border); cursor: pointer; transition: var(--ease); }
  .pcv65__swatch:hover, .pcv65__swatch.active { transform: scale(1.25); border-color: var(--accent); }

  /* URGENCY MICRO */
  .pcv65__urgency { font-size: 11px; color: var(--accent); font-style: italic; font-family: var(--f-serif); display: flex; align-items: center; justify-content: center; gap: 4px; margin-top: 2px; }

  /* TOAST */
  .pcv65__toast {
    position: fixed; bottom: 20px; right: 20px;
    background: var(--accent); color: #FFFFFF;
    padding: 12px 22px; border-radius: 20px;
    font-size: 13px; font-weight: 600; font-family: var(--f-sans);
    z-index: 9999;
    transform: translateY(100px); opacity: 0;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .pcv65__toast.show { transform: translateY(0); opacity: 1; }
</style>

<div class="pcv65">
  <div class="pcv65__wrap">
    
    {% if section.settings.title != blank or section.settings.subheading != blank %}
      <div class="pcv65__head">
        <div>
          {% if section.settings.title != blank %}
            <h2 class="pcv65__title">{{ section.settings.title | escape }}</h2>
          {% endif %}
          {% if section.settings.subheading != blank %}
            <p class="pcv65__sub">{{ section.settings.subheading | escape }}</p>
          {% endif %}
        </div>
        {% if section.settings.show_view_all and section.settings.collection != blank %}
          <a href="{{ collections[section.settings.collection].url }}" class="pcv65__viewall">
            {{ section.settings.view_all_label | default: 'Explore Linen Bedding' }} &rarr;
          </a>
        {% endif %}
      </div>
    {% endif %}

    <div class="pcv65__grid">
      {% assign target_coll = collections[section.settings.collection] %}
      {% assign limit_num = section.settings.products_per_page | default: 8 %}

      {% if target_coll != blank and target_coll.products.size > 0 %}
        {% for product in target_coll.products limit: limit_num %}
          {% assign current_variant = product.selected_or_first_available_variant %}
          
          {% render 'card-v65', product: product, show_rating: section.settings.show_rating, rating_value: section.settings.rating_value, rating_count: section.settings.rating_count %}
        {% endfor %}
      {% else %}
        {% for i in (1..limit_num) %}
          {% render 'card-v65', product: nil, show_rating: section.settings.show_rating, rating_value: section.settings.rating_value, rating_count: section.settings.rating_count %}
        {% endfor %}
      {% endif %}
    </div>

  </div>
</div>

<div class="pcv65__toast" id="pcv65-toast">Linen Set Added!</div>

<script>
(function(){
  // Wishlist is handled globally by assets/wishlist.js (shared store: cf_wishlist_items).
  // Buttons are rendered by snippets/wishlist-button.liquid — no per-section storage.


  document.querySelectorAll('[data-pcv65-add]').forEach(function(btn){
    btn.addEventListener('click', function(e){
      e.preventDefault(); e.stopPropagation();
      var id = btn.dataset.pcv65Add, orig = btn.innerHTML;
      btn.disabled = true;
      btn.innerHTML = 'Weaving...';

      fetch('/cart/add.js', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ id: id, quantity: 1 })
      })
      .then(function(r){ return r.json(); })
      .then(function(){
        btn.innerHTML = '✓ Added';
        showT('Linen Set Added!');
        setTimeout(function(){ btn.disabled = false; btn.innerHTML = orig; }, 2000);
      })
      .catch(function(){
        btn.innerHTML = 'Error';
        setTimeout(function(){ btn.disabled = false; btn.innerHTML = orig; }, 2000);
      });
    });
  });

  function showT(msg){
    var t = document.getElementById('pcv65-toast');
    if(!t) return;
    t.textContent = msg;
    t.classList.add('show');
    setTimeout(function(){ t.classList.remove('show'); }, 3000);
  }
})();
</script>

{% schema %}
{
  "name": "PC V65 — Nordic Linen Bed",
  "tag": "section",
  "class": "pcv65-section",
  "settings": [
    {
      "type": "header",
      "content": "Collection Settings"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "range",
      "id": "products_per_page",
      "label": "Products to show",
      "min": 2,
      "max": 16,
      "step": 1,
      "default": 8
    },
    {
      "type": "header",
      "content": "Grid Layout"
    },
    {
      "type": "range",
      "id": "cols_desktop",
      "label": "Columns on Desktop",
      "min": 2,
      "max": 5,
      "default": 4
    },
    {
      "type": "range",
      "id": "cols_tablet",
      "label": "Columns on Tablet",
      "min": 2,
      "max": 4,
      "default": 3
    },
    {
      "type": "select",
      "id": "cols_mobile",
      "label": "Columns on Mobile",
      "options": [
        {
          "value": "1",
          "label": "1 Column"
        },
        {
          "value": "2",
          "label": "2 Columns"
        }
      ],
      "default": "2"
    },
    {
      "type": "select",
      "id": "img_ratio",
      "label": "Image Ratio",
      "options": [
        {
          "value": "1/1",
          "label": "Square (1:1)"
        },
        {
          "value": "3/4",
          "label": "Portrait (3:4)"
        },
        {
          "value": "4/5",
          "label": "Tall (4:5)"
        }
      ],
      "default": "4/5"
    },
    {
      "type": "header",
      "content": "Card Features"
    },
    {
      "type": "checkbox",
      "id": "enable_sec_img",
      "label": "Show second image on hover",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_quick_add",
      "label": "Enable Quick Add button",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_rating",
      "label": "Show star ratings",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_vendor",
      "label": "Show vendor",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_swatches",
      "label": "Show color swatches preview",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_urgency",
      "label": "Show inventory urgency tag",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_custom_badge",
      "label": "Show custom tag badge",
      "default": true
    },
    {
      "type": "text",
      "id": "custom_badge_text",
      "label": "Custom Badge Text",
      "default": "100% ORGANIC LINEN"
    },
    {
      "type": "header",
      "content": "Section Header"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "MINIMALIST NORDIC BEDDING"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Stonewashed 100% French organic linen duvet covers and luxury sheets."
    },
    {
      "type": "select",
      "id": "header_align",
      "label": "Header Alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        }
      ],
      "default": "center"
    },
    {
      "type": "checkbox",
      "id": "show_view_all",
      "label": "Show 'View All' link",
      "default": true
    },
    {
      "type": "text",
      "id": "view_all_label",
      "label": "'View All' text",
      "default": "Explore Linen Bedding"
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background",
      "default": "#FAFAFA"
    },
    {
      "type": "color",
      "id": "card_bg",
      "label": "Card Background",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#2F4F4F"
    },
    {
      "type": "color",
      "id": "muted_color",
      "label": "Muted Text Color",
      "default": "#667C7C"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Accent / CTA Color",
      "default": "#556B2F"
    },
    {
      "type": "color",
      "id": "border_color",
      "label": "Card Border Color",
      "default": "#E2E8F0"
    },
    {
      "type": "header",
      "content": "Spacing"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top Padding (px)",
      "min": 0,
      "max": 100,
      "step": 4,
      "default": 56
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom Padding (px)",
      "min": 0,
      "max": 100,
      "step": 4,
      "default": 56
    },
    {
      "type": "text",
      "id": "rating_value",
      "label": "Rating Value (e.g. 4.9)",
      "default": "4.9"
    },
    {
      "type": "text",
      "id": "rating_count",
      "label": "Rating Count (e.g. 128)",
      "default": "128"
    }
  ],
  "presets": [
    {
      "name": "PC V65 — Nordic Linen Bed"
    }
  ]
}
{% endschema %}
