{%- style -%}
  #shopify-section-{{ section.id }} {
    background-color: {{ section.settings.bg_color | default: "#080B10" }};
    padding: {{ section.settings.padding_top | default: 80 }}px 20px {{ section.settings.padding_bottom | default: 80 }}px;
  }

  .hp43-insta__title {
    font-family: 'Orbitron', sans-serif;
    font-size: 28px;
    font-weight: 800;
    color: {{ section.settings.title_color | default: "#F1F5F9" }};
    text-align: center;
    margin: 0 0 40px;
    text-transform: uppercase;
  }

  .hp43-insta__grid {
    max-width: 1320px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat({{ section.settings.columns | default: 4 }}, 1fr);
    gap: 16px;
  }

  .hp43-insta__card {
    aspect-ratio: 1;
    border-radius: 6px;
    overflow: hidden;
    position: relative;
    border: 1px solid {{ section.settings.border_color | default: "#1E293B" }};
    display: block;
  }

  .hp43-insta__card img,
  .hp43-insta__card svg {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
    display: block;
  }

  .hp43-insta__card:hover img {
    transform: scale(1.06);
  }

  .hp43-insta__tag {
    position: absolute;
    left: 8px;
    bottom: 8px;
    background: rgba(0, 0, 0, 0.65);
    color: #FFFFFF;
    font-size: 11px;
    font-weight: 700;
    padding: 4px 9px;
    border-radius: 99px;
  }

  @media screen and (max-width: 768px) {
    .hp43-insta__grid { grid-template-columns: repeat({{ section.settings.columns_mobile | default: 2 }}, 1fr); }
    .hp43-insta__title { font-size: 22px; margin-bottom: 24px; }
  }

  @media (prefers-reduced-motion: reduce) {
    .hp43-insta__card img { transition: none; }
    .hp43-insta__card:hover img { transform: none; }
  }
{%- endstyle -%}

{%- if section.settings.title != blank -%}
  <div class="hp43-insta__title">{{ section.settings.title }}</div>
{%- endif -%}

<div class="hp43-insta__grid">
  {%- if section.blocks.size > 0 -%}
    {%- for block in section.blocks -%}
      {%- liquid
        assign gl_link = block.settings.link
        if gl_link == blank
          assign gl_link = '#'
        endif
      -%}
      <a class="hp43-insta__card" href="{{ gl_link }}" {{ block.shopify_attributes }}>
        {%- if block.settings.image != blank -%}
          <img
            src="{{ block.settings.image | image_url: width: 600 }}"
            alt="{{ block.settings.image.alt | default: block.settings.tag | escape }}"
            width="600" height="600" loading="lazy">
        {%- else -%}
          {{ 'lifestyle-1' | placeholder_svg_tag }}
        {%- endif -%}
        {%- if block.settings.tag != blank -%}
          <span class="hp43-insta__tag">{{ block.settings.tag }}</span>
        {%- endif -%}
      </a>
    {%- endfor -%}
  {%- else -%}
    {%- for i in (1..4) -%}
      <div class="hp43-insta__card">{{ 'lifestyle-1' | placeholder_svg_tag }}</div>
    {%- endfor -%}
  {%- endif -%}
</div>

{% schema %}
{
  "name": "HP43 - Cyber Gallery",
  "class": "section-hp43-gallery",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Pro battlestation setups"
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "range",
      "id": "columns",
      "label": "Columns (desktop)",
      "min": 2,
      "max": 6,
      "step": 1,
      "default": 4
    },
    {
      "type": "range",
      "id": "columns_mobile",
      "label": "Columns (mobile)",
      "min": 1,
      "max": 3,
      "step": 1,
      "default": 2
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top",
      "min": 0,
      "max": 140,
      "step": 4,
      "unit": "px",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom",
      "min": 0,
      "max": 140,
      "step": 4,
      "unit": "px",
      "default": 80
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background",
      "default": "#080B10"
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "Heading",
      "default": "#F1F5F9"
    },
    {
      "type": "color",
      "id": "border_color",
      "label": "Card border",
      "default": "#1E293B"
    }
  ],
  "blocks": [
    {
      "type": "photo",
      "name": "Photo",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "tag",
          "label": "Tag label",
          "info": "Optional caption shown over the image."
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        }
      ]
    }
  ],
  "max_blocks": 12,
  "presets": [
    {
      "name": "HP43 - Cyber Gallery",
      "blocks": [
        { "type": "photo" },
        { "type": "photo" },
        { "type": "photo" },
        { "type": "photo" }
      ]
    }
  ]
}
{% endschema %}
