{%- style -%}
  #shopify-section-{{ section.id }} {
    background-color: {{ section.settings.bg_color | default: '#111827' }};
    padding-top: {{ section.settings.padding_top | default: 80 }}px;
    padding-bottom: {{ section.settings.padding_bottom | default: 80 }}px;
  }
  .hp58-ugc__container { max-width: {{ section.settings.container_width | default: 1280 }}px; margin: 0 auto; padding: 0 24px; }
  .hp58-ugc__header { text-align: center; margin-bottom: 40px; }
  .hp58-ugc__eyebrow { font-family: 'Space Grotesk', sans-serif; font-size: 11px; font-weight: 700; letter-spacing: 3px; text-transform: uppercase; color: {{ section.settings.accent_color | default: '#06B6D4' }}; margin: 0 0 12px; }
  .hp58-ugc__heading { font-family: 'Syne', sans-serif; font-size: clamp(28px, 3.5vw, 42px); font-weight: 800; color: #FFFFFF; margin: 0; }
  .hp58-ugc__grid { display: grid; grid-template-columns: repeat({{ section.settings.columns | default: 4 }}, 1fr); gap: 16px; }
  .hp58-ugc__item {
    aspect-ratio: 9/16; border-radius: 8px; overflow: hidden; position: relative;
    background: #0B0F19; border: 1px solid rgba(6, 182, 212, 0.25);
  }
  .hp58-ugc__item img { width: 100%; height: 100%; object-fit: cover; filter: brightness(0.9) contrast(1.1); }
  .hp58-ugc__overlay {
    position: absolute; bottom: 0; left: 0; right: 0; padding: 16px;
    background: linear-gradient(transparent, rgba(11, 15, 25, 0.95));
  }
  .hp58-ugc__handle { font-family: 'Space Grotesk', sans-serif; font-size: 12px; font-weight: 600; color: #06B6D4; margin: 0; }
  @media screen and (max-width: 992px) { .hp58-ugc__grid { grid-template-columns: repeat(2, 1fr); } }
  @media screen and (max-width: 480px) { .hp58-ugc__grid { grid-template-columns: repeat(2, 1fr); gap: 10px; } }
{%- endstyle -%}
<div class="hp58-ugc__container">
  <div class="hp58-ugc__header">
    {%- if section.settings.eyebrow != blank -%}<p class="hp58-ugc__eyebrow">{{ section.settings.eyebrow }}</p>{%- endif -%}
    <h2 class="hp58-ugc__heading">{{ section.settings.heading | default: 'Cyber Community' }}</h2>
  </div>
  <div class="hp58-ugc__grid">
    {%- for block in section.blocks -%}
      <div class="hp58-ugc__item" {{ block.shopify_attributes }}>
        {%- if block.settings.image != blank -%}
          <img src="{{ block.settings.image | image_url: width: 400 }}" alt="{{ block.settings.handle }}" loading="lazy">
        {%- else -%}
          {{ 'lifestyle-2' | placeholder_svg_tag }}
        {%- endif -%}
        <div class="hp58-ugc__overlay">
          <p class="hp58-ugc__handle">{{ block.settings.handle | default: '@cyber_skin' }}</p>
        </div>
      </div>
    {%- endfor -%}
  </div>
</div>
{% schema %}
{
  "name": "HP58 UGC",
  "class": "section-hp58-ugc",
  "settings": [
    { "type": "color", "id": "bg_color", "label": "Background", "default": "#111827" },
    { "type": "color", "id": "accent_color", "label": "Accent", "default": "#06B6D4" },
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "⚡ FEED" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Cyber Community" },
    { "type": "range", "id": "columns", "min": 2, "max": 6, "step": 1, "label": "Columns", "default": 4 },
    { "type": "range", "id": "padding_top", "min": 20, "max": 140, "step": 10, "unit": "px", "label": "Padding Top", "default": 80 },
    { "type": "range", "id": "padding_bottom", "min": 20, "max": 140, "step": 10, "unit": "px", "label": "Padding Bottom", "default": 80 },
    { "type": "range", "id": "container_width", "min": 1000, "max": 1440, "step": 20, "unit": "px", "label": "Container Width", "default": 1280 }
  ],
  "blocks": [{
    "type": "reel", "name": "Reel",
    "settings": [
      { "type": "image_picker", "id": "image", "label": "Image" },
      { "type": "text", "id": "handle", "label": "Handle", "default": "@cyber_skin" }
    ]
  }],
  "presets": [{ "name": "HP58 UGC", "blocks": [
    { "type": "reel" },
    { "type": "reel", "settings": { "handle": "@tokyo_peptides" } },
    { "type": "reel", "settings": { "handle": "@quantum_glow" } },
    { "type": "reel", "settings": { "handle": "@biotech_routine" } }
  ]}]
}
{% endschema %}
