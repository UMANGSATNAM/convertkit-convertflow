<style>
@media screen and (max-width: 768px) {
  .hp62-marquee {
    padding-left: 16px !important;
    padding-right: 16px !important;
    box-sizing: border-box !important;
  }
  img {
    max-width: 100% !important;
    height: auto !important;
  }
}
</style>
{%- style -%}
  #shopify-section-{{ section.id }} {
    --hp62-marq-bg: {{ section.settings.bg_color | default: '#064E3B' }};
    --hp62-marq-text: {{ section.settings.text_color | default: '#ECFDF5' }};
    background: var(--hp62-marq-bg);
    overflow: hidden;
    padding: {{ section.settings.padding_vertical | default: 10 }}px 0;
  }
  .hp62-marquee__track {
    display: flex; gap: 50px;
    animation: hp62MarqueeScroll {{ section.settings.speed | default: 25 }}s linear infinite;
    width: max-content;
  }
  .hp62-marquee__item {
    display: flex; align-items: center; gap: 50px; white-space: nowrap;
    font-family: 'Outfit', sans-serif;
    font-size: {{ section.settings.font_size | default: 13 }}px;
    font-weight: 700; letter-spacing: 2.5px; text-transform: uppercase;
    color: var(--hp62-marq-text);
  }
  .hp62-marquee__dot {
    width: 6px; height: 6px;
    background: {{ section.settings.dot_color | default: '#34D399' }};
    border-radius: 50%; flex-shrink: 0;
  }
  @keyframes hp62MarqueeScroll {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }
{%- endstyle -%}
<div class="hp62-marquee__track">
  {%- for i in (1..12) -%}
    <div class="hp62-marquee__item">
      <span>{{ section.settings.marquee_text | default: '🏔️ SWISS ALPS EDELWEISS ❄️ ZERMATT GLACIAL WATER 🌿 LEONTOPODIC ACID 💧 ALPINE HYDRATION' }}</span>
      <span class="hp62-marquee__dot"></span>
    </div>
  {%- endfor -%}
</div>
{% schema %}
{
  "name": "HP62 Marquee",
  "class": "section-hp62-marquee",
  "settings": [
    { "type": "color", "id": "bg_color", "label": "Background", "default": "#064E3B" },
    { "type": "color", "id": "text_color", "label": "Text Color", "default": "#ECFDF5" },
    { "type": "color", "id": "dot_color", "label": "Dot Color", "default": "#34D399" },
    { "type": "text", "id": "marquee_text", "label": "Marquee Text", "default": "🏔️ SWISS ALPS EDELWEISS ❄️ ZERMATT GLACIAL WATER 🌿 LEONTOPODIC ACID 💧 ALPINE HYDRATION" },
    { "type": "range", "id": "speed", "min": 10, "max": 50, "step": 5, "unit": "s", "label": "Speed", "default": 25 },
    { "type": "range", "id": "font_size", "min": 10, "max": 22, "step": 1, "unit": "px", "label": "Font Size", "default": 13 },
    { "type": "range", "id": "padding_vertical", "min": 4, "max": 24, "step": 2, "unit": "px", "label": "Padding", "default": 10 }
  ],
  "presets": [{ "name": "HP62 Marquee" }]
}
{% endschema %}
