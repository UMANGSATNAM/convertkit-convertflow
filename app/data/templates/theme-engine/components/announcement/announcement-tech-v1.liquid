{%- comment -%}
  Component: announcement-tech-electronics-v1
  Family: Tech | Style: tech
  Archetypes: gaming, gadgets
  v2.0 — StoreForge Component Library
{%- endcomment -%}
<div class="announcement-bar-niche announcement-bar--electronics" 
     style="background-color: var(--color-surface); color: var(--color-text); font-size: 0.85rem; padding: var(--space-8) var(--space-16); text-align: center; position: relative; border-bottom: 2px solid var(--color-accent); font-family: var(--font-heading-family); font-weight: 700; letter-spacing: 0.05em; text-transform: uppercase;">
  
  <div class="announcement-bar-content">
    {%- if section.settings.link != blank -%}
      <a href="{{ section.settings.link }}" class="announcement-bar-link" style="color: inherit;">
    {%- endif -%}

    <span style="color: var(--color-accent);">[ SYSTEM UPDATE ]</span>
    <span style="margin-left: 6px;">{{ section.settings.text | escape }}</span>

    {%- if section.settings.show_countdown and section.settings.countdown_date != blank -%}
      <span class="announcement-countdown" data-date="{{ section.settings.countdown_date }}" 
            style="background-color: var(--color-sale-accent); color: #fff; padding: 2px 8px; font-family: var(--font-body-family); font-weight: bold; border-radius: 4px; margin-left: var(--space-12); font-size: 0.8rem; box-shadow: 0 0 8px var(--color-sale-accent);">
        <span class="countdown-days">00</span>d : 
        <span class="countdown-hours">00</span>h : 
        <span class="countdown-minutes">00</span>m : 
        <span class="countdown-seconds">00</span>s
      </span>
    {%- endif -%}

    {%- if section.settings.link != blank -%}
      </a>
    {%- endif -%}
  </div>

  <button type="button" class="announcement-bar-close" aria-label="{{ 'sections.announcement_bar.close' | t }}" 
          onclick="this.parentNode.remove()" 
          style="position: absolute; right: 15px; top: 50%; transform: translateY(-50%); background: none; border: none; color: inherit; cursor: pointer; display: flex; align-items: center;">
    {% render 'icon-close' %}
  </button>
</div>

{% schema %}
{
  "name": "Announcement Bar",
  "settings": [
    {
      "type": "text",
      "id": "text",
      "label": "Announcement Text",
      "default": "Free shipping on orders over $50"
    },
    {
      "type": "url",
      "id": "link",
      "label": "Announcement Link"
    },
    {
      "type": "checkbox",
      "id": "show_countdown",
      "label": "Show countdown",
      "default": false
    },
    {
      "type": "text",
      "id": "countdown_date",
      "label": "Countdown Date (ISO 8601)",
      "placeholder": "2026-12-31T23:59:59Z"
    }
  ]
}
{% endschema %}
