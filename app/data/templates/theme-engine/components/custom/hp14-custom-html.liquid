{%- liquid
  assign section_id = section.id
-%}

<div id="shopify-section-{{ section_id }}" class="hp14-custom-html-wrapper" style="padding: {{ section.settings.padding_top }}px {{ section.settings.padding_side }}px {{ section.settings.padding_bottom }}px;">
  {{ section.settings.custom_html }}
</div>

{% schema %}
{
  "name": "HP14 Custom HTML",
  "settings": [
    {
      "type": "html",
      "id": "custom_html",
      "label": "Custom HTML",
      "default": "<div style='text-align: center; font-family: Georgia, serif; font-size: 1.5rem; color: #cb997e; font-style: italic;'>~ Naturally Yours ~</div>"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "Padding Top",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_side",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "Padding Side",
      "default": 20
    }
  ],
  "presets": [
    {
      "name": "HP14 Custom HTML"
    }
  ]
}
{% endschema %}
