<style>
  #shopify-section-{{ section.id }} .sf-activate {
    background: var(--color-bg, #fff);
    color: var(--color-text, #1a1a1a);
    padding: var(--spacing-section-y, 80px) var(--spacing-section-x, 20px);
    min-height: 70vh;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  #shopify-section-{{ section.id }} .sf-activate__box {
    max-width: 440px;
    width: 100%;
    margin: 0 auto;
    text-align: center;
  }
  #shopify-section-{{ section.id }} .sf-activate__title {
    font-family: var(--font-heading, inherit);
    font-size: clamp(1.6rem, 3vw, 2rem);
    font-weight: 400;
    margin: 0 0 8px;
  }
  #shopify-section-{{ section.id }} .sf-activate__sub {
    color: var(--color-text-muted, #888);
    font-size: 0.95rem;
    margin: 0 0 32px;
  }
  #shopify-section-{{ section.id }} .sf-activate__form {
    display: flex;
    flex-direction: column;
    gap: 16px;
    text-align: left;
  }
  #shopify-section-{{ section.id }} .sf-activate__field {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }
  #shopify-section-{{ section.id }} .sf-activate__label {
    font-size: 0.75rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    font-weight: 500;
    color: var(--color-text, #1a1a1a);
  }
  #shopify-section-{{ section.id }} .sf-activate__input {
    border: 1px solid var(--color-border, #e0e0e0);
    padding: 12px 14px;
    font-size: 0.95rem;
    background: transparent;
    color: var(--color-text, #1a1a1a);
    outline: none;
  }
  #shopify-section-{{ section.id }} .sf-activate__submit {
    border: none;
    background: var(--color-primary, #1a1a1a);
    color: var(--color-btn-text, #fff);
    padding: 14px;
    font-size: 0.8rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    cursor: pointer;
    transition: opacity 0.2s;
    margin-top: 8px;
  }
  #shopify-section-{{ section.id }} .sf-activate__submit:hover { opacity: 0.8; }
  #shopify-section-{{ section.id }} .sf-activate__decline {
    background: none;
    border: none;
    color: var(--color-text-muted, #888);
    font-size: 0.85rem;
    text-decoration: underline;
    cursor: pointer;
    margin-top: 12px;
  }
</style>

<section id="shopify-section-{{ section.id }}" class="sf-activate" data-section-id="{{ section.id }}" data-section-type="main-activate-account">
  <div class="sf-activate__box">
    <h1 class="sf-activate__title">{{ 'customer.activate_account.title' | t: default: 'Activate Account' }}</h1>
    <p class="sf-activate__sub">{{ 'customer.activate_account.subtext' | t: default: 'Create your password to activate your account.' }}</p>

    {% form 'activate_customer_password', class: 'sf-activate__form' %}
      {%- if form.errors -%}
        <div style="color:#e44;font-size:0.85rem;margin-bottom:8px;">{{ form.errors | default_errors }}</div>
      {%- endif -%}

      <div class="sf-activate__field">
        <label class="sf-activate__label" for="password">{{ 'customer.activate_account.password' | t: default: 'Password' }}</label>
        <input type="password" name="customer[password]" id="password" class="sf-activate__input" autocomplete="new-password" required>
      </div>

      <div class="sf-activate__field">
        <label class="sf-activate__label" for="password_confirmation">{{ 'customer.activate_account.password_confirm' | t: default: 'Confirm Password' }}</label>
        <input type="password" name="customer[password_confirmation]" id="password_confirmation" class="sf-activate__input" autocomplete="new-password" required>
      </div>

      <button type="submit" class="sf-activate__submit">{{ 'customer.activate_account.submit' | t: default: 'Activate Account' }}</button>
      <button type="submit" name="decline" class="sf-activate__decline">{{ 'customer.activate_account.cancel' | t: default: 'Decline Invitation' }}</button>
    {% endform %}
  </div>
</section>

{% schema %}
{
  "name": "Activate Account Main",
  "tag": "section",
  "class": "section-main-activate-account",
  "settings": [],
  "presets": [
    {
      "name": "Activate Account Main"
    }
  ]
}
{% endschema %}
