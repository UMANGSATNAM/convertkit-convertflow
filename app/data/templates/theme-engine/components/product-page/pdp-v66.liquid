{%- comment -%}
  PDP V66 — CYBERPUNK MATRIX VIOLET TERMINAL
  Design: Sci-fi violet matrix HUD telemetry aesthetic, Orbitron header + Fira Code body
  Cyber Dark (#18181B), Violet Cyber (#8B5CF6), Neon Violet (#A78BFA), Deep Purple (#4C1D95)
{%- endcomment -%}

{%- assign current_variant = product.selected_or_first_available_variant -%}
{%- assign inv_qty = current_variant.inventory_quantity | default: 15 -%}
{%- if inv_qty <= 0 -%}{%- assign inv_qty = 8 -%}{%- endif -%}
{%- assign viewer_count = inv_qty | times: 7 | modulo: 50 | plus: 25 -%}
{%- assign sold_count = inv_qty | times: 17 | modulo: 430 | plus: 230 -%}

<style>
  @import url('https://fonts.googleapis.com/css2?family=Fira+Code:wght@500;600;700&family=Orbitron:wght@600;700;800&display=swap');

  .pdpv66 {
    --bg: #18181B; --surface: #27272A; --border: #3F3F46; --border-dark: #8B5CF6;
    --text: #FAFAFA; --muted: #C4B5FD; --accent: #8B5CF6; --accent-hover: #7C3AED;
    --violet: #8B5CF6; --danger: #EF4444;
    --f-head: 'Orbitron', sans-serif; --f-body: 'Fira Code', monospace;
    --r: 6px; --r-pill: 9999px; --ease: all 0.2s ease;
    background: var(--bg); color: var(--text); font-family: var(--f-body); font-size: 14px; line-height: 1.5; -webkit-font-smoothing: antialiased;
  }
  .pdpv66 *, .pdpv66 *::before, .pdpv66 *::after { box-sizing: border-box; margin: 0; padding: 0; }
  .pdpv66 img { max-width: 100%; display: block; }
  .pdpv66 button { font-family: inherit; cursor: pointer; }
  .pdpv66__wrap { max-width: 1280px; margin: 0 auto; padding: 0 20px; }

  /* OFFER BAR */
  .pdpv66__offer { background: var(--accent); color: #FFF; padding: 10px; text-align: center; font-family: var(--f-head); font-size: 13px; font-weight: 800; letter-spacing: 2px; text-transform: uppercase; border-bottom: 2px solid var(--violet); }

  /* GRID */
  .pdpv66__grid { display: grid; grid-template-columns: 1fr; gap: 36px; padding: 36px 0; }
  @media (min-width: 992px) { .pdpv66__grid { grid-template-columns: 1.1fr 1fr; gap: 48px; padding: 48px 0; } }

  /* GALLERY */
  .pdpv66__gal { display: flex; flex-direction: column; gap: 12px; }
  .pdpv66__hero { width: 100%; aspect-ratio: 1; border-radius: var(--r); overflow: hidden; background: var(--surface); border: 2px solid var(--border-dark); display: flex; align-items: center; justify-content: center; box-shadow: 0 0 24px rgba(139,92,246,0.25); position: relative; }
  .pdpv66__hero img { width: 100%; height: 100%; object-fit: contain; }
  .pdpv66__vlt-tag { position: absolute; top: 14px; left: 14px; background: rgba(24,24,27,0.9); border: 1px solid var(--accent); color: var(--accent); font-family: var(--f-head); font-size: 11px; font-weight: 700; padding: 4px 10px; border-radius: var(--r); }

  .pdpv66__thumbs { display: flex; gap: 10px; overflow-x: auto; }
  .pdpv66__th { width: 64px; height: 64px; flex-shrink: 0; border: 1px solid var(--border); border-radius: 6px; overflow: hidden; cursor: pointer; background: var(--surface); transition: var(--ease); }
  .pdpv66__th:hover, .pdpv66__th.on { border-color: var(--accent); box-shadow: 0 0 10px rgba(139,92,246,0.4); }
  .pdpv66__th img { width: 100%; height: 100%; object-fit: cover; }

  /* INFO PANEL */
  .pdpv66__info { display: flex; flex-direction: column; gap: 18px; }
  .pdpv66__crumb { font-family: var(--f-head); font-size: 12px; letter-spacing: 2px; color: var(--accent); font-weight: 700; text-transform: uppercase; }
  .pdpv66__title { font-family: var(--f-head); font-size: 30px; font-weight: 800; line-height: 1.15; color: var(--text); letter-spacing: 1px; }
  @media (min-width: 992px) { .pdpv66__title { font-size: 38px; } }

  .pdpv66__rating { display: flex; align-items: center; gap: 8px; font-size: 13px; color: var(--muted); font-weight: 600; }
  .pdpv66__stars { color: var(--accent); }

  /* PRICE CARD */
  .pdpv66__price-card { background: var(--surface); border: 1px solid var(--border-dark); border-radius: var(--r); padding: 16px 20px; display: flex; align-items: baseline; justify-content: space-between; flex-wrap: wrap; gap: 10px; box-shadow: 0 0 16px rgba(139,92,246,0.15); }
  .pdpv66__price { font-family: var(--f-head); font-size: 34px; font-weight: 800; color: var(--accent); }
  .pdpv66__cmp { font-size: 18px; color: var(--muted); text-decoration: line-through; }
  .pdpv66__badge { background: var(--accent); color: #FFF; font-family: var(--f-head); font-weight: 800; font-size: 11px; padding: 4px 10px; border-radius: 4px; }

  /* VARIANTS */
  .pdpv66__vlabel { font-family: var(--f-head); font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 1px; color: var(--accent); margin-bottom: 8px; }
  .pdpv66__vopts { display: flex; flex-wrap: wrap; gap: 10px; }
  .pdpv66__vr { display: none; }
  .pdpv66__vbtn { padding: 10px 22px; border: 1px solid var(--border); border-radius: var(--r); background: var(--surface); color: var(--text); font-family: var(--f-body); font-size: 13px; font-weight: 700; transition: var(--ease); cursor: pointer; }
  .pdpv66__vbtn:hover { border-color: var(--accent); color: var(--accent); }
  .pdpv66__vr:checked + .pdpv66__vbtn { border-color: var(--accent); background: var(--accent); color: #FFF; }

  /* PROMO */
  .pdpv66__promo { background: var(--surface); border: 1px dashed var(--accent); border-radius: var(--r); padding: 14px 18px; display: flex; align-items: center; justify-content: space-between; font-size: 13px; font-weight: 700; color: var(--text); }
  .pdpv66__promo-code { background: var(--accent); color: #FFF; font-family: var(--f-head); padding: 4px 10px; border-radius: 4px; }

  /* URGENCY */
  .pdpv66__urg { background: var(--surface); border-left: 4px solid var(--accent); border-radius: 0 var(--r) var(--r) 0; padding: 16px; display: flex; flex-direction: column; gap: 8px; font-size: 13px; font-weight: 700; }
  .pdpv66__pulse { display: inline-block; width: 8px; height: 8px; background: var(--accent); border-radius: 50%; margin-right: 6px; animation: pdpv66p 1.5s infinite; }
  @keyframes pdpv66p { 0%,100% { opacity: 1; } 50% { opacity: 0.3; } }
  .pdpv66__bar { height: 6px; background: var(--border); border-radius: 3px; overflow: hidden; margin-top: 4px; }
  .pdpv66__fill { height: 100%; background: var(--accent); border-radius: 3px; }

  /* QTY & CTA */
  .pdpv66__qty-row { display: flex; align-items: center; gap: 14px; }
  .pdpv66__qty { display: flex; border: 1px solid var(--border); border-radius: var(--r); overflow: hidden; background: var(--surface); }
  .pdpv66__qty button { width: 44px; height: 48px; border: none; background: transparent; font-size: 18px; font-weight: 700; color: var(--text); }
  .pdpv66__qty input { width: 44px; height: 48px; border: none; border-left: 1px solid var(--border); border-right: 1px solid var(--border); text-align: center; font-size: 15px; font-weight: 700; background: transparent; color: var(--text); }

  .pdpv66__atc { width: 100%; padding: 18px; border: none; border-radius: var(--r); background: var(--accent); color: #FFF; font-family: var(--f-head); font-size: 16px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; transition: var(--ease); box-shadow: 0 0 20px rgba(139,92,246,0.3); }
  .pdpv66__atc:hover { background: var(--accent-hover); transform: translateY(-1px); }
  .pdpv66__atc:disabled { opacity: 0.4; cursor: not-allowed; box-shadow: none; }
  .pdpv66__buynow .shopify-payment-button__button { border-radius: var(--r) !important; }

  /* TRUST */
  .pdpv66__trust { display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px; text-align: center; padding: 18px 0; border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); }
  .pdpv66__trust-item { font-family: var(--f-head); font-size: 10px; font-weight: 700; text-transform: uppercase; color: var(--muted); }
  .pdpv66__trust-icon { font-size: 20px; margin-bottom: 3px; color: var(--accent); }
  @media (max-width: 480px) { .pdpv66__trust { grid-template-columns: repeat(2, 1fr); } }

  /* ACCORDION */
  .pdpv66__acc { border-top: 1px solid var(--border); margin-top: 48px; }
  .pdpv66__acc-item { border-bottom: 1px solid var(--border); }
  .pdpv66__acc-btn { width: 100%; padding: 18px 0; border: none; background: transparent; display: flex; justify-content: space-between; align-items: center; font-family: var(--f-head); font-size: 15px; font-weight: 700; text-transform: uppercase; color: var(--text); }
  .pdpv66__acc-btn::after { content: '>'; font-size: 16px; color: var(--accent); }
  .pdpv66__acc-item.open .pdpv66__acc-btn::after { content: 'v'; }
  .pdpv66__acc-body { max-height: 0; overflow: hidden; transition: max-height 0.35s ease; }
  .pdpv66__acc-item.open .pdpv66__acc-body { max-height: 500px; }
  .pdpv66__acc-inner { padding: 0 0 18px; font-size: 14px; line-height: 1.7; color: var(--muted); }

  /* SECTION TITLE */
  .pdpv66__sh { font-family: var(--f-head); font-size: 24px; font-weight: 800; text-transform: uppercase; text-align: center; margin: 48px 0 20px; color: var(--text); letter-spacing: 1px; }

  /* REELS */
  .pdpv66__reels { display: flex; gap: 14px; overflow-x: auto; padding-bottom: 12px; }
  .pdpv66__reel { min-width: 180px; width: 180px; aspect-ratio: 9/16; background: #18181B; border: 1px solid var(--accent); border-radius: var(--r); overflow: hidden; flex-shrink: 0; position: relative; }
  .pdpv66__reel video { width: 100%; height: 100%; object-fit: cover; }
  .pdpv66__reel-cap { position: absolute; bottom: 0; left: 0; right: 0; padding: 10px; background: rgba(24,24,27,0.9); color: var(--accent); font-family: var(--f-head); font-size: 10px; }
  .pdpv66__reel-ph { width: 100%; height: 100%; display: flex; align-items: center; justify-content: center; color: var(--accent); font-size: 36px; }

  /* CARDS */
  .pdpv66__pgrid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px; }
  @media (min-width: 768px) { .pdpv66__pgrid { grid-template-columns: repeat(4, 1fr); } }
  .pdpv66__pcard { background: var(--surface); border-radius: var(--r); border: 1px solid var(--border); overflow: hidden; transition: var(--ease); text-decoration: none; color: inherit; display: block; }
  .pdpv66__pcard:hover { border-color: var(--accent); transform: translateY(-2px); box-shadow: 0 0 16px rgba(139,92,246,0.2); }
  .pdpv66__pcard img { width: 100%; aspect-ratio: 1; object-fit: cover; }
  .pdpv66__pcard-info { padding: 14px; }
  .pdpv66__pcard-title { font-family: var(--f-head); font-size: 13px; font-weight: 700; margin-bottom: 4px; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }
  .pdpv66__pcard-price { font-family: var(--f-head); font-size: 15px; font-weight: 700; color: var(--accent); }

  /* REVIEWS */
  .pdpv66__revgrid { display: grid; grid-template-columns: 1fr; gap: 14px; }
  @media (min-width: 768px) { .pdpv66__revgrid { grid-template-columns: repeat(3, 1fr); } }
  .pdpv66__revcard { background: var(--surface); border-radius: var(--r); border: 1px solid var(--border); padding: 20px; }
  .pdpv66__rev-stars { color: var(--accent); font-size: 14px; margin-bottom: 8px; }
  .pdpv66__rev-txt { font-size: 13px; line-height: 1.6; color: var(--muted); font-style: italic; margin-bottom: 12px; }
  .pdpv66__rev-auth { font-family: var(--f-head); font-size: 12px; font-weight: 700; }
  .pdpv66__rev-vf { font-family: var(--f-head); font-size: 10px; color: var(--accent); font-weight: 700; }

  /* RECENTLY VIEWED */
  #pdpv66-rv { display: none; }
  #pdpv66-rv.has { display: block; }

  /* STICKY ATC */
  .pdpv66__sticky { position: fixed; bottom: -80px; left: 0; width: 100%; background: var(--surface); border-top: 1px solid var(--accent); padding: 12px 24px; display: flex; justify-content: space-between; align-items: center; z-index: 9999; transition: bottom 0.3s ease; box-shadow: 0 -4px 20px rgba(0,0,0,0.5); }
  .pdpv66__sticky.show { bottom: 0; }
  .pdpv66__sticky-l { display: flex; align-items: center; gap: 14px; }
  .pdpv66__sticky-img { width: 42px; height: 42px; border-radius: 4px; border: 1px solid var(--border); object-fit: cover; }
  .pdpv66__sticky-t { font-family: var(--f-head); font-size: 13px; font-weight: 700; max-width: 200px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .pdpv66__sticky-p { font-family: var(--f-head); font-size: 15px; font-weight: 700; color: var(--accent); }
  .pdpv66__sticky-btn { background: var(--accent); color: #FFF; border: none; padding: 12px 26px; border-radius: var(--r); font-family: var(--f-head); font-weight: 800; font-size: 13px; text-transform: uppercase; transition: var(--ease); }
  .pdpv66__sticky-btn:hover { background: var(--accent-hover); }
  @media (max-width: 600px) { .pdpv66__sticky-l { display: none; } .pdpv66__sticky-btn { width: 100%; padding: 15px; } }

  .pdpv66__toast { position: fixed; top: 20px; right: 20px; background: var(--accent); color: #FFF; border-radius: var(--r); padding: 12px 24px; font-family: var(--f-head); font-size: 13px; font-weight: 700; z-index: 10000; transform: translateX(130%); transition: transform 0.3s ease; box-shadow: 0 0 16px rgba(139,92,246,0.4); }
  .pdpv66__toast.show { transform: translateX(0); }
</style>

<div class="pdpv66">
  {% if section.settings.show_offer %}<div class="pdpv66__offer">{{ section.settings.offer_text }}</div>{% endif %}

  <div class="pdpv66__wrap">
    <div class="pdpv66__grid">

      <!-- GALLERY -->
      <div class="pdpv66__gal">
        <div class="pdpv66__hero" id="pdpv66-hero">
          <span class="pdpv66__vlt-tag">VIOLET_OS // V66</span>
          {% if product.featured_media %}<img height="{{ product.featured_media.height }}" width="{{ product.featured_media.width }}" src="{{ product.featured_media | image_url: width: 900 }}" alt="{{ product.title | escape }}" id="pdpv66-himg">{% else %}{{ 'product-1' | placeholder_svg_tag }}{% endif %}
        </div>
        <div class="pdpv66__thumbs">
          {% for media in product.media limit: 6 %}
            <div class="pdpv66__th {% if forloop.first %}on{% endif %}">
              <img height="{{ media.height }}" width="{{ media.width }}" src="{{ media | image_url: width: 150 }}" alt="{{ media.alt | escape }}" loading="lazy">
            </div>
          {% endfor %}
        </div>
      </div>

      <!-- INFO -->
      <div class="pdpv66__info">
        <div class="pdpv66__crumb">[SYS] {% if product.collections.size > 0 %}{{ product.collections.first.title }}{% else %}Violet Terminal{% endif %}</div>
        <h1 class="pdpv66__title">{{ product.title }}</h1>

        {% if section.settings.show_rating %}
          <div class="pdpv66__rating">
            <span class="pdpv66__stars">★★★★★</span>
            <span>{{ section.settings.rating_val }} · {{ section.settings.rev_count }} Violet Logs</span>
          </div>
        {% endif %}

        <div class="pdpv66__price-card">
          <span class="pdpv66__price" id="pdpv66-price">{{ current_variant.price | money }}</span>
          {% if current_variant.compare_at_price > current_variant.price %}
            <span class="pdpv66__cmp">{{ current_variant.compare_at_price | money }}</span>
            {%- assign sv = current_variant.compare_at_price | minus: current_variant.price | times: 100.0 | divided_by: current_variant.compare_at_price | round -%}
            <span class="pdpv66__badge">SAVE {{ sv }}%</span>
          {% endif %}
        </div>

        {% if product.variants.size > 1 %}
          {% for option in product.options_with_values %}
            <div>
              <div class="pdpv66__vlabel">[SELECT_{{ option.name | upcase }}]</div>
              <div class="pdpv66__vopts">
                {% for value in option.values %}
                  <label>
                    <input type="radio" name="pdpv66-o-{{ option.position }}" value="{{ value | escape }}" class="pdpv66__vr" data-pos="{{ option.position }}" {% if option.selected_value == value %}checked{% endif %}>
                    <span class="pdpv66__vbtn">{{ value }}</span>
                  </label>
                {% endfor %}
              </div>
            </div>
          {% endfor %}
        {% endif %}

        {% if section.settings.show_promo %}
          <div class="pdpv66__promo">
            <span>Violet Pass: Apply {{ section.settings.promo_code }}</span>
            <span class="pdpv66__promo-code">Save {{ section.settings.promo_disc }}</span>
          </div>
        {% endif %}

        {% if section.settings.show_urgency %}
          <div class="pdpv66__urg">
            <div><span class="pdpv66__pulse"></span><strong>{{ viewer_count }}</strong> violet nodes active</div>
            <div>[SYS] <strong>{{ sold_count }}</strong> cyber orders completed today</div>
            <div>Buffer: Only <strong>{{ inv_qty }}</strong> remaining
              <div class="pdpv66__bar">
                {%- assign sp = inv_qty | times: 100 | divided_by: 45 -%}
                {%- if sp > 100 -%}{%- assign sp = 100 -%}{%- endif -%}
                <div class="pdpv66__fill" style="width:{{ sp }}%"></div>
              </div>
            </div>
            {% if section.settings.show_countdown %}
              <div>⏳ Violet Timeout In: <strong id="pdpv66-cd" style="color:var(--accent)">{{ section.settings.cd_hrs }}h 00m 00s</strong></div>
            {% endif %}
          </div>
        {% endif %}

        <div class="pdpv66__qty-row">
          <div class="pdpv66__qty">
            <button type="button" id="pdpv66-qm">−</button>
            <input type="number" id="pdpv66-qi" value="1" min="1" max="10" readonly>
            <button type="button" id="pdpv66-qp">+</button>
          </div>
          {% form 'product', product, id: 'pdpv66-form', style: 'flex:1' %}
            <input type="hidden" name="id" id="pdpv66-vid" value="{{ current_variant.id }}">
            <input type="hidden" name="quantity" id="pdpv66-fq" value="1">
            <button type="submit" name="add" class="pdpv66__atc" id="pdpv66-atc" {% unless current_variant.available %}disabled{% endunless %}>
              {% if current_variant.available %}ADD TO CART — {{ current_variant.price | money }}{% else %}SOLD OUT{% endif %}
            </button>
            <div class="pdpv66__buynow" style="margin-top:10px">{{ form | payment_button }}</div>
          {% endform %}
        </div>

        <div class="pdpv66__trust">
          <div class="pdpv66__trust-item"><div class="pdpv66__trust-icon">🔒</div>SSL 256-BIT</div>
          <div class="pdpv66__trust-item"><div class="pdpv66__trust-icon">⚡</div>VIOLET NODE</div>
          <div class="pdpv66__trust-item"><div class="pdpv66__trust-icon">↩️</div>EASY RETURNS</div>
          <div class="pdpv66__trust-item"><div class="pdpv66__trust-icon">✓</div>VERIFIED GRID</div>
        </div>
      </div>
    </div>

    <!-- ACCORDION -->
    <div class="pdpv66__acc">
      <div class="pdpv66__acc-item open">
        <button class="pdpv66__acc-btn" type="button">Violet Architecture & Spec</button>
        <div class="pdpv66__acc-body"><div class="pdpv66__acc-inner">{{ product.description }}</div></div>
      </div>
      {% if section.settings.vlt_specs != blank %}
        <div class="pdpv66__acc-item">
          <button class="pdpv66__acc-btn" type="button">Technical Telemetry & Protocol</button>
          <div class="pdpv66__acc-body"><div class="pdpv66__acc-inner">{{ section.settings.vlt_specs }}</div></div>
        </div>
      {% endif %}
    </div>

    <!-- FBT -->
    {% if section.settings.fbt_coll != blank %}
      {%- assign fbt = collections[section.settings.fbt_coll] -%}
      {% if fbt.products.size > 0 %}
        <div class="pdpv66__sh">Violet Terminal Modules</div>
        <div class="pdpv66__pgrid">
          {% for item in fbt.products limit: 4 %}
            <a href="{{ item.url }}" class="pdpv66__pcard">
              <img height="{{ item.featured_image.height }}" width="{{ item.featured_image.width }}" src="{{ item.featured_image | image_url: width: 400 }}" alt="{{ item.title | escape }}" loading="lazy">
              <div class="pdpv66__pcard-info">
                <div class="pdpv66__pcard-title">{{ item.title }}</div>
                <div class="pdpv66__pcard-price">{{ item.price | money }}</div>
              </div>
            </a>
          {% endfor %}
        </div>
      {% endif %}
    {% endif %}

    <!-- REELS -->
    {% if section.settings.show_reels %}
      <div class="pdpv66__sh">Violet Telemetry</div>
      <div class="pdpv66__reels">
        {% if section.settings.r1 != blank %}<div class="pdpv66__reel"><video src="{{ section.settings.r1 }}" muted playsinline loop></video><div class="pdpv66__reel-cap">[VIOLET_STREAM_1]</div></div>{% else %}<div class="pdpv66__reel"><div class="pdpv66__reel-ph">▶</div></div>{% endif %}
        {% if section.settings.r2 != blank %}<div class="pdpv66__reel"><video src="{{ section.settings.r2 }}" muted playsinline loop></video><div class="pdpv66__reel-cap">[VIOLET_STREAM_2]</div></div>{% else %}<div class="pdpv66__reel"><div class="pdpv66__reel-ph">▶</div></div>{% endif %}
        {% if section.settings.r3 != blank %}<div class="pdpv66__reel"><video src="{{ section.settings.r3 }}" muted playsinline loop></video><div class="pdpv66__reel-cap">[VIOLET_STREAM_3]</div></div>{% else %}<div class="pdpv66__reel"><div class="pdpv66__reel-ph">▶</div></div>{% endif %}
      </div>
    {% endif %}

    <!-- REVIEWS -->
    {% if section.settings.show_reviews %}
      <div class="pdpv66__sh" id="reviews">System Operator Logs</div>
      <div class="pdpv66__revgrid">
        {% if section.settings.rv1 != blank %}<div class="pdpv66__revcard"><div class="pdpv66__rev-stars">★★★★★</div><div class="pdpv66__rev-txt">"{{ section.settings.rv1 }}"</div><div class="pdpv66__rev-auth">{{ section.settings.rv1n }}</div><div class="pdpv66__rev-vf">✓ Verified Operator</div></div>{% endif %}
        {% if section.settings.rv2 != blank %}<div class="pdpv66__revcard"><div class="pdpv66__rev-stars">★★★★★</div><div class="pdpv66__rev-txt">"{{ section.settings.rv2 }}"</div><div class="pdpv66__rev-auth">{{ section.settings.rv2n }}</div><div class="pdpv66__rev-vf">✓ Verified Operator</div></div>{% endif %}
        {% if section.settings.rv3 != blank %}<div class="pdpv66__revcard"><div class="pdpv66__rev-stars">★★★★★</div><div class="pdpv66__rev-txt">"{{ section.settings.rv3 }}"</div><div class="pdpv66__rev-auth">{{ section.settings.rv3n }}</div><div class="pdpv66__rev-vf">✓ Verified Operator</div></div>{% endif %}
      </div>
    {% endif %}

    <!-- FEATURED -->
    {% if section.settings.feat_coll != blank %}
      {%- assign fc = collections[section.settings.feat_coll] -%}
      {% if fc.products.size > 0 %}
        <div class="pdpv66__sh">Recommended Violet Nodes</div>
        <div class="pdpv66__pgrid">
          {% for item in fc.products limit: 4 %}
            {% if item.handle != product.handle %}
              <a href="{{ item.url }}" class="pdpv66__pcard">
                <img height="{{ item.featured_image.height }}" width="{{ item.featured_image.width }}" src="{{ item.featured_image | image_url: width: 400 }}" alt="{{ item.title | escape }}" loading="lazy">
                <div class="pdpv66__pcard-info">
                  <div class="pdpv66__pcard-title">{{ item.title }}</div>
                  <div class="pdpv66__pcard-price">{{ item.price | money }}</div>
                </div>
              </a>
            {% endif %}
          {% endfor %}
        </div>
      {% endif %}
    {% endif %}

    <!-- RECENTLY VIEWED -->
    <div id="pdpv66-rv">
      <div class="pdpv66__sh">Recently Viewed</div>
      <div class="pdpv66__pgrid" id="pdpv66-rvg"></div>
    </div>
  </div>
</div>

<!-- STICKY ATC -->
<div class="pdpv66__sticky" id="pdpv66-sticky">
  <div class="pdpv66__sticky-l">
    {% if product.featured_media %}<img height="{{ product.featured_media.height }}" width="{{ product.featured_media.width }}" src="{{ product.featured_media | image_url: width: 80 }}" class="pdpv66__sticky-img" alt="{{ product.title | escape }}">{% endif %}
    <div>
      <div class="pdpv66__sticky-t">{{ product.title }}</div>
      <div class="pdpv66__sticky-p" id="pdpv66-sp">{{ current_variant.price | money }}</div>
    </div>
  </div>
  
      <!-- D2C SCARCITY & NOTIFY -->
      {% if product.available %}
        <div style="background:#FFF3E0; border:1px solid #FFCC80; padding:10px; border-radius:8px; margin-bottom:12px; display:flex; align-items:center; gap:12px;">
          <svg style="width:24px; height:24px; color:#E65100; flex-shrink:0;" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
          <div style="font-size:13px; color:#E65100;">
            <div style="font-weight:700;">Limited Time Offer Ends In:</div>
            <div style="font-size:16px; font-weight:700; margin-top:2px;"><b>{% render 'countdown-timer', product: product %}</b></div>
          </div>
        </div>
      {% endif %}

      {% render 'back-in-stock', product: product %}

      {% if product.available %}
      <button type="button" class="pdpv66__sticky-btn" id="pdpv66-sbtn">Add to Cart</button>
      {% else %}
      <button class="pdpv66__sticky-btn" disabled style="opacity:0.5; cursor:not-allowed;">SOLD OUT</button>
      {% endif %}
</div>

<div class="pdpv66__toast" id="pdpv66-toast">System: Added to cart!</div>

<script>
(function(){
  var PJ={{ product | json }}, cid={{ current_variant.id }};
  function getO(){ var o=[]; document.querySelectorAll('.pdpv66__vr:checked').forEach(function(r){ o.push({p:parseInt(r.dataset.pos),v:r.value}); }); o.sort(function(a,b){return a.p-b.p}); return o.map(function(x){return x.v}); }
  function findV(o){ for(var i=0;i<PJ.variants.length;i++){ var v=PJ.variants[i],ok=true; for(var j=0;j<o.length;j++){ if(v.options[j]!==o[j]){ ok=false; break; } } if(ok) return v; } return null; }
  function upd(){
    var o=getO(); if(!o.length) return; var v=findV(o); if(!v) return; cid=v.id;
    document.getElementById('pdpv66-vid').value=v.id;
    var pf=(v.price/100).toLocaleString('en-US',{style:'currency',currency:PJ.currency||'USD'});
    var pe=document.getElementById('pdpv66-price'); if(pe) pe.textContent=pf;
    var sp=document.getElementById('pdpv66-sp'); if(sp) sp.textContent=pf;
    var ab=document.getElementById('pdpv66-atc');
    if(ab){ ab.disabled=!v.available; ab.textContent=v.available?'ADD TO CART — '+pf:'SOLD OUT'; }
    var u=new URL(window.location); u.searchParams.set('variant',v.id); history.replaceState({},'',u);
  }
  document.querySelectorAll('.pdpv66__vr').forEach(function(r){ r.addEventListener('change',upd); });

  var fm=document.getElementById('pdpv66-form');
  if(fm) fm.addEventListener('submit',function(e){
    e.preventDefault(); var b=document.getElementById('pdpv66-atc'), ot=b.textContent;
    b.disabled=true; b.textContent='ADDING...';
    var q=parseInt(document.getElementById('pdpv66-qi').value)||1;
    document.getElementById('pdpv66-fq').value=q;
    fetch('/cart/add.js',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:cid,quantity:q})})
    .then(function(r){return r.json();})
    .then(function(){ b.textContent='✓ ADDED!'; toast66(); setTimeout(function(){ b.disabled=false; b.textContent=ot; },2000); })
    .catch(function(){ b.textContent='ERROR'; setTimeout(function(){ b.disabled=false; b.textContent=ot; },2000); });
  });

  function toast66(){ var t=document.getElementById('pdpv66-toast'); t.classList.add('show'); setTimeout(function(){ t.classList.remove('show'); },3000); }

  var qi=document.getElementById('pdpv66-qi');
  document.getElementById('pdpv66-qm').addEventListener('click',function(){ var v=parseInt(qi.value); if(v>1) qi.value=v-1; });
  document.getElementById('pdpv66-qp').addEventListener('click',function(){ var v=parseInt(qi.value); if(v<10) qi.value=v+1; });

  document.querySelectorAll('.pdpv66__th').forEach(function(th){
    th.addEventListener('click',function(){
      document.querySelectorAll('.pdpv66__th').forEach(function(t){ t.classList.remove('on'); });
      th.classList.add('on');
      var hi=document.getElementById('pdpv66-himg'), ti=th.querySelector('img');
      if(hi&&ti) hi.src=ti.src.replace(/width=\d+/,'width=900');
    });
  });

  var sb=document.getElementById('pdpv66-sticky'), ab=document.getElementById('pdpv66-atc');
  if(sb&&ab) window.addEventListener('scroll',function(){ sb.classList.toggle('show', ab.getBoundingClientRect().top<-50); });
  var sbn=document.getElementById('pdpv66-sbtn');
  if(sbn) sbn.addEventListener('click',function(){ document.getElementById('pdpv66-atc').click(); });

  document.querySelectorAll('.pdpv66__acc-btn').forEach(function(b){ b.addEventListener('click',function(){ b.closest('.pdpv66__acc-item').classList.toggle('open'); }); });

  var ce=document.getElementById('pdpv66-cd');
  if(ce){
    var hrs=parseInt('{{ section.settings.cd_hrs | default: 6 }}')||6, ts=hrs*3600;
    var sk=sessionStorage.getItem('pdpv66_cd'); if(sk) ts=parseInt(sk);
    setInterval(function(){ ts--; if(ts<=0){ ce.textContent='EXPIRED'; return; } var h=Math.floor(ts/3600), m=Math.floor((ts%3600)/60), s=ts%60; ce.textContent=h+'h '+(m<10?'0':'')+m+'m '+(s<10?'0':'')+s+'s'; sessionStorage.setItem('pdpv66_cd',ts); },1000);
  }

  if('IntersectionObserver' in window){
    var ro=new IntersectionObserver(function(es){ es.forEach(function(e){ var v=e.target.querySelector('video'); if(v){ e.isIntersecting?v.play():v.pause(); } }); },{threshold:0.5});
    document.querySelectorAll('.pdpv66__reel').forEach(function(r){ ro.observe(r); });
  }

  var RK='pdpv66_rv', cp={handle:'{{ product.handle }}',title:'{{ product.title | escape }}',price:'{{ product.price | money }}',image:'{{ product.featured_image | image_url: width: 400 }}',url:'{{ product.url }}'};
  try{ var its=JSON.parse(localStorage.getItem(RK)||'[]'); its=its.filter(function(i){ return i.handle!==cp.handle; }); its.unshift(cp); if(its.length>8) its=its.slice(0,8); localStorage.setItem(RK,JSON.stringify(its)); }catch(e){}
  try{
    var its2=JSON.parse(localStorage.getItem(RK)||'[]').filter(function(i){ return i.handle!==cp.handle; });
    var c=document.getElementById('pdpv66-rv'), g=document.getElementById('pdpv66-rvg');
    if(g&&its2.length>0){ c.classList.add('has'); var h=''; its2.slice(0,4).forEach(function(i){ h+='<a href="'+i.url+'" class="pdpv66__pcard"><img src="'+i.image+'" alt="'+i.title+'" loading="lazy"><div class="pdpv66__pcard-info"><div class="pdpv66__pcard-title">'+i.title+'</div><div class="pdpv66__pcard-price">'+i.price+'</div></div></a>'; }); g.innerHTML=h; }
  }catch(e){}
})();
</script>

{% schema %}
{
  "name": "PDP V66 — Violet Cyber",
  "tag": "section",
  "class": "pdpv66-section",
  "settings": [
    { "type": "header", "content": "Rating" },
    { "type": "checkbox", "id": "show_rating", "label": "Show rating", "default": true },
    { "type": "text", "id": "rating_val", "label": "Rating score", "default": "4.98" },
    { "type": "text", "id": "rev_count", "label": "Total reviews", "default": "2,210" },
    { "type": "header", "content": "Offer Banner" },
    { "type": "checkbox", "id": "show_offer", "label": "Show offer", "default": true },
    { "type": "text", "id": "offer_text", "label": "Offer text", "default": "[SYS] VIOLET TERMINAL: ENJOY 20% DISCOUNT WITH CODE 'VIOLET20'" },
    { "type": "header", "content": "Promo" },
    { "type": "checkbox", "id": "show_promo", "label": "Show promo", "default": true },
    { "type": "text", "id": "promo_code", "label": "Code", "default": "VIOLET20" },
    { "type": "text", "id": "promo_disc", "label": "Discount", "default": "20%" },
    { "type": "header", "content": "Urgency" },
    { "type": "checkbox", "id": "show_urgency", "label": "Show urgency", "default": true },
    { "type": "checkbox", "id": "show_countdown", "label": "Show countdown", "default": true },
    { "type": "range", "id": "cd_hrs", "label": "Countdown hours", "min": 1, "max": 48, "default": 6 },
    { "type": "header", "content": "Accordion" },
    { "type": "richtext", "id": "vlt_specs", "label": "Technical telemetry & protocol" },
    { "type": "header", "content": "FBT" },
    { "type": "collection", "id": "fbt_coll", "label": "FBT collection" },
    { "type": "header", "content": "Reels" },
    { "type": "checkbox", "id": "show_reels", "label": "Show reels", "default": true },
    { "type": "url", "id": "r1", "label": "Reel 1 URL" },
    { "type": "url", "id": "r2", "label": "Reel 2 URL" },
    { "type": "url", "id": "r3", "label": "Reel 3 URL" },
    { "type": "header", "content": "Reviews" },
    { "type": "checkbox", "id": "show_reviews", "label": "Show reviews", "default": true },
    { "type": "text", "id": "rv1", "label": "Review 1", "default": "Violet cyber terminal grid is glowing!" },
    { "type": "text", "id": "rv1n", "label": "Author 1", "default": "Cipher X." },
    { "type": "text", "id": "rv2", "label": "Review 2", "default": "High tech dark purple aesthetic." },
    { "type": "text", "id": "rv2n", "label": "Author 2", "default": "Nexus K." },
    { "type": "text", "id": "rv3", "label": "Review 3", "default": "100% cyberpunk perfection." },
    { "type": "text", "id": "rv3n", "label": "Author 3", "default": "Vortex P." },
    { "type": "header", "content": "Featured" },
    { "type": "collection", "id": "feat_coll", "label": "Featured collection" }
  ],
  "presets": [{ "name": "PDP V66 — Violet Cyber" }]
}
{% endschema %}
