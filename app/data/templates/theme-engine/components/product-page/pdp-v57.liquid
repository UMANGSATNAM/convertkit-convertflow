{%- comment -%}
  PDP V57 — PARISIAN LILAC SOFT GLAMOUR
  Design: Soft romantic French glamour fashion aesthetic, Playfair Display serif + Outfit body font
  Cream Lavender background (#F3E8FF), Soft Lilac (#A855F7), Deep Purple (#6B21A8), Charcoal Dark (#1F2937)
{%- endcomment -%}

{%- assign current_variant = product.selected_or_first_available_variant -%}
{%- assign inv_qty = current_variant.inventory_quantity | default: 15 -%}
{%- if inv_qty <= 0 -%}{%- assign inv_qty = 9 -%}{%- endif -%}
{%- assign viewer_count = inv_qty | times: 6 | modulo: 45 | plus: 22 -%}
{%- assign sold_count = inv_qty | times: 13 | modulo: 310 | plus: 150 -%}

<style>
  @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600;700&family=Playfair+Display:ital,wght@0,600;0,700;1,600&display=swap');

  .pdpv57 {
    --bg: #F3E8FF; --surface: #FFFFFF; --border: #E9D5FF; --border-dark: #A855F7;
    --text: #1F2937; --muted: #6B21A8; --accent: #A855F7; --accent-hover: #7E22CE;
    --lilac: #7E22CE; --danger: #DC2626;
    --f-head: 'Playfair Display', Georgia, serif; --f-body: 'Outfit', sans-serif;
    --r: 16px; --r-pill: 9999px; --ease: all 0.25s ease;
    background: var(--bg); color: var(--text); font-family: var(--f-body); font-size: 15px; line-height: 1.6; -webkit-font-smoothing: antialiased;
  }
  .pdpv57 *, .pdpv57 *::before, .pdpv57 *::after { box-sizing: border-box; margin: 0; padding: 0; }
  .pdpv57 img { max-width: 100%; display: block; }
  .pdpv57 button { font-family: inherit; cursor: pointer; }
  .pdpv57__wrap { max-width: 1280px; margin: 0 auto; padding: 0 20px; }

  /* OFFER BAR */
  .pdpv57__offer { background: var(--accent); color: #FFF; padding: 12px; text-align: center; font-family: var(--f-head); font-size: 15px; font-weight: 700; letter-spacing: 1px; }

  /* GRID */
  .pdpv57__grid { display: grid; grid-template-columns: 1fr; gap: 36px; padding: 36px 0; }
  @media (min-width: 992px) { .pdpv57__grid { grid-template-columns: 1.1fr 1fr; gap: 48px; padding: 48px 0; } }

  /* GALLERY */
  .pdpv57__gal { display: flex; flex-direction: column; gap: 12px; }
  .pdpv57__hero { width: 100%; aspect-ratio: 1; border-radius: var(--r); overflow: hidden; background: var(--surface); border: 1px solid var(--border); display: flex; align-items: center; justify-content: center; box-shadow: 0 10px 30px rgba(168,85,247,0.15); position: relative; }
  .pdpv57__hero img { width: 100%; height: 100%; object-fit: contain; }
  .pdpv57__lilac-tag { position: absolute; top: 14px; left: 14px; background: var(--accent); color: #FFF; font-family: var(--f-head); font-size: 12px; font-weight: 700; padding: 4px 14px; border-radius: var(--r-pill); }

  .pdpv57__thumbs { display: flex; gap: 10px; overflow-x: auto; }
  .pdpv57__th { width: 64px; height: 64px; flex-shrink: 0; border: 2px solid transparent; border-radius: 12px; overflow: hidden; cursor: pointer; background: var(--surface); transition: var(--ease); }
  .pdpv57__th:hover, .pdpv57__th.on { border-color: var(--accent); }
  .pdpv57__th img { width: 100%; height: 100%; object-fit: cover; }

  /* INFO PANEL */
  .pdpv57__info { display: flex; flex-direction: column; gap: 18px; }
  .pdpv57__crumb { font-family: var(--f-head); font-size: 14px; letter-spacing: 2px; color: var(--accent); font-weight: 700; text-transform: uppercase; }
  .pdpv57__title { font-family: var(--f-head); font-size: 36px; font-weight: 700; line-height: 1.2; color: var(--text); }
  @media (min-width: 992px) { .pdpv57__title { font-size: 44px; } }

  .pdpv57__rating { display: flex; align-items: center; gap: 8px; font-size: 14px; color: var(--muted); font-weight: 600; }
  .pdpv57__stars { color: var(--accent); }

  /* PRICE */
  .pdpv57__price-row { display: flex; align-items: baseline; gap: 12px; }
  .pdpv57__price { font-family: var(--f-head); font-size: 38px; font-weight: 800; color: var(--accent); }
  .pdpv57__cmp { font-size: 20px; color: var(--muted); text-decoration: line-through; }
  .pdpv57__badge { background: var(--accent); color: #FFF; font-weight: 700; font-size: 12px; padding: 4px 12px; border-radius: var(--r-pill); }

  /* VARIANTS */
  .pdpv57__vlabel { font-family: var(--f-head); font-size: 14px; font-weight: 700; text-transform: uppercase; color: var(--text); margin-bottom: 8px; }
  .pdpv57__vopts { display: flex; flex-wrap: wrap; gap: 10px; }
  .pdpv57__vr { display: none; }
  .pdpv57__vbtn { padding: 10px 22px; border: 1px solid var(--border-dark); border-radius: var(--r-pill); background: var(--surface); color: var(--text); font-weight: 600; font-size: 14px; transition: var(--ease); cursor: pointer; }
  .pdpv57__vbtn:hover { border-color: var(--accent); }
  .pdpv57__vr:checked + .pdpv57__vbtn { border-color: var(--accent); background: var(--accent); color: #FFF; }

  /* PROMO */
  .pdpv57__promo { background: var(--surface); border: 1px dashed var(--accent); border-radius: var(--r); padding: 14px 18px; display: flex; align-items: center; justify-content: space-between; font-size: 14px; font-weight: 600; color: var(--accent); }
  .pdpv57__promo-code { background: var(--accent); color: #FFF; font-family: var(--f-head); font-size: 13px; font-weight: 700; padding: 4px 12px; border-radius: 6px; }

  /* URGENCY */
  .pdpv57__urg { background: var(--surface); border-left: 4px solid var(--accent); border-radius: 0 var(--r) var(--r) 0; padding: 16px; display: flex; flex-direction: column; gap: 8px; font-size: 14px; font-weight: 600; box-shadow: 0 4px 14px rgba(0,0,0,0.03); }
  .pdpv57__pulse { display: inline-block; width: 8px; height: 8px; background: var(--accent); border-radius: 50%; margin-right: 6px; animation: pdpv57p 1.5s infinite; }
  @keyframes pdpv57p { 0%,100% { opacity: 1; } 50% { opacity: 0.3; } }
  .pdpv57__bar { height: 6px; background: #E9D5FF; border-radius: 3px; overflow: hidden; margin-top: 4px; }
  .pdpv57__fill { height: 100%; background: var(--accent); border-radius: 3px; }

  /* QTY & CTA */
  .pdpv57__qty-row { display: flex; align-items: center; gap: 14px; }
  .pdpv57__qty { display: flex; border: 1px solid var(--border-dark); border-radius: var(--r-pill); overflow: hidden; background: var(--surface); }
  .pdpv57__qty button { width: 42px; height: 46px; border: none; background: transparent; font-size: 18px; color: var(--text); }
  .pdpv57__qty input { width: 44px; height: 46px; border: none; border-left: 1px solid var(--border-dark); border-right: 1px solid var(--border-dark); text-align: center; font-size: 15px; font-weight: 700; background: transparent; }

  .pdpv57__atc { width: 100%; padding: 16px; border: none; border-radius: var(--r-pill); background: var(--accent); color: #FFF; font-family: var(--f-head); font-size: 17px; font-weight: 700; letter-spacing: 1px; transition: var(--ease); box-shadow: 0 6px 18px rgba(168,85,247,0.3); }
  .pdpv57__atc:hover { background: var(--accent-hover); transform: translateY(-1px); }
  .pdpv57__atc:disabled { opacity: 0.4; cursor: not-allowed; box-shadow: none; }
  .pdpv57__buynow .shopify-payment-button__button { border-radius: var(--r-pill) !important; }

  /* TRUST */
  .pdpv57__trust { display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px; text-align: center; padding: 18px 0; border-top: 1px solid var(--border-dark); border-bottom: 1px solid var(--border-dark); }
  .pdpv57__trust-item { font-size: 11px; font-weight: 700; color: var(--muted); text-transform: uppercase; }
  .pdpv57__trust-icon { font-size: 20px; margin-bottom: 3px; color: var(--accent); }
  @media (max-width: 480px) { .pdpv57__trust { grid-template-columns: repeat(2, 1fr); } }

  /* ACCORDION */
  .pdpv57__acc { border-top: 1px solid var(--border-dark); margin-top: 48px; }
  .pdpv57__acc-item { border-bottom: 1px solid var(--border-dark); }
  .pdpv57__acc-btn { width: 100%; padding: 18px 0; border: none; background: transparent; display: flex; justify-content: space-between; align-items: center; font-family: var(--f-head); font-size: 19px; font-weight: 700; color: var(--text); }
  .pdpv57__acc-btn::after { content: '🪻'; font-size: 18px; }
  .pdpv57__acc-body { max-height: 0; overflow: hidden; transition: max-height 0.35s ease; }
  .pdpv57__acc-item.open .pdpv57__acc-body { max-height: 500px; }
  .pdpv57__acc-inner { padding: 0 0 18px; font-size: 15px; line-height: 1.7; color: var(--muted); }

  /* SECTION TITLE */
  .pdpv57__sh { font-family: var(--f-head); font-size: 30px; font-weight: 700; text-align: center; margin: 48px 0 20px; color: var(--text); }

  /* REELS */
  .pdpv57__reels { display: flex; gap: 14px; overflow-x: auto; padding-bottom: 12px; }
  .pdpv57__reel { min-width: 180px; width: 180px; aspect-ratio: 9/16; background: #6B21A8; border-radius: var(--r); overflow: hidden; flex-shrink: 0; position: relative; }
  .pdpv57__reel video { width: 100%; height: 100%; object-fit: cover; }
  .pdpv57__reel-cap { position: absolute; bottom: 0; left: 0; right: 0; padding: 10px; background: linear-gradient(transparent, rgba(0,0,0,0.85)); color: #FFF; font-size: 12px; font-weight: 600; }
  .pdpv57__reel-ph { width: 100%; height: 100%; display: flex; align-items: center; justify-content: center; color: var(--bg); font-size: 36px; }

  /* CARDS */
  .pdpv57__pgrid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px; }
  @media (min-width: 768px) { .pdpv57__pgrid { grid-template-columns: repeat(4, 1fr); } }
  .pdpv57__pcard { background: var(--surface); border-radius: var(--r); border: 1px solid var(--border); overflow: hidden; transition: var(--ease); text-decoration: none; color: inherit; display: block; }
  .pdpv57__pcard:hover { border-color: var(--accent); transform: translateY(-2px); box-shadow: 0 8px 20px rgba(168,85,247,0.15); }
  .pdpv57__pcard img { width: 100%; aspect-ratio: 1; object-fit: cover; }
  .pdpv57__pcard-info { padding: 14px; }
  .pdpv57__pcard-title { font-family: var(--f-head); font-size: 16px; font-weight: 700; margin-bottom: 4px; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }
  .pdpv57__pcard-price { font-size: 15px; font-weight: 700; color: var(--accent); }

  /* REVIEWS */
  .pdpv57__revgrid { display: grid; grid-template-columns: 1fr; gap: 14px; }
  @media (min-width: 768px) { .pdpv57__revgrid { grid-template-columns: repeat(3, 1fr); } }
  .pdpv57__revcard { background: var(--surface); border-radius: var(--r); border: 1px solid var(--border); padding: 20px; }
  .pdpv57__rev-stars { color: var(--accent); font-size: 14px; margin-bottom: 8px; }
  .pdpv57__rev-txt { font-size: 14px; line-height: 1.6; color: var(--muted); font-style: italic; margin-bottom: 12px; }
  .pdpv57__rev-auth { font-family: var(--f-head); font-size: 15px; font-weight: 700; }
  .pdpv57__rev-vf { font-size: 11px; color: var(--accent); font-weight: 700; }

  /* RECENTLY VIEWED */
  #pdpv57-rv { display: none; }
  #pdpv57-rv.has { display: block; }

  /* STICKY ATC */
  .pdpv57__sticky { position: fixed; bottom: -80px; left: 0; width: 100%; background: var(--surface); border-top: 3px solid var(--accent); padding: 12px 24px; display: flex; justify-content: space-between; align-items: center; z-index: 9999; transition: bottom 0.3s ease; box-shadow: 0 -4px 20px rgba(0,0,0,0.06); }
  .pdpv57__sticky.show { bottom: 0; }
  .pdpv57__sticky-l { display: flex; align-items: center; gap: 14px; }
  .pdpv57__sticky-img { width: 42px; height: 42px; border-radius: 10px; object-fit: cover; }
  .pdpv57__sticky-t { font-family: var(--f-head); font-size: 15px; font-weight: 700; max-width: 200px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .pdpv57__sticky-p { font-size: 15px; font-weight: 700; color: var(--accent); }
  .pdpv57__sticky-btn { background: var(--accent); color: #FFF; border: none; padding: 12px 26px; border-radius: var(--r-pill); font-family: var(--f-head); font-weight: 700; font-size: 15px; transition: var(--ease); }
  .pdpv57__sticky-btn:hover { background: var(--accent-hover); }
  @media (max-width: 600px) { .pdpv57__sticky-l { display: none; } .pdpv57__sticky-btn { width: 100%; padding: 15px; } }

  .pdpv57__toast { position: fixed; top: 20px; right: 20px; background: var(--accent); color: #FFF; padding: 12px 24px; border-radius: var(--r-pill); font-family: var(--f-head); font-size: 15px; font-weight: 700; z-index: 10000; transform: translateX(130%); transition: transform 0.3s ease; box-shadow: 0 4px 16px rgba(168,85,247,0.3); }
  .pdpv57__toast.show { transform: translateX(0); }
</style>

<div class="pdpv57">
  {% if section.settings.show_offer %}<div class="pdpv57__offer">{{ section.settings.offer_text }}</div>{% endif %}

  <div class="pdpv57__wrap">
    <div class="pdpv57__grid">

      <!-- GALLERY -->
      <div class="pdpv57__gal">
        <div class="pdpv57__hero" id="pdpv57-hero">
          <span class="pdpv57__lilac-tag">🪻 SOFT LILAC</span>
          {% if product.featured_media %}<img src="{{ product.featured_media | image_url: width: 900 }}" alt="{{ product.title | escape }}" id="pdpv57-himg">{% else %}{{ 'product-1' | placeholder_svg_tag }}{% endif %}
        </div>
        <div class="pdpv57__thumbs">
          {% for media in product.media limit: 6 %}
            <div class="pdpv57__th {% if forloop.first %}on{% endif %}">
              <img src="{{ media | image_url: width: 150 }}" alt="{{ media.alt | escape }}" loading="lazy">
            </div>
          {% endfor %}
        </div>
      </div>

      <!-- INFO -->
      <div class="pdpv57__info">
        <div class="pdpv57__crumb">🪻 {% if product.collections.size > 0 %}{{ product.collections.first.title }}{% else %}Lilac Glamour{% endif %}</div>
        <h1 class="pdpv57__title">{{ product.title }}</h1>

        {% if section.settings.show_rating %}
          <div class="pdpv57__rating">
            <span class="pdpv57__stars">★★★★★</span>
            <span>{{ section.settings.rating_val }} · {{ section.settings.rev_count }} Lilac Reviews</span>
          </div>
        {% endif %}

        <div class="pdpv57__price-row">
          <span class="pdpv57__price" id="pdpv57-price">{{ current_variant.price | money }}</span>
          {% if current_variant.compare_at_price > current_variant.price %}
            <span class="pdpv57__cmp">{{ current_variant.compare_at_price | money }}</span>
            {%- assign sv = current_variant.compare_at_price | minus: current_variant.price | times: 100.0 | divided_by: current_variant.compare_at_price | round -%}
            <span class="pdpv57__badge">SAVE {{ sv }}%</span>
          {% endif %}
        </div>

        {% if product.variants.size > 1 %}
          {% for option in product.options_with_values %}
            <div>
              <div class="pdpv57__vlabel">Select {{ option.name }}</div>
              <div class="pdpv57__vopts">
                {% for value in option.values %}
                  <label>
                    <input type="radio" name="pdpv57-o-{{ option.position }}" value="{{ value | escape }}" class="pdpv57__vr" data-pos="{{ option.position }}" {% if option.selected_value == value %}checked{% endif %}>
                    <span class="pdpv57__vbtn">{{ value }}</span>
                  </label>
                {% endfor %}
              </div>
            </div>
          {% endfor %}
        {% endif %}

        {% if section.settings.show_promo %}
          <div class="pdpv57__promo">
            <span>🪻 Lilac Pass: Apply {{ section.settings.promo_code }}</span>
            <span class="pdpv57__promo-code">Save {{ section.settings.promo_disc }}</span>
          </div>
        {% endif %}

        {% if section.settings.show_urgency %}
          <div class="pdpv57__urg">
            <div><span class="pdpv57__pulse"></span><strong>{{ viewer_count }}</strong> fashion lovers active</div>
            <div>🪻 <strong>{{ sold_count }}</strong> lilac orders completed today</div>
            <div>Availability: Only <strong>{{ inv_qty }}</strong> remaining
              <div class="pdpv57__bar">
                {%- assign sp = inv_qty | times: 100 | divided_by: 45 -%}
                {%- if sp > 100 -%}{%- assign sp = 100 -%}{%- endif -%}
                <div class="pdpv57__fill" style="width:{{ sp }}%"></div>
              </div>
            </div>
            {% if section.settings.show_countdown %}
              <div>⏳ Lilac Offer Ends In: <strong id="pdpv57-cd" style="color:var(--accent)">{{ section.settings.cd_hrs }}h 00m 00s</strong></div>
            {% endif %}
          </div>
        {% endif %}

        <div class="pdpv57__qty-row">
          <div class="pdpv57__qty">
            <button type="button" id="pdpv57-qm">−</button>
            <input type="number" id="pdpv57-qi" value="1" min="1" max="10" readonly>
            <button type="button" id="pdpv57-qp">+</button>
          </div>
          {% form 'product', product, id: 'pdpv57-form', style: 'flex:1' %}
            <input type="hidden" name="id" id="pdpv57-vid" value="{{ current_variant.id }}">
            <input type="hidden" name="quantity" id="pdpv57-fq" value="1">
            <button type="submit" name="add" class="pdpv57__atc" id="pdpv57-atc" {% unless current_variant.available %}disabled{% endunless %}>
              {% if current_variant.available %}Add to Cart — {{ current_variant.price | money }}{% else %}Sold Out{% endif %}
            </button>
            <div class="pdpv57__buynow" style="margin-top:10px">{{ form | payment_button }}</div>
          {% endform %}
        </div>

        <div class="pdpv57__trust">
          <div class="pdpv57__trust-item"><div class="pdpv57__trust-icon">🔒</div>Secure</div>
          <div class="pdpv57__trust-item"><div class="pdpv57__trust-icon">🪻</div>Lilac Craft</div>
          <div class="pdpv57__trust-item"><div class="pdpv57__trust-icon">↩️</div>Easy Returns</div>
          <div class="pdpv57__trust-item"><div class="pdpv57__trust-icon">✨</div>Soft Glamour</div>
        </div>
      </div>
    </div>

    <!-- ACCORDION -->
    <div class="pdpv57__acc">
      <div class="pdpv57__acc-item open">
        <button class="pdpv57__acc-btn" type="button">Parisian Story & Lilac Craft</button>
        <div class="pdpv57__acc-body"><div class="pdpv57__acc-inner">{{ product.description }}</div></div>
      </div>
      {% if section.settings.lilac_specs != blank %}
        <div class="pdpv57__acc-item">
          <button class="pdpv57__acc-btn" type="button">Materials & Care Instructions</button>
          <div class="pdpv57__acc-body"><div class="pdpv57__acc-inner">{{ section.settings.lilac_specs }}</div></div>
        </div>
      {% endif %}
    </div>

    <!-- FBT -->
    {% if section.settings.fbt_coll != blank %}
      {%- assign fbt = collections[section.settings.fbt_coll] -%}
      {% if fbt.products.size > 0 %}
        <div class="pdpv57__sh">Soft Glamour Pairings</div>
        <div class="pdpv57__pgrid">
          {% for item in fbt.products limit: 4 %}
            <a href="{{ item.url }}" class="pdpv57__pcard">
              <img src="{{ item.featured_image | image_url: width: 400 }}" alt="{{ item.title | escape }}" loading="lazy">
              <div class="pdpv57__pcard-info">
                <div class="pdpv57__pcard-title">{{ item.title }}</div>
                <div class="pdpv57__pcard-price">{{ item.price | money }}</div>
              </div>
            </a>
          {% endfor %}
        </div>
      {% endif %}
    {% endif %}

    <!-- REELS -->
    {% if section.settings.show_reels %}
      <div class="pdpv57__sh">Soft Stream</div>
      <div class="pdpv57__reels">
        {% if section.settings.r1 != blank %}<div class="pdpv57__reel"><video src="{{ section.settings.r1 }}" muted playsinline loop></video><div class="pdpv57__reel-cap">🪻 Lilac Glow</div></div>{% else %}<div class="pdpv57__reel"><div class="pdpv57__reel-ph">▶</div></div>{% endif %}
        {% if section.settings.r2 != blank %}<div class="pdpv57__reel"><video src="{{ section.settings.r2 }}" muted playsinline loop></video><div class="pdpv57__reel-cap">✨ Soft Touch</div></div>{% else %}<div class="pdpv57__reel"><div class="pdpv57__reel-ph">▶</div></div>{% endif %}
        {% if section.settings.r3 != blank %}<div class="pdpv57__reel"><video src="{{ section.settings.r3 }}" muted playsinline loop></video><div class="pdpv57__reel-cap">🪻 Glamour Unboxing</div></div>{% else %}<div class="pdpv57__reel"><div class="pdpv57__reel-ph">▶</div></div>{% endif %}
      </div>
    {% endif %}

    <!-- REVIEWS -->
    {% if section.settings.show_reviews %}
      <div class="pdpv57__sh" id="reviews">Customer Reviews</div>
      <div class="pdpv57__revgrid">
        {% if section.settings.rv1 != blank %}<div class="pdpv57__revcard"><div class="pdpv57__rev-stars">★★★★★</div><div class="pdpv57__rev-txt">"{{ section.settings.rv1 }}"</div><div class="pdpv57__rev-auth">{{ section.settings.rv1n }}</div><div class="pdpv57__rev-vf">✓ Verified Buyer</div></div>{% endif %}
        {% if section.settings.rv2 != blank %}<div class="pdpv57__revcard"><div class="pdpv57__rev-stars">★★★★★</div><div class="pdpv57__rev-txt">"{{ section.settings.rv2 }}"</div><div class="pdpv57__rev-auth">{{ section.settings.rv2n }}</div><div class="pdpv57__rev-vf">✓ Verified Buyer</div></div>{% endif %}
        {% if section.settings.rv3 != blank %}<div class="pdpv57__revcard"><div class="pdpv57__rev-stars">★★★★★</div><div class="pdpv57__rev-txt">"{{ section.settings.rv3 }}"</div><div class="pdpv57__rev-auth">{{ section.settings.rv3n }}</div><div class="pdpv57__rev-vf">✓ Verified Buyer</div></div>{% endif %}
      </div>
    {% endif %}

    <!-- FEATURED -->
    {% if section.settings.feat_coll != blank %}
      {%- assign fc = collections[section.settings.feat_coll] -%}
      {% if fc.products.size > 0 %}
        <div class="pdpv57__sh">Recommended Lilac Items</div>
        <div class="pdpv57__pgrid">
          {% for item in fc.products limit: 4 %}
            {% if item.handle != product.handle %}
              <a href="{{ item.url }}" class="pdpv57__pcard">
                <img src="{{ item.featured_image | image_url: width: 400 }}" alt="{{ item.title | escape }}" loading="lazy">
                <div class="pdpv57__pcard-info">
                  <div class="pdpv57__pcard-title">{{ item.title }}</div>
                  <div class="pdpv57__pcard-price">{{ item.price | money }}</div>
                </div>
              </a>
            {% endif %}
          {% endfor %}
        </div>
      {% endif %}
    {% endif %}

    <!-- RECENTLY VIEWED -->
    <div id="pdpv57-rv">
      <div class="pdpv57__sh">Recently Viewed</div>
      <div class="pdpv57__pgrid" id="pdpv57-rvg"></div>
    </div>
  </div>
</div>

<!-- STICKY ATC -->
<div class="pdpv57__sticky" id="pdpv57-sticky">
  <div class="pdpv57__sticky-l">
    {% if product.featured_media %}<img src="{{ product.featured_media | image_url: width: 80 }}" class="pdpv57__sticky-img" alt="{{ product.title | escape }}">{% endif %}
    <div>
      <div class="pdpv57__sticky-t">{{ product.title }}</div>
      <div class="pdpv57__sticky-p" id="pdpv57-sp">{{ current_variant.price | money }}</div>
    </div>
  </div>
  
      <!-- D2C SCARCITY & NOTIFY -->
      {% if product.available %}
        <div style="background:#FFF3E0; border:1px solid #FFCC80; padding:10px; border-radius:8px; margin-bottom:12px; display:flex; align-items:center; gap:12px;">
          <svg style="width:24px; height:24px; color:#E65100; flex-shrink:0;" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
          <div style="font-size:13px; color:#E65100;">
            <div style="font-weight:700;">Limited Time Offer Ends In:</div>
            <div style="font-size:16px; font-weight:700; margin-top:2px;"><b>{% render 'countdown-timer', product: product %}</b></div>
          </div>
        </div>
      {% endif %}

      {% render 'back-in-stock', product: product %}

      {% if product.available %}
      <button type="button" class="pdpv57__sticky-btn" id="pdpv57-sbtn">Add to Cart</button>
      {% else %}
      <button class="pdpv57__sticky-btn" disabled style="opacity:0.5; cursor:not-allowed;">SOLD OUT</button>
      {% endif %}
</div>

<div class="pdpv57__toast" id="pdpv57-toast">🪻 Added to cart!</div>

<script>
(function(){
  var PJ={{ product | json }}, cid={{ current_variant.id }};
  function getO(){ var o=[]; document.querySelectorAll('.pdpv57__vr:checked').forEach(function(r){ o.push({p:parseInt(r.dataset.pos),v:r.value}); }); o.sort(function(a,b){return a.p-b.p}); return o.map(function(x){return x.v}); }
  function findV(o){ for(var i=0;i<PJ.variants.length;i++){ var v=PJ.variants[i],ok=true; for(var j=0;j<o.length;j++){ if(v.options[j]!==o[j]){ ok=false; break; } } if(ok) return v; } return null; }
  function upd(){
    var o=getO(); if(!o.length) return; var v=findV(o); if(!v) return; cid=v.id;
    document.getElementById('pdpv57-vid').value=v.id;
    var pf=(v.price/100).toLocaleString('en-US',{style:'currency',currency:PJ.currency||'USD'});
    var pe=document.getElementById('pdpv57-price'); if(pe) pe.textContent=pf;
    var sp=document.getElementById('pdpv57-sp'); if(sp) sp.textContent=pf;
    var ab=document.getElementById('pdpv57-atc');
    if(ab){ ab.disabled=!v.available; ab.textContent=v.available?'Add to Cart — '+pf:'Sold Out'; }
    var u=new URL(window.location); u.searchParams.set('variant',v.id); history.replaceState({},'',u);
  }
  document.querySelectorAll('.pdpv57__vr').forEach(function(r){ r.addEventListener('change',upd); });

  var fm=document.getElementById('pdpv57-form');
  if(fm) fm.addEventListener('submit',function(e){
    e.preventDefault(); var b=document.getElementById('pdpv57-atc'), ot=b.textContent;
    b.disabled=true; b.textContent='Adding...';
    var q=parseInt(document.getElementById('pdpv57-qi').value)||1;
    document.getElementById('pdpv57-fq').value=q;
    fetch('/cart/add.js',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:cid,quantity:q})})
    .then(function(r){return r.json();})
    .then(function(){ b.textContent='✓ Added!'; toast57(); setTimeout(function(){ b.disabled=false; b.textContent=ot; },2000); })
    .catch(function(){ b.textContent='Error'; setTimeout(function(){ b.disabled=false; b.textContent=ot; },2000); });
  });

  function toast57(){ var t=document.getElementById('pdpv57-toast'); t.classList.add('show'); setTimeout(function(){ t.classList.remove('show'); },3000); }

  var qi=document.getElementById('pdpv57-qi');
  document.getElementById('pdpv57-qm').addEventListener('click',function(){ var v=parseInt(qi.value); if(v>1) qi.value=v-1; });
  document.getElementById('pdpv57-qp').addEventListener('click',function(){ var v=parseInt(qi.value); if(v<10) qi.value=v+1; });

  document.querySelectorAll('.pdpv57__th').forEach(function(th){
    th.addEventListener('click',function(){
      document.querySelectorAll('.pdpv57__th').forEach(function(t){ t.classList.remove('on'); });
      th.classList.add('on');
      var hi=document.getElementById('pdpv57-himg'), ti=th.querySelector('img');
      if(hi&&ti) hi.src=ti.src.replace(/width=\d+/,'width=900');
    });
  });

  var sb=document.getElementById('pdpv57-sticky'), ab=document.getElementById('pdpv57-atc');
  if(sb&&ab) window.addEventListener('scroll',function(){ sb.classList.toggle('show', ab.getBoundingClientRect().top<-50); });
  var sbn=document.getElementById('pdpv57-sbtn');
  if(sbn) sbn.addEventListener('click',function(){ document.getElementById('pdpv57-atc').click(); });

  document.querySelectorAll('.pdpv57__acc-btn').forEach(function(b){ b.addEventListener('click',function(){ b.closest('.pdpv57__acc-item').classList.toggle('open'); }); });

  var ce=document.getElementById('pdpv57-cd');
  if(ce){
    var hrs=parseInt('{{ section.settings.cd_hrs | default: 8 }}')||8, ts=hrs*3600;
    var sk=sessionStorage.getItem('pdpv57_cd'); if(sk) ts=parseInt(sk);
    setInterval(function(){ ts--; if(ts<=0){ ce.textContent='EXPIRED'; return; } var h=Math.floor(ts/3600), m=Math.floor((ts%3600)/60), s=ts%60; ce.textContent=h+'h '+(m<10?'0':'')+m+'m '+(s<10?'0':'')+s+'s'; sessionStorage.setItem('pdpv57_cd',ts); },1000);
  }

  if('IntersectionObserver' in window){
    var ro=new IntersectionObserver(function(es){ es.forEach(function(e){ var v=e.target.querySelector('video'); if(v){ e.isIntersecting?v.play():v.pause(); } }); },{threshold:0.5});
    document.querySelectorAll('.pdpv57__reel').forEach(function(r){ ro.observe(r); });
  }

  var RK='pdpv57_rv', cp={handle:'{{ product.handle }}',title:'{{ product.title | escape }}',price:'{{ product.price | money }}',image:'{{ product.featured_image | image_url: width: 400 }}',url:'{{ product.url }}'};
  try{ var its=JSON.parse(localStorage.getItem(RK)||'[]'); its=its.filter(function(i){ return i.handle!==cp.handle; }); its.unshift(cp); if(its.length>8) its=its.slice(0,8); localStorage.setItem(RK,JSON.stringify(its)); }catch(e){}
  try{
    var its2=JSON.parse(localStorage.getItem(RK)||'[]').filter(function(i){ return i.handle!==cp.handle; });
    var c=document.getElementById('pdpv57-rv'), g=document.getElementById('pdpv57-rvg');
    if(g&&its2.length>0){ c.classList.add('has'); var h=''; its2.slice(0,4).forEach(function(i){ h+='<a href="'+i.url+'" class="pdpv57__pcard"><img src="'+i.image+'" alt="'+i.title+'" loading="lazy"><div class="pdpv57__pcard-info"><div class="pdpv57__pcard-title">'+i.title+'</div><div class="pdpv57__pcard-price">'+i.price+'</div></div></a>'; }); g.innerHTML=h; }
  }catch(e){}
})();
</script>

{% schema %}
{
  "name": "PDP V57 — Soft Lilac",
  "tag": "section",
  "class": "pdpv57-section",
  "settings": [
    { "type": "header", "content": "Rating" },
    { "type": "checkbox", "id": "show_rating", "label": "Show rating", "default": true },
    { "type": "text", "id": "rating_val", "label": "Rating score", "default": "4.96" },
    { "type": "text", "id": "rev_count", "label": "Total reviews", "default": "1,820" },
    { "type": "header", "content": "Offer Banner" },
    { "type": "checkbox", "id": "show_offer", "label": "Show offer", "default": true },
    { "type": "text", "id": "offer_text", "label": "Offer text", "default": "🪻 PARISIAN LILAC: ENJOY 20% DISCOUNT WITH CODE 'SOFTLILAC20'" },
    { "type": "header", "content": "Promo" },
    { "type": "checkbox", "id": "show_promo", "label": "Show promo", "default": true },
    { "type": "text", "id": "promo_code", "label": "Code", "default": "SOFTLILAC20" },
    { "type": "text", "id": "promo_disc", "label": "Discount", "default": "20%" },
    { "type": "header", "content": "Urgency" },
    { "type": "checkbox", "id": "show_urgency", "label": "Show urgency", "default": true },
    { "type": "checkbox", "id": "show_countdown", "label": "Show countdown", "default": true },
    { "type": "range", "id": "cd_hrs", "label": "Countdown hours", "min": 1, "max": 48, "default": 8 },
    { "type": "header", "content": "Accordion" },
    { "type": "richtext", "id": "lilac_specs", "label": "Materials & care instructions" },
    { "type": "header", "content": "FBT" },
    { "type": "collection", "id": "fbt_coll", "label": "FBT collection" },
    { "type": "header", "content": "Reels" },
    { "type": "checkbox", "id": "show_reels", "label": "Show reels", "default": true },
    { "type": "url", "id": "r1", "label": "Reel 1 URL" },
    { "type": "url", "id": "r2", "label": "Reel 2 URL" },
    { "type": "url", "id": "r3", "label": "Reel 3 URL" },
    { "type": "header", "content": "Reviews" },
    { "type": "checkbox", "id": "show_reviews", "label": "Show reviews", "default": true },
    { "type": "text", "id": "rv1", "label": "Review 1", "default": "Soft lilac color and French glamour styling." },
    { "type": "text", "id": "rv1n", "label": "Author 1", "default": "Camille D." },
    { "type": "text", "id": "rv2", "label": "Review 2", "default": "Delicate lavender finish and fast shipping." },
    { "type": "text", "id": "rv2n", "label": "Author 2", "default": "Colette L." },
    { "type": "text", "id": "rv3", "label": "Review 3", "default": "True Parisian romantic aesthetic." },
    { "type": "text", "id": "rv3n", "label": "Author 3", "default": "Solene P." },
    { "type": "header", "content": "Featured" },
    { "type": "collection", "id": "feat_coll", "label": "Featured collection" }
  ],
  "presets": [{ "name": "PDP V57 — Soft Lilac" }]
}
{% endschema %}
