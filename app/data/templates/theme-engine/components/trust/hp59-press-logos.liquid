{%- style -%}
  #shopify-section-{{ section.id }} {
    background-color: {{ section.settings.bg_color | default: '#FFFBEB' }};
    padding: {{ section.settings.padding_vertical | default: 30 }}px 0;
    border-bottom: 1px solid rgba(249, 115, 22, 0.15);
  }
  .hp59-press__container { max-width: 1280px; margin: 0 auto; padding: 0 24px; }
  .hp59-press__label {
    text-align: center; font-family: 'Plus Jakarta Sans', sans-serif; font-size: 11px; font-weight: 700;
    letter-spacing: 3px; text-transform: uppercase;
    color: {{ section.settings.label_color | default: '#F97316' }}; margin: 0 0 24px;
  }
  .hp59-press__logos { display: flex; align-items: center; justify-content: center; gap: 50px; flex-wrap: wrap; }
  .hp59-press__logo {
    height: {{ section.settings.logo_height | default: 26 }}px;
    opacity: 0.55; filter: grayscale(100%);
    transition: all 0.3s ease;
  }
  .hp59-press__logo:hover { opacity: 1; filter: grayscale(0%); }
  @media screen and (max-width: 768px) { .hp59-press__logos { gap: 30px; } .hp59-press__logo { height: 20px; } }
{%- endstyle -%}
<div class="hp59-press__container">
  {%- if section.settings.label != blank -%}<p class="hp59-press__label">{{ section.settings.label }}</p>{%- endif -%}
  <div class="hp59-press__logos">
    {%- for block in section.blocks -%}
      {%- if block.settings.logo != blank -%}<img height="{{ block.settings.logo.height }}" width="{{ block.settings.logo.width }}" src="{{ block.settings.logo | image_url: height: 60 }}" class="hp59-press__logo" alt="{{ block.settings.alt | default: 'Press' }}" loading="lazy">{%- endif -%}
    {%- endfor -%}
  </div>
</div>
{% schema %}
{
  "name": "HP59 Press",
  "class": "section-hp59-press",
  "settings": [
    { "type": "color", "id": "bg_color", "label": "Background", "default": "#FFFBEB" },
    { "type": "color", "id": "label_color", "label": "Label", "default": "#F97316" },
    { "type": "text", "id": "label", "label": "Label", "default": "AS FEATURED IN" },
    { "type": "range", "id": "logo_height", "min": 16, "max": 50, "step": 2, "unit": "px", "label": "Logo Height", "default": 26 },
    { "type": "range", "id": "padding_vertical", "min": 10, "max": 60, "step": 5, "unit": "px", "label": "Padding", "default": 30 }
  ],
  "blocks": [{ "type": "logo", "name": "Logo", "settings": [
    { "type": "image_picker", "id": "logo", "label": "Logo" },
    { "type": "text", "id": "alt", "label": "Alt Text" }
  ]}],
  "presets": [{ "name": "HP59 Press" }]
}
{% endschema %}
