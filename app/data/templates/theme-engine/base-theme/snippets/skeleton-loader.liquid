<style>
  .sf-skeleton {
    animation: sf-pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    background-color: color-mix(in srgb, var(--color-surface) 95%, var(--color-text));
    border-radius: var(--radius-card);
  }
  @keyframes sf-pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: .5; }
  }
</style>
<div class="sf-skeleton w-full h-full min-h-[200px]" aria-hidden="true"></div>
