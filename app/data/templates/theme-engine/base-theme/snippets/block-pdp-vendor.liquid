{%- if product.vendor != blank -%}
                  <div class="sf-pdp-vendor sf-stagger-item" {{ block.shopify_attributes }}>
                    {{ product.vendor }}
                  </div>
                {%- endif -%}
