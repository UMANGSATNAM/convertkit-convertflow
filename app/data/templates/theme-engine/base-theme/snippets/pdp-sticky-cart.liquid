<div class="sticky-bar" id="pdpSticky-{{ section.id }}">
  <div class="sb-info">
    <div class="nm">{{ product.title | truncate: 22 }}</div>
    <div class="pr"><b id="pdpSbPrice-{{ section.id }}">{{ current_variant.price | money }}</b></div>
  </div>
  <button type="button" class="btn btn-add pdp-sticky-add">Add to Bag</button>
</div>
