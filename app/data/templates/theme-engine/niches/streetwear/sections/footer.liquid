<footer class="footer footer--streetwear" style="background-color: #000; color: #fff; padding: var(--space-64) 0; border-top: 3px solid var(--color-border); font-size: 0.85rem; font-family: var(--font-body-family); font-weight: 600;">
  <div class="container grid grid-cols-1 grid-cols-md-2 grid-cols-lg-4" style="--grid-spacing: var(--space-48); margin-bottom: var(--space-48);">
    
    {%- for block in section.blocks -%}
      {%- case block.type -%}
        {%- when 'link_list' -%}
          <div class="footer-widget footer-widget--menu" {{ block.shopify_attributes }}>
            <h4 class="footer-widget-title" style="font-family: var(--font-heading-family); font-size: 1.3rem; font-weight: 900; color: #fff; margin-bottom: var(--space-20);">
              // {{ block.settings.heading | uppercase }} //
            </h4>
            <ul style="list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: var(--space-12);">
              {%- for link in linklists[block.settings.menu].links -%}
                <li>
                  <a href="{{ link.url }}" class="footer-menu-link" style="transition: color var(--transition-fast); color: #a5a5a5;">
                    {{ link.title | uppercase }}
                  </a>
                </li>
              {%- endfor -%}
            </ul>
          </div>

        {%- when 'text' -%}
          <div class="footer-widget footer-widget--text" {{ block.shopify_attributes }}>
            <h4 class="footer-widget-title" style="font-family: var(--font-heading-family); font-size: 1.3rem; font-weight: 900; color: #fff; margin-bottom: var(--space-20);">
              // {{ block.settings.heading | uppercase }} //
            </h4>
            <div style="line-height: 1.6; color: #a5a5a5; font-size: 0.85rem;">
              {{ block.settings.subtext }}
            </div>
          </div>

        {%- when 'newsletter' -%}
          <div class="footer-widget footer-widget--newsletter" {{ block.shopify_attributes }}>
            <h4 class="footer-widget-title" style="font-family: var(--font-heading-family); font-size: 1.3rem; font-weight: 900; color: #fff; margin-bottom: var(--space-20);">
              // {{ block.settings.heading | uppercase }} //
            </h4>
            <div style="margin-bottom: var(--space-16); color: #a5a5a5;">
              <p>DROP PASSWORDS & ACCESS LOGS.</p>
            </div>
            
            {%- form 'customer', class: 'footer-newsletter-form' -%}
              <input type="hidden" name="contact[tags]" value="newsletter">
              <div style="display: flex; border: 2px solid #fff; padding: 4px var(--space-8); border-radius: var(--button-radius);">
                <input type="email" name="contact[email]" placeholder="EMAIL ADDRESS" required 
                       style="background: none; border: none; color: #fff; font-family: var(--font-body-family); font-weight: bold; font-size: 0.85rem; padding: var(--space-8) 0; flex-grow: 1; outline: none; width: 100%;">
                <button type="submit" aria-label="Subscribe" style="background: none; border: none; color: var(--color-accent); cursor: pointer; padding: 0 var(--space-8);">
                  {% render 'icon-chevron' %}
                </button>
              </div>
            {%- endform -%}
          </div>

      {%- endcase -%}
    {%- endfor -%}

  </div>

  <div class="container footer-bottom" style="border-top: 2px solid #2b2b2b; padding-top: var(--space-32); display: flex; flex-direction: column; gap: var(--space-20); align-items: center;" class="md-flex-row">
    
    <div style="display: flex; flex-direction: column; gap: var(--space-16); align-items: center;" class="md-flex-row">
      {% render 'social-icons' %}
      <div style="color: #a5a5a5; font-size: 0.8rem; font-weight: bold;">
        &copy; {{ 'now' | date: '%Y' }} {{ shop.name | uppercase }} // ALL OPERATIONS RESERVED.
      </div>
    </div>

    {%- if section.settings.show_payment_methods -%}
      <div class="footer-payments" style="display: flex; gap: var(--space-8); flex-wrap: wrap;">
        {%- for type in shop.enabled_payment_types -%}
          <span style="width: 36px; height: 24px; display: inline-block; background-color: #fff; padding: 1px; border-radius: 2px;">
            {{ type | payment_type_svg_tag: class: 'payment-icon' }}
          </span>
        {%- endfor -%}
      </div>
    {%- endif -%}

  </div>
</footer>

{% schema %}
{
  "name": "Footer",
  "settings": [
    {
      "type": "checkbox",
      "id": "show_payment_methods",
      "label": "Show payment methods",
      "default": true
    }
  ],
  "blocks": [
    {
      "type": "link_list",
      "name": "Link List Column",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Column Heading",
          "default": "SYSTEM LINKS"
        },
        {
          "type": "link_list",
          "id": "menu",
          "label": "Navigation Menu"
        }
      ]
    },
    {
      "type": "text",
      "name": "Text Column",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Column Heading",
          "default": "MISSION STATEMENT"
        },
        {
          "type": "richtext",
          "id": "subtext",
          "label": "Column Text",
          "default": "<p>ENGINEERED SUB-SYSTEMS FOR ACTIVE URBAN UTILITY. HIGH SPECIFICATION MANUFACTURING STANDARDS.</p>"
        }
      ]
    },
    {
      "type": "newsletter",
      "name": "Newsletter Subscribe Column",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Column Heading",
          "default": "SYSTEM COMMUNICATIONS"
        }
      ]
    }
  ]
}
{% endschema %}
