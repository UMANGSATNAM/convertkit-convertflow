<style>
  .kw-pgallery {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  @media (min-width: 768px) {
    .kw-pgallery {
      gap: 20px;
    }
  }
  .kw-pgallery__item {
    width: 100%;
    background-color: #f4f4f4;
  }
  .kw-pgallery__img {
    width: 100%;
    height: auto;
    display: block;
  }
</style>

<div class="kw-pgallery">
  {% for media in product.media %}
    <div class="kw-pgallery__item">
      {% case media.media_type %}
        {% when 'image' %}
          <img class="kw-pgallery__img" src="{{ media | image_url: width: 1200 }}" alt="{{ media.alt | escape }}">
        {% when 'video' %}
          {{ media | video_tag: controls: true, class: 'kw-pgallery__img' }}
      {% endcase %}
    </div>
  {% else %}
    <div class="kw-pgallery__item" style="padding: 100px; text-align: center;">No Images</div>
  {% endfor %}
</div>

{% schema %}
{
  "name": "KW Product Gallery",
  "tag": "section",
  "settings": [],
  "templates": ["product"],
  "presets": [
    {
      "name": "KW Product Gallery"
    }
  ]
}
{% endschema %}
