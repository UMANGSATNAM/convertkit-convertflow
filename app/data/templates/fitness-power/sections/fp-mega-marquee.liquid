<style>
  .fp-marquee-wrapper {
    overflow: hidden;
    padding: 80px 0;
    background: var(--fp-color-bg, #0a0a0a);
    color: var(--fp-color-text, #fff);
    border-top: 1px solid rgba(255,255,255,0.05);
    border-bottom: 1px solid rgba(255,255,255,0.05);
  }
  .fp-marquee {
    display: flex;
    white-space: nowrap;
    animation: fp-marquee-scroll 30s linear infinite;
  }
  .fp-marquee:hover {
    animation-play-state: paused;
  }
  .fp-marquee-text {
    font-family: var(--fp-font-sans, sans-serif);
    font-size: clamp(60px, 8vw, 120px);
    font-weight: 900;
    text-transform: uppercase;
    padding: 0 40px;
    letter-spacing: -2px;
    -webkit-text-stroke: 1px rgba(255,255,255,0.2);
    color: transparent;
    transition: all 0.3s ease;
    cursor: default;
  }
  .fp-marquee-text:hover {
    color: #ff4500;
    -webkit-text-stroke: 0;
    text-shadow: 0 0 40px #ff450080;
  }
  @keyframes fp-marquee-scroll {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }
</style>

<div class="fp-marquee-wrapper">
  <div class="fp-marquee">
    <div class="fp-marquee-text">{{ section.settings.text | default: 'UNCOMPROMISING QUALITY • INNOVATIVE DESIGN • EXCEPTIONAL EXPERIENCE •' }}</div>
    <div class="fp-marquee-text">{{ section.settings.text | default: 'UNCOMPROMISING QUALITY • INNOVATIVE DESIGN • EXCEPTIONAL EXPERIENCE •' }}</div>
  </div>
</div>

{% schema %}
{
  "name": "Mega Marquee",
  "tag": "section",
  "settings": [
    { "type": "text", "id": "text", "label": "Marquee Text" }
  ],
  "presets": [{ "name": "Mega Marquee" }]
}
{% endschema %}
