<style>
  .fp-pgallery {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  @media (min-width: 768px) {
    .fp-pgallery {
      gap: 20px;
    }
  }
  .fp-pgallery__item {
    width: 100%;
    background-color: #f4f4f4;
  }
  .fp-pgallery__img {
    width: 100%;
    height: auto;
    display: block;
  }
</style>

<div class="fp-pgallery">
  {% for media in product.media %}
    <div class="fp-pgallery__item">
      {% case media.media_type %}
        {% when 'image' %}
          <img class="fp-pgallery__img" src="{{ media | image_url: width: 1200 }}" alt="{{ media.alt | escape }}">
        {% when 'video' %}
          {{ media | video_tag: controls: true, class: 'fp-pgallery__img' }}
      {% endcase %}
    </div>
  {% else %}
    <div class="fp-pgallery__item" style="padding: 100px; text-align: center;">No Images</div>
  {% endfor %}
</div>

{% schema %}
{
  "name": "FP Product Gallery",
  "tag": "section",
  "settings": [],
  "templates": ["product"],
  "presets": [
    {
      "name": "FP Product Gallery"
    }
  ]
}
{% endschema %}
