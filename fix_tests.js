const fs=require('fs'); ['tests/compiler/component-replacement.test.ts', 'tests/compiler/registry-integrity.test.ts', 'tests/smoke/compile-jewel-luxe.test.ts', 'tests/compiler/hardening.test.ts'].forEach(p => { try { let c = fs.readFileSync(p, 'utf8'); c = c.replace(/componentId: 'mock'/g, \componentId: 'mock', sectionType: 'mock'\); c = c.replace(/componentId: 'mock-id'/g, \componentId: 'mock-id', sectionType: 'mock-id'\); fs.writeFileSync(p, c); } catch(e){} });
