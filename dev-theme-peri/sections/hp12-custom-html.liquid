{%- liquid
  assign section_id = section.id
-%}

<div id="shopify-section-{{ section_id }}" class="hp12-custom-html-wrapper" style="padding: {{ section.settings.padding_top }}px {{ section.settings.padding_side }}px {{ section.settings.padding_bottom }}px;">
  {{ section.settings.custom_html }}
</div>

{% schema %}
{
  "name": "HP12 Custom HTML",
  "settings": [
    {
      "type": "html",
      "id": "custom_html",
      "label": "Custom HTML",
      "default": "<div style='text-align: center; font-family: Inter, sans-serif; font-size: 14px; font-weight: 300; letter-spacing: 2px; color: #333; opacity: 0.5;'>— CUSTOM HTML BLOCK —</div>"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "Padding Top",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_side",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "Padding Side",
      "default": 20
    }
  ],
  "presets": [
    {
      "name": "HP12 Custom HTML"
    }
  ]
}
{% endschema %}
