<style>
@media screen and (max-width: 768px) {
  .hp56-marquee {
    padding-left: 16px !important;
    padding-right: 16px !important;
    box-sizing: border-box !important;
  }
  img {
    max-width: 100% !important;
    height: auto !important;
  }
}
</style>
{%- style -%}
  #shopify-section-{{ section.id }} {
    --hp56-marq-bg: {{ section.settings.bg_color | default: '#082F49' }};
    --hp56-marq-text: {{ section.settings.text_color | default: '#E0F2FE' }};
    background: var(--hp56-marq-bg);
    overflow: hidden;
    padding: {{ section.settings.padding_vertical | default: 10 }}px 0;
  }
  .hp56-marquee__track {
    display: flex; gap: 50px;
    animation: hp56MarqueeScroll {{ section.settings.speed | default: 25 }}s linear infinite;
    width: max-content;
  }
  .hp56-marquee__item {
    display: flex; align-items: center; gap: 50px; white-space: nowrap;
    font-family: 'Syne', sans-serif;
    font-size: {{ section.settings.font_size | default: 14 }}px;
    font-weight: 700; letter-spacing: 2.5px; text-transform: uppercase;
    color: var(--hp56-marq-text);
  }
  .hp56-marquee__dot {
    width: 6px; height: 6px;
    background: {{ section.settings.dot_color | default: '#0284C7' }};
    border-radius: 50%; flex-shrink: 0;
  }
  @keyframes hp56MarqueeScroll {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }
{%- endstyle -%}
<div class="hp56-marquee__track">
  {%- for i in (1..12) -%}
    <div class="hp56-marquee__item">
      <span>{{ section.settings.marquee_text | default: '❄️ ARCTIC CLOUDBERRY ❄️ FINNISH BIRCH SAP ❄️ GLACIAL OXYGEN ❄️ SUB-ZERO BIO' }}</span>
      <span class="hp56-marquee__dot"></span>
    </div>
  {%- endfor -%}
</div>
{% schema %}
{
  "name": "HP56 Marquee",
  "class": "section-hp56-marquee",
  "settings": [
    { "type": "color", "id": "bg_color", "label": "Background", "default": "#082F49" },
    { "type": "color", "id": "text_color", "label": "Text Color", "default": "#E0F2FE" },
    { "type": "color", "id": "dot_color", "label": "Dot Color", "default": "#0284C7" },
    { "type": "text", "id": "marquee_text", "label": "Marquee Text", "default": "❄️ ARCTIC CLOUDBERRY ❄️ FINNISH BIRCH SAP ❄️ GLACIAL OXYGEN ❄️ SUB-ZERO BIO" },
    { "type": "range", "id": "speed", "min": 10, "max": 50, "step": 5, "unit": "s", "label": "Speed", "default": 25 },
    { "type": "range", "id": "font_size", "min": 10, "max": 22, "step": 1, "unit": "px", "label": "Font Size", "default": 14 },
    { "type": "range", "id": "padding_vertical", "min": 4, "max": 24, "step": 2, "unit": "px", "label": "Padding", "default": 10 }
  ],
  "presets": [{ "name": "HP56 Marquee" }]
}
{% endschema %}
