<style>
@media screen and (max-width: 768px) {
  .hp54-marquee {
    padding-left: 16px !important;
    padding-right: 16px !important;
    box-sizing: border-box !important;
  }
  img {
    max-width: 100% !important;
    height: auto !important;
  }
}
</style>
{%- style -%}
  #shopify-section-{{ section.id }} {
    --hp54-marq-bg: {{ section.settings.bg_color | default: '#0F1D32' }};
    --hp54-marq-text: {{ section.settings.text_color | default: '#60A5FA' }};
    background: var(--hp54-marq-bg);
    overflow: hidden;
    padding: {{ section.settings.padding_vertical | default: 10 }}px 0;
  }
  .hp54-marquee__track {
    display: flex; gap: 50px;
    animation: hp54MarqueeScroll {{ section.settings.speed | default: 25 }}s linear infinite;
    width: max-content;
  }
  .hp54-marquee__item {
    display: flex; align-items: center; gap: 50px; white-space: nowrap;
    font-family: 'Tenor Sans', serif;
    font-size: {{ section.settings.font_size | default: 14 }}px;
    font-weight: 400; letter-spacing: 3px; text-transform: uppercase;
    color: var(--hp54-marq-text);
  }
  .hp54-marquee__dot {
    width: 6px; height: 6px;
    background: {{ section.settings.dot_color | default: '#3B82F6' }};
    border-radius: 50%; flex-shrink: 0;
  }
  @keyframes hp54MarqueeScroll {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }
{%- endstyle -%}
<div class="hp54-marquee__track">
  {%- for i in (1..12) -%}
    <div class="hp54-marquee__item">
      <span>{{ section.settings.marquee_text | default: '◆ MIDNIGHT SAPPHIRE ◆ DEEP SEA PEPTIDES ◆ BLUE TANSY ◆ LUNAR GLOW' }}</span>
      <span class="hp54-marquee__dot"></span>
    </div>
  {%- endfor -%}
</div>
{% schema %}
{
  "name": "HP54 - Sapphire Marquee",
  "class": "section-hp54-marquee",
  "settings": [
    { "type": "color", "id": "bg_color", "label": "Background", "default": "#0F1D32" },
    { "type": "color", "id": "text_color", "label": "Text Color", "default": "#60A5FA" },
    { "type": "color", "id": "dot_color", "label": "Dot Color", "default": "#3B82F6" },
    { "type": "text", "id": "marquee_text", "label": "Marquee Text", "default": "◆ MIDNIGHT SAPPHIRE ◆ DEEP SEA PEPTIDES ◆ BLUE TANSY ◆ LUNAR GLOW" },
    { "type": "range", "id": "speed", "min": 10, "max": 50, "step": 5, "unit": "s", "label": "Speed", "default": 25 },
    { "type": "range", "id": "font_size", "min": 10, "max": 22, "step": 1, "unit": "px", "label": "Font Size", "default": 14 },
    { "type": "range", "id": "padding_vertical", "min": 4, "max": 24, "step": 2, "unit": "px", "label": "Padding", "default": 10 }
  ],
  "presets": [{ "name": "HP54 - Sapphire Marquee" }]
}
{% endschema %}
