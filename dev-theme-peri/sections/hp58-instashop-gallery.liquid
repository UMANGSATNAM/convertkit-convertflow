{%- style -%}
  #shopify-section-{{ section.id }} {
    background-color: {{ section.settings.bg_color | default: '#111827' }};
    padding-top: {{ section.settings.padding_top | default: 70 }}px;
    padding-bottom: {{ section.settings.padding_bottom | default: 70 }}px;
  }
  .hp58-insta__container { max-width: {{ section.settings.container_width | default: 1280 }}px; margin: 0 auto; padding: 0 24px; }
  .hp58-insta__header { text-align: center; margin-bottom: 40px; }
  .hp58-insta__eyebrow { font-family: 'Space Grotesk', sans-serif; font-size: 11px; font-weight: 700; letter-spacing: 3px; text-transform: uppercase; color: {{ section.settings.accent_color | default: '#06B6D4' }}; margin: 0 0 12px; }
  .hp58-insta__heading { font-family: 'Syne', sans-serif; font-size: clamp(26px, 3vw, 38px); font-weight: 800; color: #FFFFFF; margin: 0; }
  .hp58-insta__grid { display: grid; grid-template-columns: repeat({{ section.settings.columns | default: 5 }}, 1fr); gap: 12px; }
  .hp58-insta__item {
    aspect-ratio: 1; border-radius: 8px; overflow: hidden; position: relative;
    background: #0B0F19; border: 1px solid rgba(6, 182, 212, 0.2);
  }
  .hp58-insta__item img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.4s ease; filter: brightness(0.9) contrast(1.1); }
  .hp58-insta__item:hover img { transform: scale(1.08); }
  .hp58-insta__hover {
    position: absolute; inset: 0; background: rgba(11, 15, 25, 0.75);
    display: flex; align-items: center; justify-content: center; opacity: 0;
    transition: opacity 0.3s ease; color: #06B6D4; font-size: 24px;
  }
  .hp58-insta__item:hover .hp58-insta__hover { opacity: 1; }
  @media screen and (max-width: 992px) { .hp58-insta__grid { grid-template-columns: repeat(3, 1fr); } }
  @media screen and (max-width: 480px) { .hp58-insta__grid { grid-template-columns: repeat(2, 1fr); } }
{%- endstyle -%}
<div class="hp58-insta__container">
  <div class="hp58-insta__header">
    {%- if section.settings.eyebrow != blank -%}<p class="hp58-insta__eyebrow">{{ section.settings.eyebrow }}</p>{%- endif -%}
    <h2 class="hp58-insta__heading">{{ section.settings.heading | default: 'Tokyo Cyber Gallery' }}</h2>
  </div>
  <div class="hp58-insta__grid">
    {%- for block in section.blocks -%}
      <a href="{{ block.settings.url | default: '#' }}" class="hp58-insta__item" {{ block.shopify_attributes }}>
        {%- if block.settings.image != blank -%}
          <img height="{{ block.settings.image.height }}" width="{{ block.settings.image.width }}" src="{{ block.settings.image | image_url: width: 400 }}" alt="Cyber Glow" loading="lazy">
        {%- else -%}
          {{ 'lifestyle-1' | placeholder_svg_tag }}
        {%- endif -%}
        <div class="hp58-insta__hover">⚡</div>
      </a>
    {%- endfor -%}
  </div>
</div>
{% schema %}
{
  "name": "HP58 Gallery",
  "class": "section-hp58-insta",
  "settings": [
    { "type": "color", "id": "bg_color", "label": "Background", "default": "#111827" },
    { "type": "color", "id": "accent_color", "label": "Accent", "default": "#06B6D4" },
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "⚡ VISUAL LOG" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Tokyo Cyber Gallery" },
    { "type": "range", "id": "columns", "min": 3, "max": 6, "step": 1, "label": "Columns", "default": 5 },
    { "type": "range", "id": "padding_top", "min": 20, "max": 140, "step": 10, "unit": "px", "label": "Padding Top", "default": 70 },
    { "type": "range", "id": "padding_bottom", "min": 20, "max": 140, "step": 10, "unit": "px", "label": "Padding Bottom", "default": 70 },
    { "type": "range", "id": "container_width", "min": 1000, "max": 1440, "step": 20, "unit": "px", "label": "Container Width", "default": 1280 }
  ],
  "blocks": [{
    "type": "photo", "name": "Photo",
    "settings": [
      { "type": "image_picker", "id": "image", "label": "Image" },
      { "type": "url", "id": "url", "label": "Link" }
    ]
  }],
  "presets": [{
    "name": "HP58 Gallery",
    "blocks": [{ "type": "photo" }, { "type": "photo" }, { "type": "photo" }, { "type": "photo" }, { "type": "photo" }]
  }]
}
{% endschema %}
