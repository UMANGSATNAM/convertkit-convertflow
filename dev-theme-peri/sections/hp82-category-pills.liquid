<style>
@media screen and (max-width: 768px) {
  .hp82-category-pills {
    padding-left: 16px !important;
    padding-right: 16px !important;
    box-sizing: border-box !important;
  }
  img {
    max-width: 100% !important;
    height: auto !important;
  }
}
</style>
{%- style -%}#shopify-section-{{ section.id }}{background:{{ section.settings.bg_color | default: '#fff' }};padding:24px 0}.hp82-pills{max-width:1280px;margin:0 auto;padding:0 24px;display:flex;gap:12px;overflow-x:auto;scrollbar-width:none}.hp82-pills::-webkit-scrollbar{display:none}.hp82-pill{flex:0 0 auto;padding:10px 18px;border-radius:999px;background:#fff;border:1px solid rgba(0,0,0,.1);font-size:13px;font-weight:600;text-decoration:none;color:inherit;white-space:nowrap} .hp82-pill--active{background:#7C3AED;color:#fff;border-color:#7C3AED}{%- endstyle -%}<div class="hp82-pills">{% for block in section.blocks %}<a href="{{ block.settings.url }}" class="hp82-pill">{{ block.settings.label }}</a>{% else %}{% for cat in collections limit:6 %}<a href="{{ cat.url }}" class="hp82-pill">{{ cat.title }}</a>{% endfor %}{% endfor %}</div>{% schema %}
{
  "name": "HP82 Pills",
  "settings": [
    {
      "type": "color",
      "id": "bg_color",
      "label": "BG",
      "default": "#ffffff"
    }
  ],
  "blocks": [
    {
      "type": "pill",
      "name": "Pill",
      "settings": [
        {
          "type": "text",
          "id": "label",
          "label": "Label",
          "default": "All"
        },
        {
          "type": "url",
          "id": "url",
          "label": "URL"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "HP82 Pills"
    }
  ]
}
{% endschema %}