{%- style -%}
  #shopify-section-{{ section.id }} {
    background-color: {{ section.settings.bg_color | default: '#090D16' }};
    padding: {{ section.settings.padding_vertical | default: 30 }}px 0;
    border-bottom: 1px solid rgba(6, 182, 212, 0.15);
  }
  .hp64-press__container { max-width: 1280px; margin: 0 auto; padding: 0 24px; }
  .hp64-press__label {
    text-align: center; font-family: 'Space Grotesk', sans-serif; font-size: 11px; font-weight: 700;
    letter-spacing: 3px; text-transform: uppercase;
    color: {{ section.settings.label_color | default: '#06B6D4' }}; margin: 0 0 24px;
  }
  .hp64-press__logos { display: flex; align-items: center; justify-content: center; gap: 50px; flex-wrap: wrap; }
  .hp64-press__logo {
    height: {{ section.settings.logo_height | default: 26 }}px;
    opacity: 0.45; filter: grayscale(100%) brightness(3);
    transition: all 0.3s ease;
  }
  .hp64-press__logo:hover { opacity: 0.9; filter: grayscale(0%); }
  @media screen and (max-width: 768px) { .hp64-press__logos { gap: 30px; } .hp64-press__logo { height: 20px; } }
{%- endstyle -%}
<div class="hp64-press__container">
  {%- if section.settings.label != blank -%}<p class="hp64-press__label">{{ section.settings.label }}</p>{%- endif -%}
  <div class="hp64-press__logos">
    {%- for block in section.blocks -%}
      {%- if block.settings.logo != blank -%}<img src="{{ block.settings.logo | image_url: height: 60 }}" class="hp64-press__logo" alt="{{ block.settings.alt | default: 'Press' }}" loading="lazy">{%- endif -%}
    {%- endfor -%}
  </div>
</div>
{% schema %}
{
  "name": "HP64 Press",
  "class": "section-hp64-press",
  "settings": [
    { "type": "color", "id": "bg_color", "label": "Background", "default": "#090D16" },
    { "type": "color", "id": "label_color", "label": "Label", "default": "#06B6D4" },
    { "type": "text", "id": "label", "label": "Label", "default": "AS FEATURED IN" },
    { "type": "range", "id": "logo_height", "min": 16, "max": 50, "step": 2, "unit": "px", "label": "Logo Height", "default": 26 },
    { "type": "range", "id": "padding_vertical", "min": 10, "max": 60, "step": 5, "unit": "px", "label": "Padding", "default": 30 }
  ],
  "blocks": [{ "type": "logo", "name": "Logo", "settings": [
    { "type": "image_picker", "id": "logo", "label": "Logo" },
    { "type": "text", "id": "alt", "label": "Alt Text" }
  ]}],
  "presets": [{ "name": "HP64 Press" }]
}
{% endschema %}
