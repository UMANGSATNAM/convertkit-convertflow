{%- comment -%}
  PDP V51 — CYBERPUNK NEON EMERALD TERMINAL
  Design: Sci-fi emerald terminal prompt aesthetic, Fira Code display + Space Grotesk body
  Cyber Dark (#022C22), Emerald Cyber (#059669), Neon Mint (#34D399), Deep Forest (#064E3B)
{%- endcomment -%}

{%- assign current_variant = product.selected_or_first_available_variant -%}
{%- assign inv_qty = current_variant.inventory_quantity | default: 15 -%}
{%- if inv_qty <= 0 -%}{%- assign inv_qty = 7 -%}{%- endif -%}
{%- assign viewer_count = inv_qty | times: 7 | modulo: 50 | plus: 25 -%}
{%- assign sold_count = inv_qty | times: 15 | modulo: 400 | plus: 200 -%}

<style>
  @import url('https://fonts.googleapis.com/css2?family=Fira+Code:wght@600;700&family=Space+Grotesk:wght@500;600;700&display=swap');

  .pdpv51 {
    --bg: #022C22; --surface: #064E3B; --border: #047857; --border-dark: #34D399;
    --text: #ECFDF5; --muted: #6EE7B7; --accent: #34D399; --accent-hover: #059669;
    --emerald: #059669; --danger: #EF4444;
    --f-head: 'Fira Code', monospace; --f-body: 'Space Grotesk', sans-serif;
    --r: 6px; --r-pill: 9999px; --ease: all 0.2s ease;
    background: var(--bg); color: var(--text); font-family: var(--f-body); font-size: 15px; line-height: 1.5; -webkit-font-smoothing: antialiased;
  }
  .pdpv51 *, .pdpv51 *::before, .pdpv51 *::after { box-sizing: border-box; margin: 0; padding: 0; }
  .pdpv51 img { max-width: 100%; display: block; }
  .pdpv51 button { font-family: inherit; cursor: pointer; }
  .pdpv51__wrap { max-width: 1280px; margin: 0 auto; padding: 0 20px; }

  /* OFFER BAR */
  .pdpv51__offer { background: var(--accent); color: #000; padding: 10px; text-align: center; font-family: var(--f-head); font-size: 13px; font-weight: 700; letter-spacing: 2px; text-transform: uppercase; border-bottom: 2px solid var(--emerald); }

  /* GRID */
  .pdpv51__grid { display: grid; grid-template-columns: 1fr; gap: 36px; padding: 36px 0; }
  @media (min-width: 992px) { .pdpv51__grid { grid-template-columns: 1.1fr 1fr; gap: 48px; padding: 48px 0; } }

  /* GALLERY */
  .pdpv51__gal { display: flex; flex-direction: column; gap: 12px; }
  .pdpv51__hero { width: 100%; aspect-ratio: 1; border-radius: var(--r); overflow: hidden; background: var(--surface); border: 2px solid var(--border-dark); display: flex; align-items: center; justify-content: center; box-shadow: 0 0 24px rgba(52,211,153,0.25); position: relative; }
  .pdpv51__hero img { width: 100%; height: 100%; object-fit: contain; }
  .pdpv51__em-tag { position: absolute; top: 14px; left: 14px; background: rgba(2,44,34,0.9); border: 1px solid var(--accent); color: var(--accent); font-family: var(--f-head); font-size: 11px; font-weight: 700; padding: 4px 10px; border-radius: var(--r); }

  .pdpv51__thumbs { display: flex; gap: 10px; overflow-x: auto; }
  .pdpv51__th { width: 64px; height: 64px; flex-shrink: 0; border: 1px solid var(--border); border-radius: 6px; overflow: hidden; cursor: pointer; background: var(--surface); transition: var(--ease); }
  .pdpv51__th:hover, .pdpv51__th.on { border-color: var(--accent); box-shadow: 0 0 10px rgba(52,211,153,0.4); }
  .pdpv51__th img { width: 100%; height: 100%; object-fit: cover; }

  /* INFO PANEL */
  .pdpv51__info { display: flex; flex-direction: column; gap: 18px; }
  .pdpv51__crumb { font-family: var(--f-head); font-size: 12px; letter-spacing: 2px; color: var(--accent); font-weight: 700; text-transform: uppercase; }
  .pdpv51__title { font-family: var(--f-head); font-size: 28px; font-weight: 700; line-height: 1.2; color: var(--text); }
  @media (min-width: 992px) { .pdpv51__title { font-size: 36px; } }

  .pdpv51__rating { display: flex; align-items: center; gap: 8px; font-size: 14px; color: var(--muted); font-weight: 600; }
  .pdpv51__stars { color: var(--accent); }

  /* PRICE CARD */
  .pdpv51__price-card { background: var(--surface); border: 1px solid var(--border-dark); border-radius: var(--r); padding: 16px 20px; display: flex; align-items: baseline; justify-content: space-between; flex-wrap: wrap; gap: 10px; box-shadow: 0 0 16px rgba(52,211,153,0.15); }
  .pdpv51__price { font-family: var(--f-head); font-size: 34px; font-weight: 700; color: var(--accent); }
  .pdpv51__cmp { font-size: 18px; color: var(--muted); text-decoration: line-through; }
  .pdpv51__badge { background: var(--accent); color: #000; font-family: var(--f-head); font-weight: 700; font-size: 11px; padding: 4px 10px; border-radius: 4px; }

  /* VARIANTS */
  .pdpv51__vlabel { font-family: var(--f-head); font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 1px; color: var(--accent); margin-bottom: 8px; }
  .pdpv51__vopts { display: flex; flex-wrap: wrap; gap: 10px; }
  .pdpv51__vr { display: none; }
  .pdpv51__vbtn { padding: 10px 22px; border: 1px solid var(--border); border-radius: var(--r); background: var(--surface); color: var(--text); font-family: var(--f-body); font-size: 14px; font-weight: 700; transition: var(--ease); cursor: pointer; }
  .pdpv51__vbtn:hover { border-color: var(--accent); color: var(--accent); }
  .pdpv51__vr:checked + .pdpv51__vbtn { border-color: var(--accent); background: var(--accent); color: #000; }

  /* PROMO */
  .pdpv51__promo { background: var(--surface); border: 1px dashed var(--accent); border-radius: var(--r); padding: 14px 18px; display: flex; align-items: center; justify-content: space-between; font-size: 14px; font-weight: 700; color: var(--text); }
  .pdpv51__promo-code { background: var(--accent); color: #000; font-family: var(--f-head); padding: 4px 10px; border-radius: 4px; }

  /* URGENCY */
  .pdpv51__urg { background: var(--surface); border-left: 4px solid var(--accent); border-radius: 0 var(--r) var(--r) 0; padding: 16px; display: flex; flex-direction: column; gap: 8px; font-size: 14px; font-weight: 600; }
  .pdpv51__pulse { display: inline-block; width: 8px; height: 8px; background: var(--accent); border-radius: 50%; margin-right: 6px; animation: pdpv51p 1.5s infinite; }
  @keyframes pdpv51p { 0%,100% { opacity: 1; } 50% { opacity: 0.3; } }
  .pdpv51__bar { height: 6px; background: var(--border); border-radius: 3px; overflow: hidden; margin-top: 4px; }
  .pdpv51__fill { height: 100%; background: var(--accent); border-radius: 3px; }

  /* QTY & CTA */
  .pdpv51__qty-row { display: flex; align-items: center; gap: 14px; }
  .pdpv51__qty { display: flex; border: 1px solid var(--border); border-radius: var(--r); overflow: hidden; background: var(--surface); }
  .pdpv51__qty button { width: 44px; height: 48px; border: none; background: transparent; font-size: 18px; font-weight: 700; color: var(--text); }
  .pdpv51__qty input { width: 44px; height: 48px; border: none; border-left: 1px solid var(--border); border-right: 1px solid var(--border); text-align: center; font-size: 15px; font-weight: 700; background: transparent; color: var(--text); }

  .pdpv51__atc { width: 100%; padding: 18px; border: none; border-radius: var(--r); background: var(--accent); color: #000; font-family: var(--f-head); font-size: 16px; font-weight: 700; text-transform: uppercase; letter-spacing: 1px; transition: var(--ease); box-shadow: 0 0 20px rgba(52,211,153,0.3); }
  .pdpv51__atc:hover { background: var(--accent-hover); color: #FFF; transform: translateY(-1px); }
  .pdpv51__atc:disabled { opacity: 0.4; cursor: not-allowed; box-shadow: none; }
  .pdpv51__buynow .shopify-payment-button__button { border-radius: var(--r) !important; }

  /* TRUST */
  .pdpv51__trust { display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px; text-align: center; padding: 18px 0; border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); }
  .pdpv51__trust-item { font-family: var(--f-head); font-size: 11px; font-weight: 700; text-transform: uppercase; color: var(--muted); }
  .pdpv51__trust-icon { font-size: 20px; margin-bottom: 3px; color: var(--accent); }
  @media (max-width: 480px) { .pdpv51__trust { grid-template-columns: repeat(2, 1fr); } }

  /* ACCORDION */
  .pdpv51__acc { border-top: 1px solid var(--border); margin-top: 48px; }
  .pdpv51__acc-item { border-bottom: 1px solid var(--border); }
  .pdpv51__acc-btn { width: 100%; padding: 18px 0; border: none; background: transparent; display: flex; justify-content: space-between; align-items: center; font-family: var(--f-head); font-size: 15px; font-weight: 700; text-transform: uppercase; color: var(--text); }
  .pdpv51__acc-btn::after { content: '>'; font-size: 16px; color: var(--accent); }
  .pdpv51__acc-item.open .pdpv51__acc-btn::after { content: 'v'; }
  .pdpv51__acc-body { max-height: 0; overflow: hidden; transition: max-height 0.35s ease; }
  .pdpv51__acc-item.open .pdpv51__acc-body { max-height: 500px; }
  .pdpv51__acc-inner { padding: 0 0 18px; font-size: 15px; line-height: 1.7; color: var(--muted); }

  /* SECTION TITLE */
  .pdpv51__sh { font-family: var(--f-head); font-size: 22px; font-weight: 700; text-transform: uppercase; text-align: center; margin: 48px 0 20px; color: var(--text); letter-spacing: 1px; }

  /* REELS */
  .pdpv51__reels { display: flex; gap: 14px; overflow-x: auto; padding-bottom: 12px; }
  .pdpv51__reel { min-width: 180px; width: 180px; aspect-ratio: 9/16; background: #022C22; border: 1px solid var(--accent); border-radius: var(--r); overflow: hidden; flex-shrink: 0; position: relative; }
  .pdpv51__reel video { width: 100%; height: 100%; object-fit: cover; }
  .pdpv51__reel-cap { position: absolute; bottom: 0; left: 0; right: 0; padding: 10px; background: rgba(2,44,34,0.9); color: var(--accent); font-family: var(--f-head); font-size: 10px; }
  .pdpv51__reel-ph { width: 100%; height: 100%; display: flex; align-items: center; justify-content: center; color: var(--accent); font-size: 36px; }

  /* CARDS */
  .pdpv51__pgrid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px; }
  @media (min-width: 768px) { .pdpv51__pgrid { grid-template-columns: repeat(4, 1fr); } }
  .pdpv51__pcard { background: var(--surface); border-radius: var(--r); border: 1px solid var(--border); overflow: hidden; transition: var(--ease); text-decoration: none; color: inherit; display: block; }
  .pdpv51__pcard:hover { border-color: var(--accent); transform: translateY(-2px); box-shadow: 0 0 16px rgba(52,211,153,0.2); }
  .pdpv51__pcard img { width: 100%; aspect-ratio: 1; object-fit: cover; }
  .pdpv51__pcard-info { padding: 14px; }
  .pdpv51__pcard-title { font-family: var(--f-head); font-size: 13px; font-weight: 700; margin-bottom: 4px; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }
  .pdpv51__pcard-price { font-family: var(--f-head); font-size: 15px; font-weight: 700; color: var(--accent); }

  /* REVIEWS */
  .pdpv51__revgrid { display: grid; grid-template-columns: 1fr; gap: 14px; }
  @media (min-width: 768px) { .pdpv51__revgrid { grid-template-columns: repeat(3, 1fr); } }
  .pdpv51__revcard { background: var(--surface); border-radius: var(--r); border: 1px solid var(--border); padding: 20px; }
  .pdpv51__rev-stars { color: var(--accent); font-size: 14px; margin-bottom: 8px; }
  .pdpv51__rev-txt { font-size: 14px; line-height: 1.6; color: var(--muted); font-style: italic; margin-bottom: 12px; }
  .pdpv51__rev-auth { font-family: var(--f-head); font-size: 12px; font-weight: 700; }
  .pdpv51__rev-vf { font-family: var(--f-head); font-size: 10px; color: var(--accent); font-weight: 700; }

  /* RECENTLY VIEWED */
  #pdpv51-rv { display: none; }
  #pdpv51-rv.has { display: block; }

  /* STICKY ATC */
  .pdpv51__sticky { position: fixed; bottom: -80px; left: 0; width: 100%; background: var(--surface); border-top: 1px solid var(--accent); padding: 12px 24px; display: flex; justify-content: space-between; align-items: center; z-index: 9999; transition: bottom 0.3s ease; box-shadow: 0 -4px 20px rgba(0,0,0,0.5); }
  .pdpv51__sticky.show { bottom: 0; }
  .pdpv51__sticky-l { display: flex; align-items: center; gap: 14px; }
  .pdpv51__sticky-img { width: 42px; height: 42px; border-radius: 4px; border: 1px solid var(--border); object-fit: cover; }
  .pdpv51__sticky-t { font-family: var(--f-head); font-size: 13px; font-weight: 700; max-width: 200px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .pdpv51__sticky-p { font-family: var(--f-head); font-size: 15px; font-weight: 700; color: var(--accent); }
  .pdpv51__sticky-btn { background: var(--accent); color: #000; border: none; padding: 12px 26px; border-radius: var(--r); font-family: var(--f-head); font-weight: 700; font-size: 13px; text-transform: uppercase; transition: var(--ease); }
  .pdpv51__sticky-btn:hover { background: var(--accent-hover); color: #FFF; }
  @media (max-width: 600px) { .pdpv51__sticky-l { display: none; } .pdpv51__sticky-btn { width: 100%; padding: 15px; } }

  .pdpv51__toast { position: fixed; top: 20px; right: 20px; background: var(--accent); color: #000; border-radius: var(--r); padding: 12px 24px; font-family: var(--f-head); font-size: 13px; font-weight: 700; z-index: 10000; transform: translateX(130%); transition: transform 0.3s ease; box-shadow: 0 0 16px rgba(52,211,153,0.4); }
  .pdpv51__toast.show { transform: translateX(0); }
</style>

<div class="pdpv51">
  {% if section.settings.show_offer %}<div class="pdpv51__offer">{{ section.settings.offer_text }}</div>{% endif %}

  <div class="pdpv51__wrap">
    <div class="pdpv51__grid">

      <!-- GALLERY -->
      <div class="pdpv51__gal">
        <div class="pdpv51__hero" id="pdpv51-hero">
          <span class="pdpv51__em-tag">EMERALD_OS // V51</span>
          {% if product.featured_media %}<img height="{{ product.featured_media.height }}" width="{{ product.featured_media.width }}" src="{{ product.featured_media | image_url: width: 900 }}" alt="{{ product.title | escape }}" id="pdpv51-himg">{% else %}{{ 'product-1' | placeholder_svg_tag }}{% endif %}
        </div>
        <div class="pdpv51__thumbs">
          {% for media in product.media limit: 6 %}
            <div class="pdpv51__th {% if forloop.first %}on{% endif %}">
              <img height="{{ media.height }}" width="{{ media.width }}" src="{{ media | image_url: width: 150 }}" alt="{{ media.alt | escape }}" loading="lazy">
            </div>
          {% endfor %}
        </div>
      </div>

      <!-- INFO -->
      <div class="pdpv51__info">
        <div class="pdpv51__crumb">[SYS] {% if product.collections.size > 0 %}{{ product.collections.first.title }}{% else %}Emerald Terminal{% endif %}</div>
        <h1 class="pdpv51__title">{{ product.title }}</h1>

        {% if section.settings.show_rating %}
          <div class="pdpv51__rating">
            <span class="pdpv51__stars">★★★★★</span>
            <span>{{ section.settings.rating_val }} · {{ section.settings.rev_count }} Emerald Logs</span>
          </div>
        {% endif %}

        <div class="pdpv51__price-card">
          <span class="pdpv51__price" id="pdpv51-price">{{ current_variant.price | money }}</span>
          {% if current_variant.compare_at_price > current_variant.price %}
            <span class="pdpv51__cmp">{{ current_variant.compare_at_price | money }}</span>
            {%- assign sv = current_variant.compare_at_price | minus: current_variant.price | times: 100.0 | divided_by: current_variant.compare_at_price | round -%}
            <span class="pdpv51__badge">SAVE {{ sv }}%</span>
          {% endif %}
        </div>

        {% if product.variants.size > 1 %}
          {% for option in product.options_with_values %}
            <div>
              <div class="pdpv51__vlabel">[SELECT_{{ option.name | upcase }}]</div>
              <div class="pdpv51__vopts">
                {% for value in option.values %}
                  <label>
                    <input type="radio" name="pdpv51-o-{{ option.position }}" value="{{ value | escape }}" class="pdpv51__vr" data-pos="{{ option.position }}" {% if option.selected_value == value %}checked{% endif %}>
                    <span class="pdpv51__vbtn">{{ value }}</span>
                  </label>
                {% endfor %}
              </div>
            </div>
          {% endfor %}
        {% endif %}

        {% if section.settings.show_promo %}
          <div class="pdpv51__promo">
            <span>Emerald Pass: Apply {{ section.settings.promo_code }}</span>
            <span class="pdpv51__promo-code">Save {{ section.settings.promo_disc }}</span>
          </div>
        {% endif %}

        {% if section.settings.show_urgency %}
          <div class="pdpv51__urg">
            <div><span class="pdpv51__pulse"></span><strong>{{ viewer_count }}</strong> emerald terminals connected</div>
            <div>[SYS] <strong>{{ sold_count }}</strong> cyber orders completed today</div>
            <div>Buffer: Only <strong>{{ inv_qty }}</strong> remaining
              <div class="pdpv51__bar">
                {%- assign sp = inv_qty | times: 100 | divided_by: 45 -%}
                {%- if sp > 100 -%}{%- assign sp = 100 -%}{%- endif -%}
                <div class="pdpv51__fill" style="width:{{ sp }}%"></div>
              </div>
            </div>
            {% if section.settings.show_countdown %}
              <div>⏳ Terminal Timeout In: <strong id="pdpv51-cd" style="color:var(--accent)">{{ section.settings.cd_hrs }}h 00m 00s</strong></div>
            {% endif %}
          </div>
        {% endif %}

        <div class="pdpv51__qty-row">
          <div class="pdpv51__qty">
            <button type="button" id="pdpv51-qm">−</button>
            <input type="number" id="pdpv51-qi" value="1" min="1" max="10" readonly>
            <button type="button" id="pdpv51-qp">+</button>
          </div>
          {% form 'product', product, id: 'pdpv51-form', style: 'flex:1' %}
            <input type="hidden" name="id" id="pdpv51-vid" value="{{ current_variant.id }}">
            <input type="hidden" name="quantity" id="pdpv51-fq" value="1">
            <button type="submit" name="add" class="pdpv51__atc" id="pdpv51-atc" {% unless current_variant.available %}disabled{% endunless %}>
              {% if current_variant.available %}ADD TO CART — {{ current_variant.price | money }}{% else %}SOLD OUT{% endif %}
            </button>
            <div class="pdpv51__buynow" style="margin-top:10px">{{ form | payment_button }}</div>
          {% endform %}
        </div>

        <div class="pdpv51__trust">
          <div class="pdpv51__trust-item"><div class="pdpv51__trust-icon">🔒</div>ENCRYPTED</div>
          <div class="pdpv51__trust-item"><div class="pdpv51__trust-icon">⚡</div>FAST NODE</div>
          <div class="pdpv51__trust-item"><div class="pdpv51__trust-icon">↩️</div>RETURNS</div>
          <div class="pdpv51__trust-item"><div class="pdpv51__trust-icon">✓</div>VERIFIED</div>
        </div>
      </div>
    </div>

    <!-- ACCORDION -->
    <div class="pdpv51__acc">
      <div class="pdpv51__acc-item open">
        <button class="pdpv51__acc-btn" type="button">Emerald Architecture & Spec</button>
        <div class="pdpv51__acc-body"><div class="pdpv51__acc-inner">{{ product.description }}</div></div>
      </div>
      {% if section.settings.em_specs != blank %}
        <div class="pdpv51__acc-item">
          <button class="pdpv51__acc-btn" type="button">Technical Telemetry & Protocol</button>
          <div class="pdpv51__acc-body"><div class="pdpv51__acc-inner">{{ section.settings.em_specs }}</div></div>
        </div>
      {% endif %}
    </div>

    <!-- FBT -->
    {% if section.settings.fbt_coll != blank %}
      {%- assign fbt = collections[section.settings.fbt_coll] -%}
      {% if fbt.products.size > 0 %}
        <div class="pdpv51__sh">Emerald Terminal Modules</div>
        <div class="pdpv51__pgrid">
          {% for item in fbt.products limit: 4 %}
            <a href="{{ item.url }}" class="pdpv51__pcard">
              <img height="{{ item.featured_image.height }}" width="{{ item.featured_image.width }}" src="{{ item.featured_image | image_url: width: 400 }}" alt="{{ item.title | escape }}" loading="lazy">
              <div class="pdpv51__pcard-info">
                <div class="pdpv51__pcard-title">{{ item.title }}</div>
                <div class="pdpv51__pcard-price">{{ item.price | money }}</div>
              </div>
            </a>
          {% endfor %}
        </div>
      {% endif %}
    {% endif %}

    <!-- REELS -->
    {% if section.settings.show_reels %}
      <div class="pdpv51__sh">Terminal Telemetry</div>
      <div class="pdpv51__reels">
        {% if section.settings.r1 != blank %}<div class="pdpv51__reel"><video src="{{ section.settings.r1 }}" muted playsinline loop></video><div class="pdpv51__reel-cap">[EMERALD_LOG_1]</div></div>{% else %}<div class="pdpv51__reel"><div class="pdpv51__reel-ph">▶</div></div>{% endif %}
        {% if section.settings.r2 != blank %}<div class="pdpv51__reel"><video src="{{ section.settings.r2 }}" muted playsinline loop></video><div class="pdpv51__reel-cap">[EMERALD_LOG_2]</div></div>{% else %}<div class="pdpv51__reel"><div class="pdpv51__reel-ph">▶</div></div>{% endif %}
        {% if section.settings.r3 != blank %}<div class="pdpv51__reel"><video src="{{ section.settings.r3 }}" muted playsinline loop></video><div class="pdpv51__reel-cap">[EMERALD_LOG_3]</div></div>{% else %}<div class="pdpv51__reel"><div class="pdpv51__reel-ph">▶</div></div>{% endif %}
      </div>
    {% endif %}

    <!-- REVIEWS -->
    {% if section.settings.show_reviews %}
      <div class="pdpv51__sh" id="reviews">Terminal Operator Logs</div>
      <div class="pdpv51__revgrid">
        {% if section.settings.rv1 != blank %}<div class="pdpv51__revcard"><div class="pdpv51__rev-stars">★★★★★</div><div class="pdpv51__rev-txt">"{{ section.settings.rv1 }}"</div><div class="pdpv51__rev-auth">{{ section.settings.rv1n }}</div><div class="pdpv51__rev-vf">✓ Verified Operator</div></div>{% endif %}
        {% if section.settings.rv2 != blank %}<div class="pdpv51__revcard"><div class="pdpv51__rev-stars">★★★★★</div><div class="pdpv51__rev-txt">"{{ section.settings.rv2 }}"</div><div class="pdpv51__rev-auth">{{ section.settings.rv2n }}</div><div class="pdpv51__rev-vf">✓ Verified Operator</div></div>{% endif %}
        {% if section.settings.rv3 != blank %}<div class="pdpv51__revcard"><div class="pdpv51__rev-stars">★★★★★</div><div class="pdpv51__rev-txt">"{{ section.settings.rv3 }}"</div><div class="pdpv51__rev-auth">{{ section.settings.rv3n }}</div><div class="pdpv51__rev-vf">✓ Verified Operator</div></div>{% endif %}
      </div>
    {% endif %}

    <!-- FEATURED -->
    {% if section.settings.feat_coll != blank %}
      {%- assign fc = collections[section.settings.feat_coll] -%}
      {% if fc.products.size > 0 %}
        <div class="pdpv51__sh">Recommended Emerald Nodes</div>
        <div class="pdpv51__pgrid">
          {% for item in fc.products limit: 4 %}
            {% if item.handle != product.handle %}
              <a href="{{ item.url }}" class="pdpv51__pcard">
                <img height="{{ item.featured_image.height }}" width="{{ item.featured_image.width }}" src="{{ item.featured_image | image_url: width: 400 }}" alt="{{ item.title | escape }}" loading="lazy">
                <div class="pdpv51__pcard-info">
                  <div class="pdpv51__pcard-title">{{ item.title }}</div>
                  <div class="pdpv51__pcard-price">{{ item.price | money }}</div>
                </div>
              </a>
            {% endif %}
          {% endfor %}
        </div>
      {% endif %}
    {% endif %}

    <!-- RECENTLY VIEWED -->
    <div id="pdpv51-rv">
      <div class="pdpv51__sh">Recently Viewed</div>
      <div class="pdpv51__pgrid" id="pdpv51-rvg"></div>
    </div>
  </div>
</div>

<!-- STICKY ATC -->
<div class="pdpv51__sticky" id="pdpv51-sticky">
  <div class="pdpv51__sticky-l">
    {% if product.featured_media %}<img height="{{ product.featured_media.height }}" width="{{ product.featured_media.width }}" src="{{ product.featured_media | image_url: width: 80 }}" class="pdpv51__sticky-img" alt="{{ product.title | escape }}">{% endif %}
    <div>
      <div class="pdpv51__sticky-t">{{ product.title }}</div>
      <div class="pdpv51__sticky-p" id="pdpv51-sp">{{ current_variant.price | money }}</div>
    </div>
  </div>
  
      <!-- D2C SCARCITY & NOTIFY -->
      {% if product.available %}
        <div style="background:#FFF3E0; border:1px solid #FFCC80; padding:10px; border-radius:8px; margin-bottom:12px; display:flex; align-items:center; gap:12px;">
          <svg style="width:24px; height:24px; color:#E65100; flex-shrink:0;" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
          <div style="font-size:13px; color:#E65100;">
            <div style="font-weight:700;">Limited Time Offer Ends In:</div>
            <div style="font-size:16px; font-weight:700; margin-top:2px;"><b>{% render 'countdown-timer', product: product %}</b></div>
          </div>
        </div>
      {% endif %}

      {% render 'back-in-stock', product: product %}

      {% if product.available %}
      <button type="button" class="pdpv51__sticky-btn" id="pdpv51-sbtn">Add to Cart</button>
      {% else %}
      <button class="pdpv51__sticky-btn" disabled style="opacity:0.5; cursor:not-allowed;">SOLD OUT</button>
      {% endif %}
</div>

<div class="pdpv51__toast" id="pdpv51-toast">Terminal: Added to cart!</div>

<script>
(function(){
  var PJ={{ product | json }}, cid={{ current_variant.id }};
  function getO(){ var o=[]; document.querySelectorAll('.pdpv51__vr:checked').forEach(function(r){ o.push({p:parseInt(r.dataset.pos),v:r.value}); }); o.sort(function(a,b){return a.p-b.p}); return o.map(function(x){return x.v}); }
  function findV(o){ for(var i=0;i<PJ.variants.length;i++){ var v=PJ.variants[i],ok=true; for(var j=0;j<o.length;j++){ if(v.options[j]!==o[j]){ ok=false; break; } } if(ok) return v; } return null; }
  function upd(){
    var o=getO(); if(!o.length) return; var v=findV(o); if(!v) return; cid=v.id;
    document.getElementById('pdpv51-vid').value=v.id;
    var pf=(v.price/100).toLocaleString('en-US',{style:'currency',currency:PJ.currency||'USD'});
    var pe=document.getElementById('pdpv51-price'); if(pe) pe.textContent=pf;
    var sp=document.getElementById('pdpv51-sp'); if(sp) sp.textContent=pf;
    var ab=document.getElementById('pdpv51-atc');
    if(ab){ ab.disabled=!v.available; ab.textContent=v.available?'ADD TO CART — '+pf:'SOLD OUT'; }
    var u=new URL(window.location); u.searchParams.set('variant',v.id); history.replaceState({},'',u);
  }
  document.querySelectorAll('.pdpv51__vr').forEach(function(r){ r.addEventListener('change',upd); });

  var fm=document.getElementById('pdpv51-form');
  if(fm) fm.addEventListener('submit',function(e){
    e.preventDefault(); var b=document.getElementById('pdpv51-atc'), ot=b.textContent;
    b.disabled=true; b.textContent='ADDING...';
    var q=parseInt(document.getElementById('pdpv51-qi').value)||1;
    document.getElementById('pdpv51-fq').value=q;
    fetch('/cart/add.js',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:cid,quantity:q})})
    .then(function(r){return r.json();})
    .then(function(){ b.textContent='✓ ADDED!'; toast51(); setTimeout(function(){ b.disabled=false; b.textContent=ot; },2000); })
    .catch(function(){ b.textContent='ERROR'; setTimeout(function(){ b.disabled=false; b.textContent=ot; },2000); });
  });

  function toast51(){ var t=document.getElementById('pdpv51-toast'); t.classList.add('show'); setTimeout(function(){ t.classList.remove('show'); },3000); }

  var qi=document.getElementById('pdpv51-qi');
  document.getElementById('pdpv51-qm').addEventListener('click',function(){ var v=parseInt(qi.value); if(v>1) qi.value=v-1; });
  document.getElementById('pdpv51-qp').addEventListener('click',function(){ var v=parseInt(qi.value); if(v<10) qi.value=v+1; });

  document.querySelectorAll('.pdpv51__th').forEach(function(th){
    th.addEventListener('click',function(){
      document.querySelectorAll('.pdpv51__th').forEach(function(t){ t.classList.remove('on'); });
      th.classList.add('on');
      var hi=document.getElementById('pdpv51-himg'), ti=th.querySelector('img');
      if(hi&&ti) hi.src=ti.src.replace(/width=\d+/,'width=900');
    });
  });

  var sb=document.getElementById('pdpv51-sticky'), ab=document.getElementById('pdpv51-atc');
  if(sb&&ab) window.addEventListener('scroll',function(){ sb.classList.toggle('show', ab.getBoundingClientRect().top<-50); });
  var sbn=document.getElementById('pdpv51-sbtn');
  if(sbn) sbn.addEventListener('click',function(){ document.getElementById('pdpv51-atc').click(); });

  document.querySelectorAll('.pdpv51__acc-btn').forEach(function(b){ b.addEventListener('click',function(){ b.closest('.pdpv51__acc-item').classList.toggle('open'); }); });

  var ce=document.getElementById('pdpv51-cd');
  if(ce){
    var hrs=parseInt('{{ section.settings.cd_hrs | default: 6 }}')||6, ts=hrs*3600;
    var sk=sessionStorage.getItem('pdpv51_cd'); if(sk) ts=parseInt(sk);
    setInterval(function(){ ts--; if(ts<=0){ ce.textContent='EXPIRED'; return; } var h=Math.floor(ts/3600), m=Math.floor((ts%3600)/60), s=ts%60; ce.textContent=h+'h '+(m<10?'0':'')+m+'m '+(s<10?'0':'')+s+'s'; sessionStorage.setItem('pdpv51_cd',ts); },1000);
  }

  if('IntersectionObserver' in window){
    var ro=new IntersectionObserver(function(es){ es.forEach(function(e){ var v=e.target.querySelector('video'); if(v){ e.isIntersecting?v.play():v.pause(); } }); },{threshold:0.5});
    document.querySelectorAll('.pdpv51__reel').forEach(function(r){ ro.observe(r); });
  }

  var RK='pdpv51_rv', cp={handle:'{{ product.handle }}',title:'{{ product.title | escape }}',price:'{{ product.price | money }}',image:'{{ product.featured_image | image_url: width: 400 }}',url:'{{ product.url }}'};
  try{ var its=JSON.parse(localStorage.getItem(RK)||'[]'); its=its.filter(function(i){ return i.handle!==cp.handle; }); its.unshift(cp); if(its.length>8) its=its.slice(0,8); localStorage.setItem(RK,JSON.stringify(its)); }catch(e){}
  try{
    var its2=JSON.parse(localStorage.getItem(RK)||'[]').filter(function(i){ return i.handle!==cp.handle; });
    var c=document.getElementById('pdpv51-rv'), g=document.getElementById('pdpv51-rvg');
    if(g&&its2.length>0){ c.classList.add('has'); var h=''; its2.slice(0,4).forEach(function(i){ h+='<a href="'+i.url+'" class="pdpv51__pcard"><img src="'+i.image+'" alt="'+i.title+'" loading="lazy"><div class="pdpv51__pcard-info"><div class="pdpv51__pcard-title">'+i.title+'</div><div class="pdpv51__pcard-price">'+i.price+'</div></div></a>'; }); g.innerHTML=h; }
  }catch(e){}
})();
</script>

{% schema %}
{
  "name": "PDP V51 — Emerald Cyber",
  "tag": "section",
  "class": "pdpv51-section",
  "settings": [
    { "type": "header", "content": "Rating" },
    { "type": "checkbox", "id": "show_rating", "label": "Show rating", "default": true },
    { "type": "text", "id": "rating_val", "label": "Rating score", "default": "4.97" },
    { "type": "text", "id": "rev_count", "label": "Total reviews", "default": "2,050" },
    { "type": "header", "content": "Offer Banner" },
    { "type": "checkbox", "id": "show_offer", "label": "Show offer", "default": true },
    { "type": "text", "id": "offer_text", "label": "Offer text", "default": "[SYS] EMERALD TERMINAL: ENJOY 20% DISCOUNT WITH CODE 'EMERALD20'" },
    { "type": "header", "content": "Promo" },
    { "type": "checkbox", "id": "show_promo", "label": "Show promo", "default": true },
    { "type": "text", "id": "promo_code", "label": "Code", "default": "EMERALD20" },
    { "type": "text", "id": "promo_disc", "label": "Discount", "default": "20%" },
    { "type": "header", "content": "Urgency" },
    { "type": "checkbox", "id": "show_urgency", "label": "Show urgency", "default": true },
    { "type": "checkbox", "id": "show_countdown", "label": "Show countdown", "default": true },
    { "type": "range", "id": "cd_hrs", "label": "Countdown hours", "min": 1, "max": 48, "default": 6 },
    { "type": "header", "content": "Accordion" },
    { "type": "richtext", "id": "em_specs", "label": "Technical telemetry & protocol" },
    { "type": "header", "content": "FBT" },
    { "type": "collection", "id": "fbt_coll", "label": "FBT collection" },
    { "type": "header", "content": "Reels" },
    { "type": "checkbox", "id": "show_reels", "label": "Show reels", "default": true },
    { "type": "url", "id": "r1", "label": "Reel 1 URL" },
    { "type": "url", "id": "r2", "label": "Reel 2 URL" },
    { "type": "url", "id": "r3", "label": "Reel 3 URL" },
    { "type": "header", "content": "Reviews" },
    { "type": "checkbox", "id": "show_reviews", "label": "Show reviews", "default": true },
    { "type": "text", "id": "rv1", "label": "Review 1", "default": "Emerald terminal styling is slick and ultra fast!" },
    { "type": "text", "id": "rv1n", "label": "Author 1", "default": "Zane T." },
    { "type": "text", "id": "rv2", "label": "Review 2", "default": "Neon mint accents on deep forest green looks great." },
    { "type": "text", "id": "rv2n", "label": "Author 2", "default": "Orion C." },
    { "type": "text", "id": "rv3", "label": "Review 3", "default": "100% cyberpunk quality." },
    { "type": "text", "id": "rv3n", "label": "Author 3", "default": "Kaelen V." },
    { "type": "header", "content": "Featured" },
    { "type": "collection", "id": "feat_coll", "label": "Featured collection" }
  ],
  "presets": [{ "name": "PDP V51 — Emerald Cyber" }]
}
{% endschema %}
